---
phase: 05-security-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 01
  - 02
files_modified:
  - .env.example
  - backend/.env.example
autonomous: true
requirements:
  - SEC-04-ENV-EXAMPLE-DOCS
  - SEC-05-REGRESSION-TESTS-PASS
tags:
  - security
  - documentation
  - regression-tests

must_haves:
  truths:
    - "Repo root contains .env.example documenting all required environment variables"
    - "backend/.env.example documents the Django-only subset"
    - ".env.example placeholder names match real backend/.env variable names exactly"
    - "Full backend test suite passes (final regression gate for the phase)"
    - "Manual smoke-test checklist is recorded in the plan summary for the human verifier"
  artifacts:
    - path: ".env.example"
      provides: "Root-level env var documentation for new developers"
      contains: "SECRET_KEY="
    - path: "backend/.env.example"
      provides: "Django-only env var documentation"
      contains: "SECRET_KEY="
  key_links:
    - from: ".env.example"
      to: "backend/.env"
      via: "variable name parity (developers copy keys from example to real .env)"
      pattern: "SECRET_KEY=|SPOTIFY_CLIENT_ID=|SPOTIFY_CLIENT_SECRET=|SPOTIFY_REDIRECT_URI=|DEBUG=|OPENAI_API_KEY=|OAUTHLIB_INSECURE_TRANSPORT=|FRONTEND_URL="
---

<objective>
Document required environment variables and run the final phase regression check.

Purpose: Deliver SEC-04 (`.env.example` files at repo root and `backend/`) so a developer cloning the repo can set up their environment without source-diving, and deliver SEC-05 (full test suite passes after all three security changes are applied).

This plan runs AFTER Plans 01 and 02 (Wave 2) because the example files must reflect the final variable set that `settings.py` will read via `config()` after Plan 01 rewires SECRET_KEY/DEBUG.

Output: New `.env.example` at repo root, new `backend/.env.example`, full pytest suite confirmed green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-security-hardening/05-RESEARCH.md
@.planning/phases/05-security-hardening/05-PATTERNS.md
@.planning/phases/05-security-hardening/05-VALIDATION.md
@.planning/phases/05-security-hardening/05-01-SUMMARY.md
@.planning/phases/05-security-hardening/05-02-SUMMARY.md

<interfaces>
<!-- Final variable set that backend/.env must contain after Plan 01 -->

```
SECRET_KEY=<generated>
DEBUG=True
SPOTIFY_CLIENT_ID=<dashboard value>
SPOTIFY_CLIENT_SECRET=<dashboard value>
SPOTIFY_REDIRECT_URI=http://127.0.0.1:8000/spotify/callback/
OPENAI_API_KEY=<optional>
OAUTHLIB_INSECURE_TRANSPORT=True
FRONTEND_URL=http://127.0.0.1:3000
```

<!-- frontend/.env.local needs only -->
```
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
```

<!-- .gitignore at repo root already excludes .env files at all levels (verified in RESEARCH);
     .env.example is explicitly NOT ignored — it should be tracked in git -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example at repo root and backend/.env.example</name>
  <files>.env.example, backend/.env.example</files>
  <read_first>
    - .planning/phases/05-security-hardening/05-RESEARCH.md (section: "Documentation Content Map (.env.example)")
    - .planning/phases/05-security-hardening/05-PATTERNS.md (sections: ".env.example (repo root) — create new" and "backend/.env.example — create new")
    - backend/.env (confirm the current set of variable NAMES — values must NOT be copied into example files)
    - .gitignore (confirm `.env.example` is NOT ignored — files must be trackable in git)
  </read_first>
  <action>
  Create exactly two new files. Both files MUST contain placeholder keys with EMPTY values (no real credentials). Use bare `KEY=VALUE` format (no quotes, no spaces around `=`) to match the convention in `backend/.env`.

  File 1 — `.env.example` at the repo root. Content verbatim:
  ```
  # ============================================================
  # SongScope — Environment Variables
  # Copy this file to .env and fill in the values.
  # ============================================================

  # ---- Django Backend (also used by backend/.env) ----

  # Django secret key — generate with:
  # python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
  SECRET_KEY=

  # Debug mode — set to True for local development only
  DEBUG=False

  # Spotify OAuth credentials — get from https://developer.spotify.com/dashboard
  SPOTIFY_CLIENT_ID=
  SPOTIFY_CLIENT_SECRET=
  SPOTIFY_REDIRECT_URI=http://127.0.0.1:8000/spotify/callback/

  # OpenAI API key (optional — used for AI feedback feature)
  OPENAI_API_KEY=

  # Allow insecure OAuth transport for local HTTP development (set to True locally)
  OAUTHLIB_INSECURE_TRANSPORT=True

  # Frontend URL (where Next.js runs)
  FRONTEND_URL=http://127.0.0.1:3000

  # ---- Next.js Frontend (also used by frontend/.env.local) ----

  # Backend URL — must match where Django runs
  NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
  ```

  File 2 — `backend/.env.example`. Content verbatim:
  ```
  # ============================================================
  # SongScope — Django Backend Environment Variables
  # Copy this file to backend/.env and fill in the values.
  # ============================================================

  # Django secret key — generate with:
  # python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
  SECRET_KEY=

  # Debug mode — set to True for local development only
  DEBUG=False

  # Spotify OAuth credentials — get from https://developer.spotify.com/dashboard
  SPOTIFY_CLIENT_ID=
  SPOTIFY_CLIENT_SECRET=
  SPOTIFY_REDIRECT_URI=http://127.0.0.1:8000/spotify/callback/

  # OpenAI API key (optional — used for AI feedback feature)
  OPENAI_API_KEY=

  # Allow insecure OAuth transport for local HTTP development
  OAUTHLIB_INSECURE_TRANSPORT=True

  # Frontend URL (where Next.js runs)
  FRONTEND_URL=http://127.0.0.1:3000
  ```

  Variable names MUST match `backend/.env` exactly (`SECRET_KEY`, `DEBUG`, `SPOTIFY_CLIENT_ID`, `SPOTIFY_CLIENT_SECRET`, `SPOTIFY_REDIRECT_URI`, `OPENAI_API_KEY`, `OAUTHLIB_INSECURE_TRANSPORT`, `FRONTEND_URL`). No real values from `backend/.env` may be copied (verify after writing).

  Confirm `.gitignore` does NOT ignore `.env.example`: `git check-ignore .env.example backend/.env.example` should output NOTHING (both paths must be trackable). If either is ignored, STOP and surface a gap (the `.gitignore` rule would need adjustment, which is out of scope for this task).
  </action>
  <verify>
    <automated>test -f .env.example &amp;&amp; test -f backend/.env.example &amp;&amp; ! grep -q "4f0d1b7c78cd40acaefc14c293161f49" .env.example &amp;&amp; ! grep -q "4f0d1b7c78cd40acaefc14c293161f49" backend/.env.example &amp;&amp; ! grep -q "6bf1a7e2b72e4e36be9179772e15fe35" .env.example &amp;&amp; ! grep -q "6bf1a7e2b72e4e36be9179772e15fe35" backend/.env.example &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Source: file `.env.example` exists at repo root (verify with `test -f .env.example && echo PRESENT` outputs `PRESENT`).
    - Source: file `backend/.env.example` exists (verify with `test -f backend/.env.example && echo PRESENT` outputs `PRESENT`).
    - Source: `.env.example` contains the literal line `SECRET_KEY=` (verify with `grep -cE "^SECRET_KEY=$" .env.example` outputs `1`).
    - Source: `.env.example` contains the literal line `SPOTIFY_CLIENT_SECRET=` (verify with `grep -cE "^SPOTIFY_CLIENT_SECRET=$" .env.example` outputs `1`).
    - Source: `.env.example` contains `NEXT_PUBLIC_BACKEND_URL=http://localhost:8000` (verify with `grep -c "NEXT_PUBLIC_BACKEND_URL=http://localhost:8000" .env.example` outputs `1`).
    - Source: `backend/.env.example` contains `SECRET_KEY=` and `SPOTIFY_CLIENT_SECRET=` (verify each with `grep -cE "^SECRET_KEY=$" backend/.env.example` outputs `1` and `grep -cE "^SPOTIFY_CLIENT_SECRET=$" backend/.env.example` outputs `1`).
    - Source: `backend/.env.example` does NOT mention `NEXT_PUBLIC_BACKEND_URL` (verify with `grep -c "NEXT_PUBLIC_BACKEND_URL" backend/.env.example` outputs `0`).
    - Source: neither example file contains the real Spotify CLIENT_SECRET value `4f0d1b7c78cd40acaefc14c293161f49` (verify with `grep -c "4f0d1b7c78cd40acaefc14c293161f49" .env.example backend/.env.example` outputs `0:0`).
    - Source: neither example file contains the real Spotify CLIENT_ID value `6bf1a7e2b72e4e36be9179772e15fe35` (verify with `grep -c "6bf1a7e2b72e4e36be9179772e15fe35" .env.example backend/.env.example` outputs `0:0`).
    - Behavior: example files are not gitignored — `git check-ignore .env.example backend/.env.example 2>&1; echo "exit=$?"` exits non-zero (i.e. nothing is ignored).
  </acceptance_criteria>
  <done>Both example files exist at the correct paths with the documented content, contain placeholder (empty) credential values, and are trackable in git.</done>
</task>

<task type="auto">
  <name>Task 2: Run full regression test suite as phase gate</name>
  <files></files>
  <read_first>
    - .planning/phases/05-security-hardening/05-VALIDATION.md (section: "Sampling Rate" — note "Before /gsd-verify-work: Full suite must be green")
    - .planning/phases/05-security-hardening/05-01-SUMMARY.md (confirm Plan 01 changes landed)
    - .planning/phases/05-security-hardening/05-02-SUMMARY.md (confirm Plan 02 changes landed)
  </read_first>
  <action>
  Run the full backend test suite (NOT `-x`, NOT `--tb=short` — we want the complete tally) and capture the count. This is the phase regression gate that proves the SECRET_KEY rotation + CSRF re-enable did not break anything.

  Step 1 — Run: `cd backend && python -m pytest tests/ -q`

  Step 2 — Confirm the summary line shows all tests passing. The expected pattern is "`N passed in M.MMs`" where N is the test count (RESEARCH.md states 77 tests; the count may have grown if subsequent phases added tests, but no test should FAIL or ERROR).

  Step 3 — Re-run the smoke check from Plan 01 to confirm Django still boots with the new SECRET_KEY plumbing: `cd backend && python -c "import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings'); import django; django.setup(); from django.conf import settings; assert 'insecure' not in settings.SECRET_KEY; print('OK')"`

  Step 4 — Document the manual smoke-test checklist (cannot be automated — requires a running dev environment) inside the SUMMARY at the end of this plan. The checklist (copied from 05-VALIDATION.md §Manual Smoke Tests):
  1. CSRF round-trip: Log in via Spotify → submit a like/dislike → verify 200 response (not 403)
  2. CSRF rejection: `curl -X POST http://localhost:8000/api/submit-feedback/` without CSRF cookie → expect 403
  3. Frontend loads: `cd frontend && npm run dev` → visit `http://localhost:3000` → no console errors about missing env vars
  4. Spotify OAuth still works: Visit `/spotify-login/` → complete OAuth flow → redirect to `/profile`

  Do NOT execute manual smoke tests automatically. Record them as outstanding human-verification items in the SUMMARY for the user to run before declaring the phase complete.

  If ANY pytest test fails or errors, STOP and surface the failure to the user. Do not patch tests to make them pass — investigate whether Plan 01 or Plan 02 introduced a real regression first.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -m pytest tests/ -q 2&gt;&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `cd backend && python -m pytest tests/ -q` exits 0.
    - Behavior: pytest summary line contains the word "passed" and does NOT contain the word "failed" or "error" (verify by piping pytest output: `cd backend && python -m pytest tests/ -q 2>&1 | tail -1 | grep -qE "[0-9]+ passed" && cd backend && python -m pytest tests/ -q 2>&1 | tail -1 | grep -vqE "failed|error"`).
    - Behavior: Django smoke check passes — `cd backend && python -c "import os; os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'config.settings'); import django; django.setup(); from django.conf import settings; assert 'insecure' not in settings.SECRET_KEY; print('OK')"` outputs `OK`.
    - Artifact: `.planning/phases/05-security-hardening/05-03-SUMMARY.md` contains a Manual Smoke Tests section listing the four checklist items (verify with `grep -c "Manual Smoke Tests" .planning/phases/05-security-hardening/05-03-SUMMARY.md` outputs at least `1`).
  </acceptance_criteria>
  <done>Full backend test suite passes (no regression from Plans 01 + 02); Django boot smoke check passes; manual smoke-test checklist recorded in this plan's SUMMARY for the user to action before phase verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.example` (tracked in git) ↔ real `.env` (gitignored) | Example file must contain NO real credentials; it teaches developers which variables to set |
| pytest harness ↔ Django app | Test suite acts as regression gate against Plans 01 and 02 |
| Manual smoke tests ↔ live dev environment | Some CSRF and OAuth behaviors require a running browser session and cannot be automated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure | `.env.example` accidentally containing real credentials | mitigate | Example files use placeholder empty values (`SECRET_KEY=`, `SPOTIFY_CLIENT_SECRET=`). Acceptance check explicitly greps for the known real CLIENT_ID and CLIENT_SECRET values and asserts they are NOT present. |
| T-05-10 | Tampering | `.env.example` accidentally gitignored, never tracked | mitigate | `git check-ignore` acceptance check confirms the files are trackable. Default `.gitignore` rule `.env` does not match `.env.example` (verified in RESEARCH.md §Pitfall 5). |
| T-05-11 | Repudiation | Phase declared complete with broken tests | mitigate | Full pytest suite is the gate. Exit code 0 + summary line containing "passed" and NOT "failed"/"error" is required. |
| T-05-12 | Spoofing | Live CSRF round-trip / OAuth flow broken in production but tests pass | accept | Manual smoke tests required (cannot be automated without a browser). Listed in SUMMARY as outstanding human verification before `/gsd-verify-work`. |
</threat_model>

<verification>
- `test -f .env.example && test -f backend/.env.example` exits 0
- `grep -c "4f0d1b7c78cd40acaefc14c293161f49" .env.example backend/.env.example` outputs `0:0` (no real CLIENT_SECRET)
- `grep -c "6bf1a7e2b72e4e36be9179772e15fe35" .env.example backend/.env.example` outputs `0:0` (no real CLIENT_ID)
- `cd backend && python -m pytest tests/ -q` exits 0 with no failures or errors
- Manual smoke-test checklist documented in 05-03-SUMMARY.md
</verification>

<success_criteria>
- `.env.example` at repo root documents all 9 variables (8 backend + 1 frontend).
- `backend/.env.example` documents the 8 Django-side variables.
- Neither example file contains real Spotify or OpenAI credentials.
- Both example files are tracked in git (not blocked by `.gitignore`).
- Full backend test suite passes after Plans 01 + 02 changes — regression gate satisfied.
- Manual smoke-test checklist recorded in SUMMARY for the user to run before declaring the phase complete.
- All four phase deliverables from the planning context are now satisfied: SECRET_KEY rotation (Plan 01), CLIENT_SECRET removal (Plan 02), CSRF re-enable + dead-class delete (Plan 01), `.env.example` created (this plan), all existing tests pass (this plan).
</success_criteria>

<output>
After completion, create `.planning/phases/05-security-hardening/05-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. The SUMMARY MUST include a "## Manual Smoke Tests" section listing the four items from VALIDATION.md so the user knows what remains to verify in the browser.
</output>
