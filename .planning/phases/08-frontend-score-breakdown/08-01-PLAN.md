---
phase: 08-frontend-score-breakdown
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx
  - frontend/app/profile/components/DailyGem/DailyGem.tsx
  - frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
autonomous: true
requirements: [EXPLAIN-03, METRIC-03]
tags: [frontend, react, nextjs, tailwind, score-breakdown, metrics-strip]

must_haves:
  truths:
    - "ScoreBreakdown.tsx exists as a default-exported client component that accepts a breakdown: Record<string, number> prop and renders exactly 3 rows in the fixed order Genre Match / Novelty / Feedback when the breakdown is non-empty (labels locked per D-06)"
    - "ScoreBreakdown returns null (renders nothing) when Object.keys(breakdown).length === 0 — no placeholder, no greyed bars, no message (per D-02)"
    - "Each ScoreBreakdown row computes percentage via Math.round(rawValue * 100 / 5) * 5 — values rounded to nearest 5% (per D-05)"
    - "Each ScoreBreakdown row uses bg-green for the fill and bg-gray-800 for the track — single accent color across all bars (per D-03)"
    - "Each ScoreBreakdown row uses the locked `[Label] [bar] [XX%]` layout: label-span left, full-width bar in the middle (flex-1), percentage-span right — implemented via `<div className=\"flex items-center gap-2\">` with a fixed-width label, flex-1 track, and right-aligned percentage (per D-04)"
    - "ScoreBreakdown row labels are exactly the strings \"Genre Match\", \"Novelty\", \"Feedback\" (short single-line labels, hardcoded in SCORE_ROWS, mapped from API keys genre_sim / novelty / feedback_multiplier respectively, per D-06)"
    - "DailyGemResponse interface in DailyGem.tsx contains a score_breakdown: Record<string, number> field (per D-10)"
    - "DailyGem.tsx imports ScoreBreakdown from ./ScoreBreakdown and renders <ScoreBreakdown breakdown={score_breakdown ?? {}} /> directly between the explanation blockquote and the audio preview section (per D-01)"
    - "Metrics interface in MetricsStrip.tsx contains a compound_hit_rate: number | null field; gem_acceptance_rate remains in the interface for type safety (per D-11)"
    - "MetricsStrip.tsx renders exactly one <Stat label=\"Hit rate\" value={hitRate} /> tile in place of the previous Acceptance rate tile; no Acceptance rate label is rendered anywhere (per D-07, D-08)"
    - "MetricsStrip hitRate value is formatted as `${Math.round(metrics.compound_hit_rate * 100)}%` — integer percentage, no decimal places, `%` suffix — matching the existing acceptance rate rendering pattern (per D-09)"
    - "hitRate derivation uses `metrics.compound_hit_rate != null` (double-not-equal) so that both null and undefined map to the '—' fallback — defensive coding for pre-Phase-7 cached responses (per RESEARCH.md Pitfall 2)"
    - "Running `cd frontend && npx tsc --noEmit` produces no NEW TypeScript errors beyond the pre-existing TopArtists.tsx:85 baseline error (per RESEARCH.md Pitfall 3)"
  artifacts:
    - path: frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx
      provides: "ScoreBreakdown client component — 3-row labeled progress bar display, fixed-order key iteration, nearest-5% rounding, empty-state null return"
      contains: "export default function ScoreBreakdown"
    - path: frontend/app/profile/components/DailyGem/DailyGem.tsx
      provides: "Updated DailyGemResponse interface with score_breakdown field + ScoreBreakdown import + JSX render between explanation and audio preview"
      contains: "import ScoreBreakdown from \"./ScoreBreakdown\""
    - path: frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
      provides: "Updated Metrics interface with compound_hit_rate field + hitRate derivation + Hit rate Stat tile replacing Acceptance rate"
      contains: "<Stat label=\"Hit rate\""
  key_links:
    - from: "frontend/app/profile/components/DailyGem/DailyGem.tsx"
      to: "frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx"
      via: "import ScoreBreakdown from \"./ScoreBreakdown\""
      pattern: "import ScoreBreakdown from \"\\./ScoreBreakdown\""
    - from: "frontend/app/profile/components/DailyGem/DailyGem.tsx::JSX (between blockquote and audio preview)"
      to: "ScoreBreakdown component"
      via: "<ScoreBreakdown breakdown={score_breakdown ?? {}} />"
      pattern: "<ScoreBreakdown breakdown=\\{score_breakdown"
    - from: "frontend/app/profile/components/DailyGem/DailyGem.tsx::DailyGemResponse interface"
      to: "backend /api/daily-gem/ response shape (Phase 7)"
      via: "score_breakdown: Record<string, number>"
      pattern: "score_breakdown:\\s*Record<string,\\s*number>"
    - from: "frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx::Metrics interface"
      to: "backend /api/recommendation-metrics/ response shape (Phase 7)"
      via: "compound_hit_rate: number | null"
      pattern: "compound_hit_rate:\\s*number\\s*\\|\\s*null"
    - from: "frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx::Stat JSX block"
      to: "hitRate derivation"
      via: "<Stat label=\"Hit rate\" value={hitRate} />"
      pattern: "<Stat label=\"Hit rate\" value=\\{hitRate\\}"
---

<objective>
Render the v1.1 explainability surfaces in the UI: a 3-bar score breakdown on each gem card
and a "Hit rate" tile in the metrics strip. All backend data fields (`score_breakdown`,
`compound_hit_rate`) are already returned by Phase 7 — this plan is pure frontend rendering.

Purpose: Closes EXPLAIN-03 and METRIC-03 for milestone v1.1. The user finally sees the
"why" behind each daily gem (genre match %, novelty %, feedback influence %) and a single
compound success metric (liked OR saved) instead of the like-only acceptance rate.

Output: One new sub-component file (`ScoreBreakdown.tsx`), two modified components
(`DailyGem.tsx` interface + import + JSX insertion; `MetricsStrip.tsx` interface + stat
swap), and a passing `npx tsc --noEmit` type check with no new errors above the
pre-existing TopArtists.tsx baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-frontend-score-breakdown/08-CONTEXT.md
@.planning/phases/08-frontend-score-breakdown/08-RESEARCH.md
@.planning/phases/08-frontend-score-breakdown/08-PATTERNS.md
@.planning/phases/08-frontend-score-breakdown/08-UI-SPEC.md
@.planning/phases/07-backend-wiring/07-01-SUMMARY.md
@.planning/phases/07-backend-wiring/07-02-SUMMARY.md
@frontend/app/profile/components/DailyGem/DailyGem.tsx
@frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
@frontend/app/profile/components/Feedback/FeedbackButtonGroup.tsx
@frontend/tailwind.config.ts

<interfaces>
<!-- Key contracts and identifiers the executor needs. Extracted from codebase + Phase 7 output. -->

From `backend/apps/core/views.py::get_daily_gem` (Phase 7 — already shipped):
- `/api/daily-gem/` JSON response includes `score_breakdown: dict` with keys
  `genre_sim` (float 0–1), `novelty` (float 0–1), `feedback_multiplier` (float 0–1),
  `source` (string — informational, NOT rendered as a bar)
- Field is always present (non-empty `{}` for new gems written by Phase 7; legacy/empty
  `{}` for pre-Phase-7 rows — empty-state path handles this)

From `backend/apps/core/views.py::get_recommendation_metrics` (Phase 7 — already shipped):
- `/api/recommendation-metrics/` JSON response includes `compound_hit_rate: float | null`
  always present per Phase 7 D-12 (0.0 when no gems exist)

From `frontend/app/profile/components/DailyGem/DailyGem.tsx` (current state, verified line numbers):
- Line 1: `"use client";` directive (already present)
- Line 6: `import FeedbackButtonGroup from "../Feedback/FeedbackButtonGroup";` — analog import site for new ScoreBreakdown import
- Lines 21-26: `interface DailyGemResponse { track; explanation; date; cached; }` — add `score_breakdown: Record<string, number>` here
- Line 75: `if (error || !gem) { return ... }` — null guard for `gem`
- Line 91: `const { track, explanation, date } = gem;` — destructure site; add `score_breakdown` here AFTER the line-75 null guard
- Lines 150-154: explanation blockquote conditional render — insertion point for `<ScoreBreakdown />` is the next line AFTER line 154 (the closing `)}`), BEFORE the audio preview block at line 156
- Lines 156-162: `{track.preview_url && ( <div>...AudioPlayer... </div> )}` — audio preview section (do not modify)

From `frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` (current state, verified line numbers):
- Lines 6-17: `interface Metrics { ... gem_acceptance_rate: number | null; top_genres; message? }` — add `compound_hit_rate: number | null;` (keep `gem_acceptance_rate` per D-11)
- Lines 19-26: `function Stat({ label, value }: { label: string; value: string })` — DO NOT MODIFY this component; it is the drop-in target for the new Hit rate tile
- Lines 56-59: `const acceptance = metrics.gem_acceptance_rate !== null ? ...` — derivation site for `acceptance`; replace with `hitRate` (use `!= null` per Pitfall 2)
- Line 67: `<Stat label="Acceptance rate" value={acceptance} />` — exact swap target; becomes `<Stat label="Hit rate" value={hitRate} />`
- Lines 65, 66, 68, 69: other Stat tiles (Gems shown, Gems liked, Avg popularity, Hidden gem rate) — DO NOT MODIFY

From `frontend/app/profile/components/Feedback/FeedbackButtonGroup.tsx`:
- Sub-component file pattern: `"use client";` at top, named props interface above default-exported function, no index.ts barrel — direct relative imports

From `frontend/tailwind.config.ts`:
- `green: "#1DB954"` is the single accent token (used as `bg-green`, `text-green`, `border-green`)
- No other custom tokens are required for this phase

Fixed key-to-label mapping (locked by D-06 + UI-SPEC.md render contract):
- `genre_sim` -> "Genre Match"
- `novelty` -> "Novelty"
- `feedback_multiplier` -> "Feedback"

Pre-existing TypeScript baseline error (per RESEARCH.md Pitfall 3):
- `frontend/app/profile/components/TopArtists/TopArtists.tsx` line 85 has a TS2345 error
  unrelated to this phase. Executor MUST NOT attempt to fix this error and MUST baseline
  it via `npx tsc --noEmit` before starting Task 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScoreBreakdown.tsx sub-component with fixed-order SCORE_ROWS and empty-state null return</name>
  <files>
    frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx
  </files>
  <read_first>
    .planning/phases/08-frontend-score-breakdown/08-CONTEXT.md
    .planning/phases/08-frontend-score-breakdown/08-UI-SPEC.md
    .planning/phases/08-frontend-score-breakdown/08-PATTERNS.md
    frontend/app/profile/components/Feedback/FeedbackButtonGroup.tsx
    frontend/app/profile/components/DailyGem/DailyGem.tsx
    frontend/tailwind.config.ts
  </read_first>
  <action>
    Create a new file at `frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx`.
    It is a client component (mirrors `FeedbackButtonGroup.tsx` file structure: `"use client";`
    directive at top, props interface declared directly above the default-exported function,
    no extra imports needed — pure render, no hooks, no API calls).

    Component signature (locked by D-10, UI-SPEC.md): `export default function ScoreBreakdown({ breakdown }: ScoreBreakdownProps)`.
    Props interface: `interface ScoreBreakdownProps { breakdown: Record<string, number>; }`.

    Define a module-level constant `SCORE_ROWS` BEFORE the component function, typed as
    `{ key: string; label: string }[]`, containing EXACTLY these three entries in this
    fixed order (per D-06 locked labels + UI-SPEC.md render contract + RESEARCH.md Pitfall 5 —
    never iterate `Object.keys(breakdown)` for display order). Labels are the exact strings
    from D-06 ("Genre Match", "Novelty", "Feedback") — short single-line strings, no
    paraphrasing, no abbreviations:
    - `{ key: "genre_sim",           label: "Genre Match" }`
    - `{ key: "novelty",             label: "Novelty"     }`
    - `{ key: "feedback_multiplier", label: "Feedback"    }`

    Empty-state guard (D-02): the FIRST statement of the function body must be
    `if (Object.keys(breakdown).length === 0) return null;`. No placeholder, no message,
    no greyed bars. Returning null prevents the component from emitting any DOM nodes.

    Container element: `<div className="flex flex-col gap-2">` wrapping the row map.

    Per-row structure (locked by D-04 `[Label] [bar] [XX%]` layout + UI-SPEC.md Section:
    Component Inventory, ScoreBreakdown render contract). D-04 requires label on the left,
    full-width bar in the middle, percentage on the right — implemented by a flex row with
    a fixed-width label-span, a flex-1 bar track, and a fixed-width right-aligned
    percentage-span:
    - Outer row (D-04 flex row): `<div key={key} className="flex items-center gap-2">`
    - Label span (D-04 left slot, D-06 label string): `<span className="text-sm text-gray-300 w-28 flex-shrink-0">{label}</span>`
    - Bar track (D-04 middle full-width slot): `<div className="flex-1 bg-gray-800 rounded-full h-2 overflow-hidden">`
    - Bar fill (child of track): `<div className="bg-green rounded-full h-full" style={{ width: `${pct}%` }} />`
    - Percentage span (D-04 right slot): `<span className="text-sm font-bold text-gray-300 w-9 text-right flex-shrink-0">{pct}%</span>`

    Rounding (D-05): for each row, compute `const raw = breakdown[key] ?? 0;` then
    `const pct = Math.round(raw * 100 / 5) * 5;`. The `?? 0` guards against the backend
    omitting a key (e.g., legacy rows or future schema drift).

    Iterate `SCORE_ROWS.map(({ key, label }) => { const raw = ...; const pct = ...; return (<div key={key} ...>...</div>); })`.
    Use `key` (the string from SCORE_ROWS) as the React `key` attribute on the outer row div.

    No imports beyond the `"use client";` directive. No usage of `Object.keys(breakdown)`
    for iteration (only for the length check). No per-component color differentiation
    (D-03) — every bar uses `bg-green` fill on `bg-gray-800` track.
  </action>
  <verify>
    <automated>cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v "^TopArtists.tsx" | grep -v "^app/profile/components/TopArtists" | grep -E "(ScoreBreakdown|error TS)" | head -20 ; echo "---end---"</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` exists
    - `head -1 frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals `"use client";`
    - `grep -c "export default function ScoreBreakdown" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1
    - `grep -c "interface ScoreBreakdownProps" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1
    - `grep -c "breakdown: Record<string, number>" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1
    - `grep -c "SCORE_ROWS" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 2 (one declaration + one usage in `.map`)
    - `grep -c '"genre_sim"' frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1
    - `grep -c '"novelty"' frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1
    - `grep -c '"feedback_multiplier"' frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1
    - `grep -c '"Genre Match"' frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1 (D-06 label)
    - `grep -c '"Novelty"' frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1 (D-06 label)
    - `grep -c '"Feedback"' frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1 (D-06 label)
    - `grep -c "Object.keys(breakdown).length === 0" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` equals 1
    - `grep -c "return null" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1
    - `grep -c "Math.round(raw \* 100 / 5) \* 5\|Math.round(raw\*100/5)\*5" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1
    - `grep -c "bg-green" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1
    - `grep -c "bg-gray-800" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1
    - `grep -c "flex items-center gap-2" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1 (D-04 row layout: flex row with label/bar/percentage)
    - `grep -c "flex-1" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1 (D-04 full-width bar between label and percentage)
    - `grep -c "text-right" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1 (D-04 right-aligned percentage span)
    - `grep -c 'style={{ width: `${pct}%` }}\|style={{width:`${pct}%`}}' frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1 (inline width style for fill div)
    - `grep -c "overflow-hidden" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` is at least 1 (on track div, per Pitfall 4)
    - `cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend && npx tsc --noEmit 2>&1 | grep "ScoreBreakdown.tsx" | grep -c "error TS"` returns 0 (no NEW type errors in the new file)
  </acceptance_criteria>
  <done>
    `ScoreBreakdown.tsx` exists as a default-exported client component with a fixed-order
    SCORE_ROWS constant covering genre_sim/novelty/feedback_multiplier mapped to the D-06
    labels "Genre Match"/"Novelty"/"Feedback", returns null when the breakdown is empty,
    uses `bg-green` fill on `bg-gray-800` track for every row, renders each row in the
    D-04 `[Label] [bar] [XX%]` layout (label left, full-width bar middle, percentage right),
    rounds percentages to the nearest 5%, and produces zero new TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ScoreBreakdown into DailyGem.tsx — add interface field, import, destructure, and JSX render between explanation and audio preview</name>
  <files>
    frontend/app/profile/components/DailyGem/DailyGem.tsx
  </files>
  <read_first>
    frontend/app/profile/components/DailyGem/DailyGem.tsx
    frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx
    .planning/phases/08-frontend-score-breakdown/08-CONTEXT.md
    .planning/phases/08-frontend-score-breakdown/08-PATTERNS.md
    .planning/phases/08-frontend-score-breakdown/08-UI-SPEC.md
  </read_first>
  <action>
    Make four ADDITIVE edits to `frontend/app/profile/components/DailyGem/DailyGem.tsx`.
    The file's null-guard logic at line 75 (`if (error || !gem)`) and destructure at line 91
    are pre-existing — do not touch their structure, only extend them.

    Edit 1 — Import (after line 6, the existing `FeedbackButtonGroup` import):
    Add a new line: `import ScoreBreakdown from "./ScoreBreakdown";`
    Place this directly under the FeedbackButtonGroup import so DailyGem-scoped components
    are grouped (the existing imports order is FeedbackButtonGroup, AIFeedbackInput,
    AddToLiked, AudioPlayer — insert ScoreBreakdown between FeedbackButtonGroup at line 6
    and AIFeedbackInput at line 7).

    Edit 2 — Interface field (lines 21-26, DailyGemResponse interface):
    Add the line `score_breakdown: Record<string, number>;` immediately AFTER the existing
    `explanation: string;` line (line 23), BEFORE the `date: string;` line (line 24).
    Per D-10, the field is REQUIRED (not optional) — the API guarantees its presence per
    Phase 7 contract (default `{}` for legacy rows). Do not use `score_breakdown?:` and
    do not add a trailing comment that changes the line count expectation for downstream
    pattern matching.

    Edit 3 — Destructure (line 91):
    Change `const { track, explanation, date } = gem;` to
    `const { track, explanation, date, score_breakdown } = gem;`. Per RESEARCH.md Pitfall 1,
    this MUST stay AFTER the line-75 null guard — do not move the destructure earlier in
    the function body.

    Edit 4 — JSX insertion (between line 154 closing `)}` of the explanation blockquote and
    line 156 opening `{track.preview_url && (` of the audio preview):
    Insert a new JSX block AFTER the explanation closing `)}` on line 154 and BEFORE the
    `{/* Audio preview */}` comment on line 155 (currently the audio comment is on line 155;
    after this edit the ScoreBreakdown will sit between them). The inserted block is:
    `<ScoreBreakdown breakdown={score_breakdown ?? {}} />`
    Wrap it with a single short comment line above for parity with the surrounding
    `{/* AI explanation */}` / `{/* Audio preview */}` style: `{/* Score breakdown */}`.

    The `?? {}` fallback on the prop is defensive — even though the interface declares
    `score_breakdown` non-optional, this guards against runtime drift if the API ever
    returns `undefined` for this field. Per RESEARCH.md Anti-Pattern: do NOT put the
    `Object.keys(...)` guard at the call site; the guard lives inside ScoreBreakdown
    (which returns null for empty input).

    Do NOT modify any other line in DailyGem.tsx. Do NOT remove `gem_acceptance_rate`
    or any other Spotify display surface. Do NOT add a section heading above the bars
    (UI-SPEC.md Copywriting Contract: "Score bar section heading: none — bars appear
    inline with no section label above them").
  </action>
  <verify>
    <automated>cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v "TopArtists" | grep -E "(DailyGem|ScoreBreakdown|error TS)" | head -20 ; echo "---end---"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'import ScoreBreakdown from "./ScoreBreakdown"' frontend/app/profile/components/DailyGem/DailyGem.tsx` equals 1
    - `grep -c "score_breakdown: Record<string, number>" frontend/app/profile/components/DailyGem/DailyGem.tsx` equals 1
    - `grep -c "const { track, explanation, date, score_breakdown } = gem" frontend/app/profile/components/DailyGem/DailyGem.tsx` equals 1
    - `grep -c "<ScoreBreakdown breakdown={score_breakdown ?? {}} />" frontend/app/profile/components/DailyGem/DailyGem.tsx` equals 1
    - `awk '/<ScoreBreakdown breakdown=/{sb=NR} /track.preview_url/{ap=NR} END{ print (sb>0 && ap>0 && sb<ap) ? "OK" : "FAIL"}' frontend/app/profile/components/DailyGem/DailyGem.tsx` prints `OK` (ScoreBreakdown JSX appears BEFORE the audio preview block — confirms D-01 placement)
    - `awk '/border-l-2 border-green pl-4 py-1/{bq=NR} /<ScoreBreakdown breakdown=/{sb=NR} END{ print (bq>0 && sb>0 && bq<sb) ? "OK" : "FAIL"}' frontend/app/profile/components/DailyGem/DailyGem.tsx` prints `OK` (ScoreBreakdown JSX appears AFTER the explanation blockquote — confirms D-01 placement)
    - `grep -c "const { track, explanation, date } = gem" frontend/app/profile/components/DailyGem/DailyGem.tsx` equals 0 (old destructure line is gone)
    - `cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend && npx tsc --noEmit 2>&1 | grep "DailyGem.tsx" | grep -c "error TS"` returns 0 (no NEW type errors in DailyGem.tsx)
    - Behavioral assertion (manual smoke at phase gate): rendering DailyGem with a gem whose `score_breakdown` is `{genre_sim:0.72, novelty:0.61, feedback_multiplier:0.19, source:'playlist mining'}` produces 3 visible bar rows in the order Genre Match (75%) / Novelty (60%) / Feedback (20%); rendering with `score_breakdown: {}` produces NO bar section between the blockquote and the audio preview
  </acceptance_criteria>
  <done>
    `DailyGemResponse` interface declares `score_breakdown: Record<string, number>`,
    the destructure pulls `score_breakdown` from `gem` after the null guard, and the
    `<ScoreBreakdown breakdown={score_breakdown ?? {}} />` JSX sits between the explanation
    blockquote and the audio preview block. No new TypeScript errors are introduced.
  </done>
</task>

<task type="auto">
  <name>Task 3: Replace Acceptance rate stat with Hit rate in MetricsStrip.tsx — interface field, hitRate derivation, JSX swap</name>
  <files>
    frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
  </files>
  <read_first>
    frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
    .planning/phases/08-frontend-score-breakdown/08-CONTEXT.md
    .planning/phases/08-frontend-score-breakdown/08-PATTERNS.md
    .planning/phases/08-frontend-score-breakdown/08-UI-SPEC.md
    .planning/phases/08-frontend-score-breakdown/08-RESEARCH.md
  </read_first>
  <action>
    Make three additive/replacement edits to `frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx`.

    Edit 1 — Interface field (lines 6-17, Metrics interface):
    Add the line `compound_hit_rate: number | null;` immediately AFTER the existing
    `gem_acceptance_rate: number | null;` line (line 14), BEFORE the `top_genres: string[];`
    line (line 15). Per D-11, KEEP `gem_acceptance_rate` in the interface — it is still
    returned by the backend and removing it would force unsafe casts elsewhere. Only its
    display is removed, not its type.

    Edit 2 — `hitRate` derivation (lines 56-59 area):
    REPLACE the existing `acceptance` derivation block:

      `const acceptance = metrics.gem_acceptance_rate !== null ? \`${Math.round(metrics.gem_acceptance_rate * 100)}%\` : "—";`

    with a `hitRate` derivation that (a) uses double-not-equal (`!= null`) per RESEARCH.md
    Pitfall 2 — guarding against `compound_hit_rate` being `undefined` on pre-Phase-7
    cached responses — and (b) formats the value EXACTLY as `${Math.round(compound_hit_rate * 100)}%`
    per D-09, mirroring the existing acceptance-rate rendering pattern (integer percent,
    no decimal places, `%` suffix):

      `const hitRate = metrics.compound_hit_rate != null ? \`${Math.round(metrics.compound_hit_rate * 100)}%\` : "—";`

    Note the asymmetry: `acceptance` used `!== null` (strict), `hitRate` MUST use `!= null`
    (loose) to also catch `undefined`. This is intentional and locked by Pitfall 2.

    The `acceptance` variable is fully removed in this edit — it is no longer referenced
    anywhere after the JSX swap in Edit 3.

    Edit 3 — JSX swap (line 67):
    REPLACE `<Stat label="Acceptance rate" value={acceptance} />` with
    `<Stat label="Hit rate" value={hitRate} />`. Position unchanged — the new tile occupies
    the same slot in the strip (3rd Stat, between "Gems liked" and "Avg popularity") per
    UI-SPEC.md Interaction Contract.

    Do NOT modify the `<Stat>` component definition (lines 19-26). Do NOT modify any other
    Stat tile (Gems shown, Gems liked, Avg popularity, Hidden gem rate, Top genres). Do
    NOT modify the refresh button or the fetchMetrics loading/error states.

    Per D-08, the label is exactly the two-word string `"Hit rate"` (sentence case, no
    capitalization on "rate"). Per D-09, the value format is `${Math.round(x * 100)}%` —
    integer percentage, no decimal places, suffixed with `%`, matching the existing
    acceptance-rate rendering pattern (only the field reference changes).
  </action>
  <verify>
    <automated>cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | grep -v "TopArtists" | grep -E "(MetricsStrip|error TS)" | head -20 ; echo "---end---"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "compound_hit_rate: number | null" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 1
    - `grep -c "gem_acceptance_rate: number | null" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 1 (KEPT for type safety per D-11)
    - `grep -c "const hitRate" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 1
    - `grep -c "metrics.compound_hit_rate != null" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 1 (double-not-equal — Pitfall 2)
    - `grep -c 'Math.round(metrics.compound_hit_rate \* 100)' frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 1 (D-09 value format: integer percent, no decimals, `%` suffix)
    - `grep -c '<Stat label="Hit rate" value={hitRate} />' frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 1
    - `grep -c '<Stat label="Acceptance rate"' frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 0 (old stat is gone from JSX)
    - `grep -c "const acceptance" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 0 (unused derivation removed)
    - `grep -c "metrics.gem_acceptance_rate" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` equals 0 (no runtime reads of the now-unused field; interface presence is the only reference)
    - `cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend && npx tsc --noEmit 2>&1 | grep "MetricsStrip.tsx" | grep -c "error TS"` returns 0 (no NEW type errors in MetricsStrip.tsx)
    - Behavioral assertion (manual smoke at phase gate, D-09 value format): with `compound_hit_rate: 0.58`, the strip's 3rd stat displays `58%` under the label `Hit rate`; with `compound_hit_rate: null`, the 3rd stat displays `—` under the same label; with the field missing from the response, the 3rd stat still displays `—` (Pitfall 2 defensive path)
    - Phase-gate full type check: `cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 1 (only the pre-existing TopArtists.tsx:85 error per RESEARCH.md baseline — zero new errors introduced by this plan)
  </acceptance_criteria>
  <done>
    `Metrics` interface declares `compound_hit_rate: number | null` while retaining
    `gem_acceptance_rate` for type safety; `hitRate` is derived with the defensive
    `!= null` check and formatted as `${Math.round(metrics.compound_hit_rate * 100)}%`
    per D-09; the JSX renders exactly one `<Stat label="Hit rate" value={hitRate} />`
    tile where Acceptance rate used to live; the full frontend type check has no new
    errors above the pre-existing TopArtists.tsx baseline.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM <- React render | trusted boundary — both `score_breakdown` values and `compound_hit_rate` are numeric server-computed floats; React's automatic escaping covers any string content |
| `/api/daily-gem/` -> `DailyGem.tsx` state | DRF-authenticated session boundary, unchanged from Phase 7; cookie-based, no new auth surface |
| `/api/recommendation-metrics/` -> `MetricsStrip.tsx` state | DRF-authenticated session boundary, unchanged from Phase 7 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | I (Information disclosure) | `score_breakdown` field rendered to authenticated user | accept | Per Phase 7 D-12, the `/api/daily-gem/` endpoint scopes the gem to `user=request.user` — a user only ever sees their own gem's score_breakdown. No cross-user data exposure surface introduced in Phase 8. |
| T-08-02 | T (Tampering) | Score percentage values rendered in DOM | accept | Values are numeric, computed via `Math.round(raw * 100 / 5) * 5` and interpolated into `style={{ width: \`${pct}%\` }}` — no string content from the API is concatenated into HTML. React's automatic JSX escaping covers the label strings (which are hardcoded constants in SCORE_ROWS, not API-derived). |
| T-08-03 | D (Denial of service) | `ScoreBreakdown` empty-state path | mitigate | Empty-state guard at top of component returns `null` immediately when `Object.keys(breakdown).length === 0`. No infinite loops, no recursive renders, no fetch calls. Pure render path with bounded iteration (exactly 3 rows). Acceptance criterion `grep -c "Object.keys(breakdown).length === 0"` equals 1 verifies this. |
| T-08-04 | D (Denial of service) | `MetricsStrip` hit-rate undefined path | mitigate | `metrics.compound_hit_rate != null` (loose-not-equal) catches both `null` and `undefined` per RESEARCH.md Pitfall 2 — prevents `Math.round(undefined * 100) === NaN` rendering. Acceptance criterion verifies `!= null` usage. |
| T-08-05 | S (Spoofing) | N/A | accept | No new auth surfaces, no new user-input handling, no form submissions. Phase 8 is read-only display. |
</threat_model>

<verification>
After all 3 tasks complete:

1. `cd /Users/antonilueddeke/Desktop/Projects/songscope/frontend && npx tsc --noEmit 2>&1 | grep -c "error TS"` returns 1 (only the pre-existing TopArtists.tsx:85 baseline error — zero new errors)
2. `grep -c "export default function ScoreBreakdown" frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx` returns 1
3. `grep -c 'import ScoreBreakdown from "./ScoreBreakdown"' frontend/app/profile/components/DailyGem/DailyGem.tsx` returns 1
4. `grep -c '<ScoreBreakdown breakdown={score_breakdown ?? {}} />' frontend/app/profile/components/DailyGem/DailyGem.tsx` returns 1
5. `grep -c "compound_hit_rate: number | null" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` returns 1
6. `grep -c '<Stat label="Hit rate" value={hitRate} />' frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` returns 1
7. `grep -c '<Stat label="Acceptance rate"' frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` returns 0

Manual smoke at phase gate (post-merge, not blocking on individual tasks):
- `cd frontend && npm run dev`; log in; visit `/profile`
- Daily gem card: confirm three bars labeled Genre Match, Novelty, Feedback render between the italic explanation blockquote and the audio preview; values are integer percentages ending in 0 or 5
- Daily gem card with a legacy row (manually clear `score_breakdown` via Django shell on the current day's DailyGem): confirm no bar section renders; explanation blockquote and audio preview are adjacent (D-02 empty state)
- Metrics strip: confirm the 3rd stat tile reads "Hit rate" with an integer percentage value (or "—" if no gems exist)
- Verify no Acceptance rate label appears anywhere on the profile page
</verification>

<success_criteria>
- All must_haves truths verifiable via `grep`/`awk`/`tsc` checks listed in each task's acceptance_criteria
- `ScoreBreakdown.tsx` exists as a default-exported client component with the exact SCORE_ROWS constant, empty-state null guard, fixed-order rendering, locked color scheme, locked `[Label] [bar] [XX%]` row layout, and locked label strings per D-02/D-03/D-04/D-05/D-06
- `DailyGem.tsx` exposes `score_breakdown` via the response interface (D-10), destructures it after the null guard (Pitfall 1), and renders the bars between the explanation blockquote and the audio preview (D-01)
- `MetricsStrip.tsx` exposes `compound_hit_rate` via the Metrics interface while keeping `gem_acceptance_rate` (D-11), derives `hitRate` with the defensive `!= null` check (Pitfall 2), formats the value as `${Math.round(compound_hit_rate * 100)}%` (D-09), and swaps the 3rd Stat tile from Acceptance rate to Hit rate (D-07/D-08)
- Full frontend `npx tsc --noEmit` shows exactly 1 error: the pre-existing `TopArtists.tsx:85` baseline (Pitfall 3) — zero new TypeScript errors
- EXPLAIN-03 and METRIC-03 each have an artifact and a grep-verifiable acceptance assertion
</success_criteria>

<output>
After completion, create `.planning/phases/08-frontend-score-breakdown/08-01-SUMMARY.md`:
- Dependency graph frontmatter (requires Phase 7 plans 01/02 — backend writes score_breakdown and compound_hit_rate; provides frontend score bars and hit-rate tile)
- Files created: `ScoreBreakdown.tsx`
- Files modified: `DailyGem.tsx`, `MetricsStrip.tsx`
- Decisions honored: D-01 through D-11 (all 11 CONTEXT.md decisions)
- Patterns established: sub-component extraction at the ~20-line threshold (mirrors FeedbackButtonGroup), fixed-order display constant for dict-shaped API data (SCORE_ROWS), defensive `!= null` for new optional fields on pre-existing endpoint shapes
- Requirements completed: EXPLAIN-03, METRIC-03
- Milestone v1.1 readiness: Phase 8 closes both UI requirements; only Phase 9 (documentation sync) remains
</output>
