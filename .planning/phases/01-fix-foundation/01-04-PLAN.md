---
phase: 01-fix-foundation
plan: "04"
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - backend/tests/test_feedback.py
autonomous: true
requirements:
  - dailygem-sync-verified
gap_closure: true

must_haves:
  truths:
    - "Calling POST /api/submit-feedback/ with feedback_type=LIKE sets DailyGem.was_liked=True on today's gem"
    - "Calling POST /api/submit-feedback/ with feedback_type=DISLIKE sets DailyGem.was_liked=False on today's gem"
    - "Calling POST /api/submit-feedback/ a second time with LIKE on an already-liked track sets DailyGem.was_liked=None (unlike path)"
    - "The view-level tests reach submit_feedback in views.py — NOT just ORM .save() round-trips"
  artifacts:
    - path: "backend/tests/test_feedback.py"
      provides: "View-level integration tests inside TestDailyGemWasLikedSync"
      contains: "self.client.post"
  key_links:
    - from: "backend/tests/test_feedback.py:TestDailyGemWasLikedSync"
      to: "backend/apps/core/views.py:submit_feedback"
      via: "self.client.post('/api/submit-feedback/', ...)"
      pattern: "client\\.post.*submit-feedback"
---

<objective>
Add three view-level integration tests to TestDailyGemWasLikedSync in
backend/tests/test_feedback.py. Each test POSTs to /api/submit-feedback/ using
the Django test client and verifies that DailyGem.was_liked is written by the
submit_feedback view — not just by direct ORM .save().

Purpose: Close the gap from VERIFICATION.md — "TestDailyGemWasLikedSync only
tests ORM round-trips, not the view code path." The ROADMAP requirement
dailygem-sync-verified cannot be marked satisfied until the view itself is
exercised by tests.

Output: backend/tests/test_feedback.py gains three new test methods inside
TestDailyGemWasLikedSync. Existing four ORM tests are left untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fix-foundation/01-VERIFICATION.md

<interfaces>
<!-- Confirmed from codebase. Executor uses these directly — no exploration needed. -->

From backend/apps/core/views.py (submit_feedback):
  - Decorated: @api_view(['POST']), @permission_classes([IsAuthenticated])
  - URL: /api/submit-feedback/  (config/urls.py line 49)
  - Reads SpotifyToken.objects.get(user=request.user) then calls .is_expired()
  - Uses FeedbackSubmissionSerializer with fields: track_id (str), feedback_type (LIKE|DISLIKE|SKIP|SAVE)
  - Calls Track.objects.get_or_create(spotify_id=track_id) — if track already exists, sp.track() is NOT called
  - Local imports inside the function body:
      from apps.recommendations.personalization_engine import PersonalizationEngine  (lines 577, 614)
      (HybridRecommendationEngine is imported at module top, not locally)
  - Unlike path (LIKE on already-liked track): returns {'status': 'success', 'action': 'removed'}
  - LIKE/DISLIKE path: returns {'status': 'success', 'action': 'added'}

From backend/apps/core/models.py (SpotifyToken):
  - access_token = CharField(max_length=255)
  - refresh_token = CharField(max_length=255)
  - expires_at = DateTimeField                  ← DateTimeField, NOT integer epoch
  - is_expired() returns: timezone.now() >= self.expires_at
  - To make is_expired() return False, set expires_at to a far-future datetime:
      from django.utils import timezone
      from datetime import timedelta
      expires_at=timezone.now() + timedelta(days=3650)

From backend/apps/core/models.py (DailyGem):
  - user = ForeignKey(User)
  - date = DateField()
  - track = ForeignKey(Track)
  - was_liked = NullBooleanField (or BooleanField(null=True))

From backend/apps/core/serializers.py (FeedbackSubmissionSerializer):
  - track_id: CharField
  - feedback_type: ChoiceField(['LIKE', 'DISLIKE', 'SKIP', 'SAVE'])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add view-level integration tests to TestDailyGemWasLikedSync</name>

  <read_first>
    - backend/tests/test_feedback.py          (file being modified — read fully first)
    - backend/apps/core/views.py lines 523-654 (submit_feedback — confirm patch targets)
    - backend/apps/core/models.py lines 15-30 (SpotifyToken fields — already confirmed above but re-read to be safe)
  </read_first>

  <files>backend/tests/test_feedback.py</files>

  <action>
Make the following changes to backend/tests/test_feedback.py.
All line references are approximate — read the file first to get exact positions.

--- STEP 1: ADD MISSING IMPORTS AT TOP OF FILE ---

The current top of test_feedback.py is:
  from django.contrib.auth.models import User
  from django.test import TestCase
  from apps.core.models import RecommendationLog, Track

Replace those three import lines with:
  import json
  from datetime import date
  from unittest.mock import patch, MagicMock

  from django.contrib.auth.models import User
  from django.test import TestCase
  from django.utils import timezone
  from datetime import timedelta

  from apps.core.models import RecommendationLog, Track, DailyGem, SpotifyToken

(If any import is already present, do not duplicate it — just add what is missing.)

--- STEP 2: MODIFY TestDailyGemWasLikedSync.setUp ---

Find setUp inside TestDailyGemWasLikedSync (currently lines ~75-87).
The current setUp creates self.user, self.track, self.gem.
After the `self.gem = DailyGem.objects.create(...)` line, add:

    # SpotifyToken required by submit_feedback — expires_at is a DateTimeField
    self.token = SpotifyToken.objects.create(
        user=self.user,
        access_token='fake_access_token',
        refresh_token='fake_refresh_token',
        expires_at=timezone.now() + timedelta(days=3650),
    )
    # Log the test user in via the Django test client
    self.client.force_login(self.user)

Do NOT touch the existing `from datetime import date` local import inside setUp
(lines ~76-77) — leave it in place.

--- STEP 3: ADD THREE NEW TEST METHODS AT END OF TestDailyGemWasLikedSync ---

After the final existing test method (currently `test_today_gem_lookup_matches_views_pattern`
ending around line 121), add the following three methods. They remain inside
TestDailyGemWasLikedSync (same indentation level as the other test methods — 4 spaces):

    @patch('apps.core.views.HybridRecommendationEngine')
    @patch('apps.recommendations.personalization_engine.PersonalizationEngine')
    @patch('apps.core.views.get_spotipy_client')
    def test_view_sets_was_liked_true_on_like(self, mock_sp_client, mock_pe, mock_hre):
        """View path: POST LIKE → submit_feedback sets DailyGem.was_liked = True."""
        mock_sp_client.return_value = MagicMock()
        mock_pe.return_value = MagicMock()
        mock_hre.return_value = MagicMock()

        response = self.client.post(
            '/api/submit-feedback/',
            data=json.dumps({'track_id': self.track.spotify_id, 'feedback_type': 'LIKE'}),
            content_type='application/json',
        )

        self.assertEqual(response.status_code, 200, response.content)
        self.gem.refresh_from_db()
        self.assertTrue(self.gem.was_liked)

    @patch('apps.core.views.HybridRecommendationEngine')
    @patch('apps.recommendations.personalization_engine.PersonalizationEngine')
    @patch('apps.core.views.get_spotipy_client')
    def test_view_sets_was_liked_false_on_dislike(self, mock_sp_client, mock_pe, mock_hre):
        """View path: POST DISLIKE → submit_feedback sets DailyGem.was_liked = False."""
        mock_sp_client.return_value = MagicMock()
        mock_pe.return_value = MagicMock()
        mock_hre.return_value = MagicMock()

        response = self.client.post(
            '/api/submit-feedback/',
            data=json.dumps({'track_id': self.track.spotify_id, 'feedback_type': 'DISLIKE'}),
            content_type='application/json',
        )

        self.assertEqual(response.status_code, 200, response.content)
        self.gem.refresh_from_db()
        self.assertFalse(self.gem.was_liked)
        self.assertIsNotNone(self.gem.was_liked)

    @patch('apps.core.views.HybridRecommendationEngine')
    @patch('apps.recommendations.personalization_engine.PersonalizationEngine')
    @patch('apps.core.views.get_spotipy_client')
    def test_view_clears_was_liked_on_unlike(self, mock_sp_client, mock_pe, mock_hre):
        """View path: POST LIKE twice → second call is unlike → DailyGem.was_liked = None."""
        mock_sp_client.return_value = MagicMock()
        mock_pe.return_value = MagicMock()
        mock_hre.return_value = MagicMock()

        # First LIKE — creates the UserFeedback row; sets was_liked = True
        first = self.client.post(
            '/api/submit-feedback/',
            data=json.dumps({'track_id': self.track.spotify_id, 'feedback_type': 'LIKE'}),
            content_type='application/json',
        )
        self.assertEqual(first.status_code, 200, first.content)

        # Second LIKE — unlike branch: deletes feedback, clears was_liked = None
        response = self.client.post(
            '/api/submit-feedback/',
            data=json.dumps({'track_id': self.track.spotify_id, 'feedback_type': 'LIKE'}),
            content_type='application/json',
        )

        self.assertEqual(response.status_code, 200, response.content)
        self.gem.refresh_from_db()
        self.assertIsNone(self.gem.was_liked)

--- IMPORTANT NOTES ---

Patch targets:
  - 'apps.core.views.get_spotipy_client' — imported at module level from apps.spotify.utils
  - 'apps.recommendations.personalization_engine.PersonalizationEngine' — PersonalizationEngine
    is imported locally inside submit_feedback's function body (not at module level), so it
    MUST be patched at its source module, NOT at 'apps.core.views.PersonalizationEngine'
    (which does not exist in the module namespace and would raise AttributeError).
  - 'apps.core.views.HybridRecommendationEngine' — imported at module level in views.py

Track trick: The Track `trk_gem_sync_1` was created in setUp via Track.objects.create().
submit_feedback calls Track.objects.get_or_create(spotify_id=track_id). Since the track
already exists, `created=False` and `sp.track(track_id)` inside the `if created:` block
is NEVER executed. The Spotify client mock is only needed to prevent the SpotifyOAuth
initialization from making network calls — the actual sp.track() call does not fire.

Do NOT call django.setup() anywhere in test_feedback.py. conftest.py handles it.
  </action>

  <verify>
    <automated>cd /Users/antonilueddeke/Desktop/Projects/songscope/backend && python -m pytest tests/test_feedback.py -v 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "self.client.post" backend/tests/test_feedback.py returns 3 or more (counting only non-comment lines)
    - grep -c "test_view_sets_was_liked_true_on_like\|test_view_sets_was_liked_false_on_dislike\|test_view_clears_was_liked_on_unlike" backend/tests/test_feedback.py returns 3
    - python -m pytest tests/test_feedback.py::TestDailyGemWasLikedSync -v exits 0 with 7 tests collected and 7 passed (4 original ORM + 3 new view-level)
    - python -m pytest tests/test_feedback.py -v exits 0 with no regressions in TestRecommendationLogLikedField
  </acceptance_criteria>

  <done>
    TestDailyGemWasLikedSync contains 7 passing tests: the 4 original ORM
    round-trip tests unchanged, plus 3 new view-level tests that call
    self.client.post('/api/submit-feedback/') and verify DailyGem.was_liked
    is True after LIKE, False after DISLIKE, and None after unlike.
    All 7 tests pass under `python -m pytest tests/test_feedback.py::TestDailyGemWasLikedSync -v`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test client → submit_feedback | Authenticated Django test client; user is force_login'd in setUp |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Spoofing | TestDailyGemWasLikedSync.setUp | accept | Tests use force_login; no credential spoofing possible in isolated test transaction |
| T-01-04-02 | Tampering | SpotifyToken.expires_at | accept | Far-future datetime is test-only; no production secret exposure |
| T-01-04-03 | Information Disclosure | Mock patches | accept | Patches suppress real Spotify/engine calls; no external network traffic |
</threat_model>

<verification>
Run after task completes:

```bash
cd /Users/antonilueddeke/Desktop/Projects/songscope/backend

# 1. All 7 tests in TestDailyGemWasLikedSync pass
python -m pytest tests/test_feedback.py::TestDailyGemWasLikedSync -v

# 2. Full test_feedback.py passes (no regression in TestRecommendationLogLikedField)
python -m pytest tests/test_feedback.py -v

# 3. Confirm 3 view-level client calls exist (excluding comment lines)
grep -v '^#' tests/test_feedback.py | grep -c "self.client.post"
```

Expected: 7 tests collected, 7 passed; grep count >= 3.
</verification>

<success_criteria>
- TestDailyGemWasLikedSync contains 7 passing tests (4 ORM + 3 view-level)
- Each view-level test calls self.client.post('/api/submit-feedback/', ...) via Django test client
- DailyGem.was_liked is True after LIKE (view path), False after DISLIKE (view path), None after unlike (view path)
- python -m pytest tests/test_feedback.py -v exits 0 with no failures
- The dailygem-sync-verified ROADMAP requirement is now tested end-to-end through the actual view
</success_criteria>

<output>
After completion, create `.planning/phases/01-fix-foundation/01-04-SUMMARY.md`
using the summary template. Include:
- Files modified: backend/tests/test_feedback.py
- Tests added: 3 view-level tests in TestDailyGemWasLikedSync
- Requirement closed: dailygem-sync-verified
- Gap closed from VERIFICATION.md: "DailyGem.was_liked is set by submit_feedback view" — now verified via HTTP POST through self.client
</output>
