---
phase: 10-v1-2-ux-feedback-refinement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
  - frontend/app/profile/components/TopArtists/TopArtists.tsx
  - frontend/app/profile/page.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04]
must_haves:
  truths:
    - "MetricsStrip renders with no Refresh stats button and stats load automatically on mount"
    - "Each TopArtists card shows Hidden Gem (green) for popularity < 40, Rising (yellow) for 40-69, Mainstream (gray) for >= 70"
    - "The expanded TopArtists detail panel has a visible dark background (bg-gray-800)"
    - "The taste profile section subtitle reads 'Your genre taste profile'"
  artifacts:
    - path: "frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx"
      provides: "fetchMetrics simplified to async () => void; no refreshing state; no Refresh button"
    - path: "frontend/app/profile/components/TopArtists/TopArtists.tsx"
      provides: "getPopularityLabel returning {label,color}; bg-gray-800 expanded panel"
      contains: "getPopularityLabel"
    - path: "frontend/app/profile/page.tsx"
      provides: "Your genre taste profile subtitle"
      contains: "Your genre taste profile"
  key_links:
    - from: "TopArtists getPopularityLabel"
      to: "TrendingUp icon className AND text label div"
      via: "single call per artist destructured as { label, color }, used in both call sites"
      pattern: "getPopularityLabel"
---

<objective>
Apply the four independent profile-page UI quality fixes (UI-01 through UI-04). These are mechanical removals and string/class swaps with no cross-component state threading — fully isolated from the feedback-loop wiring in Plans 01 and 03.

Purpose: The profile page currently has a stale manual "Refresh stats" button (stats already auto-load), popularity labels with inverted color semantics (green=mainstream is backwards for a hidden-gem app), an invisible expanded panel caused by a non-existent Tailwind class, and an ambiguous section subtitle. These fixes make the page communicate correctly without changing any data flow.

Output: MetricsStrip with the Refresh button and its dead state removed; TopArtists with semantically-correct popularity labels and a visible expanded panel; profile page with a clearer taste-profile subtitle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-v1-2-ux-feedback-refinement/10-RESEARCH.md
@.planning/phases/10-v1-2-ux-feedback-refinement/10-PATTERNS.md
@.planning/phases/10-v1-2-ux-feedback-refinement/10-UI-SPEC.md

<interfaces>
<!-- Verified by direct inspection 2026-06-19. Exact line numbers. -->

MetricsStrip.tsx:
- line 32: `const [refreshing, setRefreshing] = useState(false);`
- line 35: `const fetchMetrics = async (showRefreshing = false) => {`
- line 36: `if (showRefreshing) setRefreshing(true);`
- line 45: `setRefreshing(false);` (in finally)
- line 50: `fetchMetrics();` (mount useEffect — KEEP unchanged)
- lines 89-95: the `<button onClick={() => fetchMetrics(true)} disabled={refreshing} ...>{refreshing ? "Refreshing…" : "Refresh stats"}</button>` block

TopArtists.tsx:
- import `{ TrendingUp } from 'lucide-react'` (line 4)
- lines 38-43: `const getPopularityColor = (popularity: number): string => { if (popularity >= 80) return 'text-green-400'; if (popularity >= 60) return 'text-yellow-400'; if (popularity >= 40) return 'text-orange-400'; return 'text-red-400'; };`
- line 158: `<TrendingUp className={\`w-3 h-3 ${getPopularityColor(artist.popularity)}\`} />`
- lines 174-175: `<div className={\`text-xs mt-1 ${getPopularityColor(artist.popularity)}\`}>{artist.popularity}% popular</div>`
- line 190: `<div className="bg-gray-850 rounded-lg p-6 border border-gray-700 w-full">`
- artist rendering occurs inside an `artists.forEach`/map callback that pushes JSX (the local `pop` const goes at the top of that per-artist render block)

page.tsx (Server Component):
- line 79: `<p ...>Like-rate trend (7-day rolling)</p>` — ALREADY CORRECT, do not touch
- line 83: `<p className="text-xs text-gray-500 uppercase tracking-widest mb-4">Your taste profile</p>` — change string only

tailwind.config.ts: `green: "#1DB954"` custom alias exists (so `text-green` is valid). No `gray-850` or `gray-750` defined. `text-yellow-400`, `text-gray-400`, `bg-gray-800` are stock Tailwind v3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI-01 — remove Refresh stats button and dead refreshing state from MetricsStrip</name>
  <files>frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx</files>
  <read_first>
    - frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx (current refreshing state at line 32, fetchMetrics at 35, the button at 89-95, and the layout div wrapping the button)
    - .planning/phases/10-v1-2-ux-feedback-refinement/10-PATTERNS.md (section "MetricsStrip.tsx (UI-01)" — exact removal targets)
    - .planning/phases/10-v1-2-ux-feedback-refinement/10-RESEARCH.md (Pitfall 3 — leftover signature/state)
  </read_first>
  <action>Make four removals in MetricsStrip.tsx. (1) Delete the `const [refreshing, setRefreshing] = useState(false);` declaration (line 32). (2) Change `fetchMetrics` signature from `async (showRefreshing = false) =>` to `async () =>` and delete the `if (showRefreshing) setRefreshing(true);` line (line 36). (3) Delete the `setRefreshing(false);` call inside the `finally` block (line 45) — keep `setLoading(false)` and any other finally statements. (4) Delete the entire `<button ...>{refreshing ? "Refreshing…" : "Refresh stats"}</button>` block (lines 89-95). Leave the mount `useEffect` that calls `fetchMetrics()` (line 50) unchanged — auto-load on mount stays. If removing the button leaves an empty layout wrapper that previously used `justify-between` only to position the button, collapse it to a plain `flex flex-wrap gap-8` per the existing layout convention; otherwise leave the wrapper as-is. Do not introduce any new import or state.</action>
  <verify>
    <human-check>Open the profile page in the browser. Confirm: (1) no "Refresh stats" button is visible anywhere in the metrics strip; (2) the stats numbers populate automatically on page load without any click. Also confirm `npm run build` in frontend/ succeeds with no TypeScript errors about unused `refreshing`/`setRefreshing`.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Refresh stats" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` returns 0
    - `grep -c "refreshing" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` returns 0 (state, setter, and JSX references all gone)
    - `grep -c "showRefreshing" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` returns 0
    - `grep -c "fetchMetrics()" frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` returns >= 1 (mount call preserved)
    - `cd frontend && npm run build` exits 0 (no unused-variable / type errors)
  </acceptance_criteria>
  <done>MetricsStrip has no Refresh button and no refreshing state; fetchMetrics is `async () => void`; stats still auto-load on mount; build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: UI-02 + UI-03 — semantic popularity labels and visible expanded panel in TopArtists</name>
  <files>frontend/app/profile/components/TopArtists/TopArtists.tsx</files>
  <read_first>
    - frontend/app/profile/components/TopArtists/TopArtists.tsx (getPopularityColor at lines 38-43, the two call sites at lines 158 and 174-175, and the expanded panel div at line 190; identify the per-artist render block where a local const can be added)
    - .planning/phases/10-v1-2-ux-feedback-refinement/10-PATTERNS.md (section "TopArtists.tsx (UI-02, UI-03)" — exact replacement function and both call sites)
    - .planning/phases/10-v1-2-ux-feedback-refinement/10-RESEARCH.md (Pitfall 4 — dual call site; anti-pattern note about bg-gray-750 being out of scope)
    - .planning/phases/10-v1-2-ux-feedback-refinement/10-UI-SPEC.md (Phase 10 Color Changes — exact tier table and color classes)
  </read_first>
  <action>UI-02: Replace the entire `getPopularityColor` function (returns string) with `getPopularityLabel = (popularity: number): { label: string; color: string } =>` returning: popularity < 40 → `{ label: 'Hidden Gem', color: 'text-green' }`; popularity < 70 → `{ label: 'Rising', color: 'text-yellow-400' }`; else → `{ label: 'Mainstream', color: 'text-gray-400' }`. `text-green` is the project's custom alias for #1DB954 — valid, no config change. At the top of the per-artist render block (before the JSX for that artist is pushed/returned), add `const pop = getPopularityLabel(artist.popularity);`. Replace BOTH call sites in one pass (Pitfall 4): at line 158 change the icon to `<TrendingUp className={\`w-3 h-3 ${pop.color}\`} />`; at lines 174-175 change the text div to `<div className={\`text-xs mt-1 ${pop.color}\`}>{pop.label}</div>` — the text content changes from `{artist.popularity}% popular` to `{pop.label}`. UI-03: At line 190 replace `bg-gray-850` with `bg-gray-800` in the expanded artist row container (`<div className="bg-gray-800 rounded-lg p-6 border border-gray-700 w-full">`). Do NOT touch any `bg-gray-750` occurrences (separate pre-existing issue, out of scope). Do not change the TrendingUp import or any other logic.</action>
  <verify>
    <human-check>Open the profile page, expand the TopArtists section. Confirm: (1) a low-popularity artist (<40) shows "Hidden Gem" in green; a mid artist (40-69) shows "Rising" in yellow; a high artist (>=70) shows "Mainstream" in gray; (2) the TrendingUp icon color matches its label's color; (3) no card shows "% popular" text anymore; (4) the expanded detail panel has a visible solid dark background (not transparent). Confirm `npm run build` succeeds.</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "getPopularityColor" frontend/app/profile/components/TopArtists/TopArtists.tsx` returns 0 (fully replaced)
    - `grep -c "getPopularityLabel" frontend/app/profile/components/TopArtists/TopArtists.tsx` returns 4 (definition + 1 local-const call + 2 usages of `pop` ... at minimum the definition + the call; allow that `pop.color`/`pop.label` reference `pop`, so assert definition+call): `grep -c "getPopularityLabel" ...` returns >= 2 and `grep -c "pop.color" ...` returns 2 and `grep -c "pop.label" ...` returns 1
    - `grep -c "Hidden Gem" frontend/app/profile/components/TopArtists/TopArtists.tsx` returns 1
    - `grep -c "% popular" frontend/app/profile/components/TopArtists/TopArtists.tsx` returns 0
    - `grep -c "bg-gray-850" frontend/app/profile/components/TopArtists/TopArtists.tsx` returns 0
    - `grep -c "bg-gray-800" frontend/app/profile/components/TopArtists/TopArtists.tsx` returns >= 1
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>getPopularityColor is gone; getPopularityLabel drives both the icon and the text label per artist with correct tiers/colors; the expanded panel uses bg-gray-800; build is clean.</done>
</task>

<task type="auto">
  <name>Task 3: UI-04 — clarify taste profile section subtitle</name>
  <files>frontend/app/profile/page.tsx</files>
  <read_first>
    - frontend/app/profile/page.tsx (lines 79 and 83 — confirm line 79 "Like-rate trend (7-day rolling)" is already correct and must NOT change; line 83 "Your taste profile" is the only edit)
    - .planning/phases/10-v1-2-ux-feedback-refinement/10-UI-SPEC.md (Copywriting Contract — exact new string)
    - .planning/phases/10-v1-2-ux-feedback-refinement/10-RESEARCH.md (Pitfall: "Misidentifying UI-04 scope")
  </read_first>
  <action>In page.tsx line 83, change the subtitle text content from `Your taste profile` to `Your genre taste profile`. Change only the inner text of that `<p className="text-xs text-gray-500 uppercase tracking-widest mb-4">` element — keep the className and tag identical. Do NOT modify line 79 (`Like-rate trend (7-day rolling)` is already correct). No other change to the file.</action>
  <verify>
    <human-check>Open the profile page. Confirm the taste-profile chart section subtitle reads "Your genre taste profile" and the trend chart subtitle still reads "Like-rate trend (7-day rolling)".</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c "Your genre taste profile" frontend/app/profile/page.tsx` returns 1
    - `grep -c "Your taste profile" frontend/app/profile/page.tsx` returns 0 (old string replaced, not duplicated)
    - `grep -c "Like-rate trend (7-day rolling)" frontend/app/profile/page.tsx` returns 1 (unchanged)
    - `cd frontend && npm run build` exits 0
  </acceptance_criteria>
  <done>The taste profile subtitle reads "Your genre taste profile"; the trend subtitle is untouched; build is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UI-01–04 are presentation-only edits: removing a button, swapping a label function, fixing a CSS class, and changing one static string. No new input crosses any boundary, no new data flow, no auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-UI | Tampering | profile UI render path | accept | No untrusted input is processed; `artist.popularity` is already-fetched numeric data rendered into a static label. No injection, no persistence, no new request. Pre-existing fetch behavior unchanged. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (RESEARCH.md "no new dependencies"; no `npm install`). No install task exists; no checkpoint required. |
</threat_model>

<verification>
- `cd frontend && npm run build` exits 0 after all three tasks (no unused-variable/type errors)
- Source assertions in each task's acceptance_criteria all pass (grep gates)
- Manual visual confirmation per `<human-check>` blocks (no frontend test framework exists in this project — RESEARCH.md confirmed; visual + build + grep is the verification contract)
</verification>

<success_criteria>
- UI-01: No Refresh stats button; stats auto-load on mount; no dead refreshing state
- UI-02: Popularity tiers Hidden Gem (<40, green) / Rising (40-69, yellow) / Mainstream (>=70, gray); icon and text colors consistent
- UI-03: Expanded panel uses bg-gray-800 (visible background)
- UI-04: "Your genre taste profile" subtitle
- Frontend build green
</success_criteria>

<output>
Create `.planning/phases/10-v1-2-ux-feedback-refinement/10-02-SUMMARY.md` when done
</output>
