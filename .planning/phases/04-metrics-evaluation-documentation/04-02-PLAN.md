---
phase: 04-metrics-evaluation-documentation
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - backend/apps/core/views.py
  - backend/config/urls.py
  - backend/tests/test_metrics.py
autonomous: true
requirements: []
tags: [backend, django, metrics, api, jaccard, rolling-window]

must_haves:
  truths:
    - "Authenticated GET /api/recommendation-metrics/ returns 200 with all 12 documented JSON fields when the user has ≥1 DailyGem (D-01, D-02)"
    - "GET /api/recommendation-metrics/ returns `{'message': 'No gems yet'}` (200) when the user has 0 DailyGems (D-01)"
    - "hidden_gem_rate is computed from `DailyGem.track_popularity < 40` — NOT from `RecommendationLog.was_novel`"
    - "avg_popularity is computed from `DailyGem.track_popularity` (never from `RecommendationLog.track_popularity`, which is always 0)"
    - "improvement_story compares first 7 vs last 7 gems by like-rate (D-04); falls back to first half vs second half when <7 gems; null when <2 gems"
    - "diversity_score is mean pairwise Jaccard distance across DailyGem tracks whose Track.genres is non-empty; returns null when <2 such gems (D-09, D-10, D-11)"
    - "top_genres_pct list in /api/recommendation-metrics/ response contains up to 10 genres normalized to percentage of total (D-07)"
    - "Authenticated GET /api/recommendation-trend/ returns `{'data': [{date, like_rate}, ...]}` with rolling 7-day like-rate per gem date; returns `{'data': [], 'message': 'Not enough data'}` when <2 dates (D-03, D-08)"
    - "Both endpoints are 403 (or 401) for unauthenticated requests (IsAuthenticated decorator enforced)"
    - "All ≥10 test stubs in backend/tests/test_metrics.py now PASS"
  artifacts:
    - path: backend/apps/core/views.py
      provides: get_recommendation_metrics and get_recommendation_trend view functions
      contains: 'def get_recommendation_metrics'
    - path: backend/config/urls.py
      provides: URL routes for both new endpoints
      contains: 'recommendation-metrics'
    - path: backend/tests/test_metrics.py
      provides: GREEN test implementations replacing the RED stubs from 04-01
      contains: 'self.assertEqual'
  key_links:
    - from: backend/config/urls.py
      to: backend/apps/core/views.get_recommendation_metrics
      via: path() registration
      pattern: 'recommendation_metrics'
    - from: backend/apps/core/views.py
      to: apps.core.models.DailyGem
      via: ORM filter aggregate
      pattern: 'DailyGem\\.objects\\.filter'
    - from: backend/apps/core/views.py
      to: apps.core.models.UserProfile
      via: data['taste_vector'] dict access
      pattern: "data\\.get\\(['\"]taste_vector"
    - from: backend/apps/core/views.py
      to: backend/tests/test_metrics.py
      via: tests assert on JSON response shape
      pattern: 'JsonResponse'
---

<objective>
Implement the two on-the-fly metrics endpoints that drive every Phase 4 frontend component. Both views are authenticated GET handlers, read-only over `DailyGem` + `UserProfile` + `RecommendationLog` + `Track`, with no new DB columns (D-01 LOCKED).

Purpose: provide a single backend contract that satisfies the existing `MetricsStrip` interface (LOCKED per D-02) AND the four new Wave 2 chart/stat components, plus a separate trend endpoint (D-03) for the 7-day rolling like-rate line chart.

Output: `views.get_recommendation_metrics`, `views.get_recommendation_trend`, two `path()` entries in `urls.py`, and a fully GREEN `backend/tests/test_metrics.py` with the stub bodies from 04-01 replaced by real assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metrics-evaluation-documentation/04-CONTEXT.md
@.planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md
@.planning/phases/04-metrics-evaluation-documentation/04-PATTERNS.md
@.planning/phases/04-metrics-evaluation-documentation/04-VALIDATION.md
@backend/apps/core/views.py
@backend/apps/core/models.py
@backend/config/urls.py
@frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx

<interfaces>
<!-- LOCKED interface from MetricsStrip.tsx lines 6-17 (Wave 2 frontend already coded against this shape). -->

From frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx:
```typescript
interface Metrics {
  total_recommended: number;
  avg_popularity: number;
  novel_track_rate: number;
  hidden_gem_rate: number;
  gem_total: number;
  gem_liked: number;
  gem_disliked: number;
  gem_acceptance_rate: number | null;   // 0..1 ratio, NOT a percentage
  top_genres: string[];
  message?: string;
}
```

Phase 4 EXTENSION fields (additive, MetricsStrip ignores unknown keys — non-breaking per 04-RESEARCH.md):
```typescript
top_genres_pct: Array<{genre: string, pct: number}>;        // top 10, sum(pct) ≈ 100.0
improvement_story: {                                          // null-safe each leaf
  first_7_rate: number | null;                                // 0..100 integer
  last_7_rate: number | null;                                 // 0..100 integer
  delta: number | null;                                       // signed integer pp
};
diversity_score: number | null;                               // 0.0..1.0 float, 4dp
```

From frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx (to be built in Wave 2 but the contract is fixed now):
```typescript
interface TrendPoint { date: string; like_rate: number; }    // date = "YYYY-MM-DD"; like_rate = 0..100 float
interface TrendResponse { data: TrendPoint[]; message?: string; }
```

From backend/apps/core/views.py existing patterns (lines 228–230, 359–370 — verified in 04-PATTERNS.md):
- Auth decorator stack: `@api_view(['GET'])` + `@permission_classes([IsAuthenticated])`
- Imports already present: `JsonResponse`, `api_view`, `permission_classes`, `IsAuthenticated`, `Track`, `RecommendationLog`, `DailyGem`, `logger` (from utils.logging_config)
- Imports to ADD: `from django.db.models import Avg`, `from itertools import combinations`, `from datetime import timedelta`, and (locally inside the view OK) `from .models import UserProfile`
- Error pattern: `except Exception as e: logger.error(f"Error in <view>: {str(e)}"); return JsonResponse({'error': '...'}, status=500)`

From backend/config/urls.py lines 39–53 (verified pattern):
- Existing path style: `path('api/<endpoint-name>/', views.<view_function>, name='<snake_case_name>'),`
- Trailing slash required.
- Add the new paths after the existing `api/get-user-name/` entry (line 52).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement get_recommendation_metrics view + URL route + fill TestMetricsEndpoint/TestTasteVector/TestJaccard test bodies</name>
  <files>backend/apps/core/views.py, backend/config/urls.py, backend/tests/test_metrics.py</files>
  <read_first>
    - backend/apps/core/views.py (existing imports block lines 1–32; existing auth decorator pattern at lines 228–230 on `check_spotify_token`; existing minimal-view template at lines 359–370 on `get_personalization_summary`; error-handling pattern at lines 340–343 on `get_track_recommendations`)
    - backend/apps/core/models.py (DailyGem schema lines 281–298; UserProfile.data lines around 60–80; Track.genres at line 167)
    - backend/config/urls.py (existing path entries lines 39–53; insertion point is immediately after line 52 `api/get-user-name/`)
    - backend/tests/test_metrics.py (the RED stubs from 04-01; each method's docstring describes the assertion to wire up)
    - backend/tests/test_feedback_learning.py (DB fixture creation pattern with `User.objects.create_user` + ORM creation of related rows — first 80 lines)
    - .planning/phases/04-metrics-evaluation-documentation/04-PATTERNS.md (Pattern Assignments → get_recommendation_metrics section; full computation skeleton lines ~71–146 in patterns doc)
    - .planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md (Pitfalls 1, 3, 4, 5; Data Model Findings — verifies which fields are actually populated)
  </read_first>
  <behavior>
    - Test 1 (TestMetricsEndpoint.test_returns_message_when_no_gems): authenticated GET to `/api/recommendation-metrics/` with zero DailyGem rows for the user → 200 + JSON body containing the `message` key set to a non-empty string. Other fields MAY be absent on this branch (the response is allowed to be just `{'message': 'No gems yet'}` per 04-PATTERNS.md).
    - Test 2 (TestMetricsEndpoint.test_gem_acceptance_rate_null_when_no_feedback): create 3 DailyGems, all with `was_liked=None`. Endpoint returns `gem_acceptance_rate` whose Python-side value equals `0.0` (3 liked / 3 total = 0). NOTE: the LOCKED interface says `number | null`; null is reserved for the zero-gems case only. With ≥1 gem we always return a numeric ratio. Test asserts a numeric type.
    - Test 3 (TestMetricsEndpoint.test_hidden_gem_rate_uses_track_popularity_lt_40): create 5 DailyGems with popularities `[10, 30, 50, 70, 90]`. Expected `hidden_gem_rate == 2/5 == 0.4` (10, 30 are < 40). Test must also assert that `RecommendationLog.was_novel` is NOT consulted (create a `RecommendationLog` with `was_novel=True` for an unrelated track to prove it doesn't leak into the metric).
    - Test 4 (TestMetricsEndpoint.test_response_includes_all_phase4_fields): assert the response dict (when gems exist) contains keys: `total_recommended`, `avg_popularity`, `novel_track_rate`, `hidden_gem_rate`, `gem_total`, `gem_liked`, `gem_disliked`, `gem_acceptance_rate`, `top_genres`, `top_genres_pct`, `improvement_story`, `diversity_score` — all 12 fields.
    - Test 5 (TestMetricsEndpoint.test_improvement_story_first_7_vs_last_7): create 14 DailyGems with `was_liked` pattern such that first 7 have 1 liked (rate 14%) and last 7 have 5 liked (rate 71%). Assert `improvement_story.first_7_rate == 14`, `last_7_rate == 71`, `delta == 57`.
    - Test 6 (TestMetricsEndpoint.test_improvement_story_null_when_fewer_than_2_gems): create 1 DailyGem. Assert `improvement_story.first_7_rate is None` OR `delta is None` (cold-start branch per D-04 specifics).
    - Test 7 (TestMetricsEndpoint.test_unauthenticated_returns_403): use the test client WITHOUT login. Assert `response.status_code in (401, 403)`.
    - Test 8 (TestTasteVector.test_top_genres_pct_normalized_to_100): create UserProfile with `data={'taste_vector': {'a': 3, 'b': 1, 'c': 1}}`. Assert `len(top_genres_pct) <= 10`, `abs(sum(p['pct'] for p in top_genres_pct) - 100.0) < 0.5`.
    - Test 9 (TestTasteVector.test_top_genres_capped_at_10): create taste_vector with 15 genres of equal count. Assert `len(top_genres) == 10 AND len(top_genres_pct) == 10`.
    - Test 10 (TestJaccard, all three pure-math methods): assert the math by importing the helper directly (see action item below: extract `_jaccard_distance` as a module-level function so it's unit-testable without HTTP). Cases per 04-RESEARCH.md Pattern 3: both empty → 0.0, fully disjoint → 1.0, partial overlap `{rock,jazz}` vs `{rock,pop}` → 1 - 1/3 ≈ 0.6667 (assert within 1e-4).
  </behavior>
  <action>
    Step 1 — Add the new imports to the top of `backend/apps/core/views.py` (alongside existing imports, do not duplicate). Required additions: `from django.db.models import Avg`; `from itertools import combinations`; `from datetime import timedelta`. `UserProfile` may be imported via `from .models import UserProfile` (top-level, alongside the existing `from .models import Track, RecommendationLog, DailyGem` import).

    Step 2 — Implement a pure-Python helper `_jaccard_distance(genres_a: list, genres_b: list) -> float` as a module-level function in `views.py` (NOT inside the view). This is the unit-testable surface for `TestJaccard`. Semantics per D-10: both empty → return 0.0; otherwise compute `1 - |A ∩ B| / |A ∪ B|` on `set()` of each list; if union is empty after dedup → return 0.0 (guard against ZeroDivisionError). The name starts with underscore by convention to mark it as a helper, but it must be importable: tests do `from apps.core.views import _jaccard_distance`.

    Step 3 — Implement `get_recommendation_metrics(request)` per the 04-PATTERNS.md skeleton (lines ~71–146 of that document). Apply the decorator stack `@api_view(['GET'])` + `@permission_classes([IsAuthenticated])`. Wrap the body in `try / except Exception` and use `logger.error` + 500 JSON response on failure, matching the existing `get_personalization_summary` pattern. Implements D-01, D-02, D-04, D-07.

    Key implementation rules (each tied to a verified research finding):
      - Use `DailyGem.objects.filter(user=user).order_by('date')` for the canonical user gem queryset. Materialize once via `.values('was_liked', 'track_popularity', 'date', 'track_id')` for downstream iteration (avoids re-hitting DB).
      - `gem_total` = `len(gem_list)`. If 0, return `JsonResponse({'message': 'No gems yet'})` and stop — this is the LOCKED early-exit branch (matches 04-PATTERNS.md and Test 1).
      - `gem_liked` = count of `g['was_liked'] is True`. `gem_disliked` = count of `g['was_liked'] is False`. Treat `None` as no feedback (do NOT count it as disliked) — Pitfall 5 in 04-RESEARCH.md.
      - `gem_acceptance_rate` = `gem_liked / gem_total` (float in 0..1) when `gem_total > 0`. (Returning a 0..1 ratio matches the LOCKED interface comment — MetricsStrip.tsx line 53 already multiplies by 100 for display.)
      - `avg_popularity` = `gems.aggregate(avg=Avg('track_popularity'))['avg'] or 0`, then `round(...)` to integer. MUST come from `DailyGem.track_popularity`, NOT `RecommendationLog.track_popularity` (Pitfall 4 — RecommendationLog.track_popularity is always 0).
      - `hidden_gem_rate` = `gems.filter(track_popularity__lt=40).count() / gem_total`, rounded to 4 decimal places. Field MUST be `DailyGem.track_popularity`, NOT `RecommendationLog.was_novel` (which is always True — Pitfall 4 and Critical Gap section of 04-RESEARCH.md).
      - `total_recommended` = `RecommendationLog.objects.filter(user=user).count()`.
      - `novel_track_rate` = same value as `hidden_gem_rate` (open-question A1 in 04-RESEARCH.md — the field exists in the LOCKED interface but MetricsStrip does NOT render it; reusing hidden_gem_rate satisfies the interface without inventing a new computation).
      - taste_vector → top_genres_pct: load `UserProfile.objects.get_or_create(user=user)[0].data.get('taste_vector', {})`. Sort by count desc, slice [:10]. `top_genres` = list of genre name strings. `top_genres_pct` = list of `{'genre': str, 'pct': float}` where pct = `round(count / sum_of_top_10_counts * 100, 1)`. If `sum == 0`, normalize against `1` (sentinel) to avoid divide-by-zero — empty taste_vector → empty list output. Implements D-07.
      - improvement_story: take `first_7 = gem_list[:7]`, `last_7 = gem_list[-7:]`. If `len(gem_list) < 2` → `{first_7_rate: None, last_7_rate: None, delta: None}`. If `2 <= len(gem_list) < 14`, the windows overlap — accept the overlap per CONTEXT.md "first half vs second half" spec, but DO NOT trim arbitrarily; just use first 7 and last 7 even if they overlap (delivers a meaningful trend signal even at 5 gems). `first_7_rate` = `round(liked / len(first_7) * 100)` as integer percentage; same for `last_7_rate`. `delta` = `last_7_rate - first_7_rate` (signed int). Implements D-04.
      - diversity_score: extract `track_ids` from gem_list, fetch `Track.objects.filter(id__in=track_ids).values('id', 'genres')` and build `{id: genres}`. Build `genre_lists = [track_genres.get(tid, []) for tid in track_ids]`. Filter to non-empty: `nonempty = [g for g in genre_lists if g]`. If `len(nonempty) < 2` → `diversity_score = None`. Otherwise `combinations(nonempty, 2)` → `_jaccard_distance(a, b)` per pair → `mean = round(sum(distances) / len(distances), 4)`. Implements D-09, D-10, D-11.

    Step 4 — Register the URL in `backend/config/urls.py`. Add this line immediately after the existing `path('api/get-user-name/', views.get_user_name, name='user_name')` entry at line 52: a `path('api/recommendation-metrics/', views.get_recommendation_metrics, name='recommendation_metrics')` entry. Match the existing trailing-slash and `name=` conventions exactly.

    Step 5 — Replace the RED stubs in `backend/tests/test_metrics.py` for classes `TestMetricsEndpoint`, `TestTasteVector`, and `TestJaccard`. (`TestTrendEndpoint` is handled in Task 2.) Use `django.test.TestCase` for DB tests (it provides `self.client` and per-test rollback) and `unittest.TestCase` for Jaccard math tests. Authenticate the test client with `self.client.force_login(self.user)` (Django's documented method) — do NOT manage session cookies manually.

    For Test 7 (unauthenticated 403): create a fresh test client WITHOUT calling `force_login`, hit the endpoint, assert status code. DRF returns 403 for IsAuthenticated by default; either 401 or 403 is acceptable per the behavior spec.

    Step 6 — Run `cd backend && python -m pytest tests/test_metrics.py::TestMetricsEndpoint tests/test_metrics.py::TestTasteVector tests/test_metrics.py::TestJaccard -x -q`. All ≥10 tests in scope must pass. Then run the full suite `pytest tests/ -x -q` and assert it remains green (Phase 1–3 tests untouched).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_metrics.py::TestMetricsEndpoint tests/test_metrics.py::TestTasteVector tests/test_metrics.py::TestJaccard -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def get_recommendation_metrics" backend/apps/core/views.py` ≥ 1
    - `grep -c "^def _jaccard_distance" backend/apps/core/views.py` ≥ 1
    - `grep -c "from django.db.models import Avg" backend/apps/core/views.py` ≥ 1
    - `grep -c "from itertools import combinations" backend/apps/core/views.py` ≥ 1
    - `grep -c "track_popularity__lt=40" backend/apps/core/views.py` ≥ 1 (Pitfall 4 mitigation explicit)
    - `grep -c "recommendation-metrics" backend/config/urls.py` ≥ 1 AND `grep -c "name='recommendation_metrics'" backend/config/urls.py` ≥ 1
    - `cd backend && python manage.py check` exits 0 (Django system check passes — URL resolves, view importable)
    - `cd backend && python -m pytest tests/test_metrics.py::TestMetricsEndpoint -x -q` exit code 0 with ≥7 passing tests
    - `cd backend && python -m pytest tests/test_metrics.py::TestJaccard -x -q` exit code 0 with ≥3 passing tests
    - `cd backend && python -m pytest tests/test_metrics.py::TestTasteVector -x -q` exit code 0 with ≥2 passing tests
    - Full suite remains green: `cd backend && python -m pytest tests/ -x -q` exit code 0
    - Behavior (single live integration spot-check): a Python REPL request via the Django test client to `/api/recommendation-metrics/` for a user with one DailyGem returns a dict whose keys include all 12 names listed in the `<interfaces>` block above.
  </acceptance_criteria>
  <done>get_recommendation_metrics view + URL route + _jaccard_distance helper implemented; TestMetricsEndpoint / TestTasteVector / TestJaccard test bodies replaced with real assertions and all pass; full suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement get_recommendation_trend view + URL route + fill TestTrendEndpoint test bodies</name>
  <files>backend/apps/core/views.py, backend/config/urls.py, backend/tests/test_metrics.py</files>
  <read_first>
    - backend/apps/core/views.py (verify Task 1 imports are now in place; existing `get_personalization_summary` shape at lines 359–370 as the skeleton to mirror)
    - backend/config/urls.py (verify Task 1's metrics URL is now present; insertion point for trend URL is immediately after that line)
    - backend/tests/test_metrics.py (the two TestTrendEndpoint stubs from 04-01)
    - .planning/phases/04-metrics-evaluation-documentation/04-PATTERNS.md (Pattern Assignments → get_recommendation_trend section; full skeleton lines ~163–190)
    - .planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md (Pattern 2 Rolling 7-Day Like-Rate; Assumption A2)
  </read_first>
  <behavior>
    - Test 1 (TestTrendEndpoint.test_returns_empty_data_with_message_when_fewer_than_2_dates): create 0 or 1 DailyGem rows. Assert response status 200, body equals `{'data': [], 'message': 'Not enough data'}` (or response['data'] == [] AND response['message'] is a non-empty string).
    - Test 2 (TestTrendEndpoint.test_rolling_7_day_window_correctness): create 10 DailyGems on 10 consecutive dates with `was_liked` = `[True, True, True, False, False, False, False, True, True, True]` (3 liked in first 7, 3 liked in last 7 — rolling rate stays at ~43%/57%). Assert: `data` length == 10 (one point per gem date); each point has keys `date` (string formatted YYYY-MM-DD) and `like_rate` (float 0..100); the rolling 7-day rate at the final date equals `round(3/7 * 100, 1)` (last 7 days: items[3:10] = [F,F,F,F,T,T,T] → 3 liked / 7 total ≈ 42.9).
    - Test 3 (TestTrendEndpoint.test_unauthenticated_returns_403): mirror Task 1 Test 7 for the trend endpoint.
  </behavior>
  <action>
    Step 1 — Implement `get_recommendation_trend(request)` in `backend/apps/core/views.py` per the 04-PATTERNS.md skeleton (lines ~163–190 of that document). Use the same decorator stack and try/except error pattern as Task 1. Implements D-03, D-08.

    Algorithm:
      - Load gems as a list of `{date, was_liked}` dicts ordered by date: `list(DailyGem.objects.filter(user=user).order_by('date').values('date', 'was_liked'))`.
      - `dates = sorted(set(g['date'] for g in gems))` — distinct gem dates only. (Per Assumption A2 in 04-RESEARCH.md, x-axis spacing may be uneven if dates have gaps; that's acceptable for an informational chart.)
      - For each date `d` in `dates`: define `window_start = d - timedelta(days=6)`; collect `window = [g for g in gems if window_start <= g['date'] <= d]`. Compute `liked = sum(1 for g in window if g['was_liked'] is True)`, `total = len(window)`, `like_rate = round((liked / total) * 100, 1) if total > 0 else 0.0`. Append `{'date': str(d), 'like_rate': like_rate}` to `data_points`.
      - If `len(data_points) < 2`: return `JsonResponse({'data': [], 'message': 'Not enough data'})`. Otherwise return `JsonResponse({'data': data_points})`.

    Error handling: same try/except → `logger.error` + 500 JsonResponse pattern as Task 1.

    The `from datetime import timedelta` import was added to views.py in Task 1 — do NOT re-import; if for some reason it's not there yet, add it at the top with the other imports (NOT inside the view body).

    Step 2 — Register the URL in `backend/config/urls.py`. Add `path('api/recommendation-trend/', views.get_recommendation_trend, name='recommendation_trend')` immediately after the `recommendation-metrics` line from Task 1. Trailing slash required.

    Step 3 — Replace the RED stubs for `TestTrendEndpoint` in `backend/tests/test_metrics.py`. Use `django.test.TestCase` with `self.client.force_login(self.user)` for the auth pattern. For the date fixture, use `datetime.date.today() - timedelta(days=n)` for `n in range(10)` so the test is deterministic and doesn't depend on system clock beyond `today()`. Use `self.assertAlmostEqual` (places=1) for the like_rate float comparison.

    For the unauthenticated test, mirror Task 1's pattern: fresh client, no force_login, assert `status_code in (401, 403)`.

    Step 4 — Run the full Phase 4 test scope and full suite. All must pass.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_metrics.py -x -q && python -m pytest tests/ -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^def get_recommendation_trend" backend/apps/core/views.py` ≥ 1
    - `grep -c "timedelta(days=6)" backend/apps/core/views.py` ≥ 1 (the rolling window is exactly 7 days inclusive = -6 to 0)
    - `grep -c "recommendation-trend" backend/config/urls.py` ≥ 1 AND `grep -c "name='recommendation_trend'" backend/config/urls.py` ≥ 1
    - `cd backend && python manage.py check` exits 0
    - `cd backend && python -m pytest tests/test_metrics.py::TestTrendEndpoint -x -q` exit code 0 with ≥3 passing tests
    - Full Phase 4 test file green: `cd backend && python -m pytest tests/test_metrics.py -x -q` exits 0 with 0 failures/errors and ≥10 passing tests total across all 4 classes
    - Full suite green: `cd backend && python -m pytest tests/ -x -q` exits 0
    - Behavior spot-check: Django test client GET `/api/recommendation-trend/` for a 0-gem user returns body matching regex `"data".*\[\]` AND contains the string `"message"`
  </acceptance_criteria>
  <done>get_recommendation_trend view + URL route implemented; TestTrendEndpoint test bodies replaced with real assertions and all pass; full test_metrics.py green; full backend suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (cookie session) → Django views | Authenticated user reads their own metrics; `IsAuthenticated` decorator is the gate |
| Django views → ORM (DailyGem, RecommendationLog, UserProfile, Track) | Server-side queryset construction; user is pinned to `request.user` to prevent cross-user data leak |
| Django views → JsonResponse → client | Response is JSON; consumed by `MetricsStrip` and Wave 2 components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Spoofing | get_recommendation_metrics, get_recommendation_trend | mitigate | Both views use `@permission_classes([IsAuthenticated])` so unauthenticated requests get 403 (verified by Test 7 / TestTrendEndpoint.test_unauthenticated). All querysets are filtered by `request.user` — never trust a `user_id` parameter from the request. |
| T-04-06 | Information Disclosure | DailyGem / RecommendationLog cross-user leak | mitigate | Both views filter strictly on `request.user`. No path parameters or query parameters identify a user — the source of identity is the authenticated session only. ASVS L1 V4.1.1 satisfied. |
| T-04-07 | Tampering | URL routing | mitigate | URLs registered with explicit `name=` so reverse lookups don't pick the wrong view. Trailing slashes match Django's `APPEND_SLASH=True` default to prevent silent redirects that could be exploited for SSRF or open-redirect (no redirect here, but consistency hardens the surface). |
| T-04-08 | Denial of Service | O(N²) Jaccard diversity on large gem sets | accept | Single-user portfolio app with <1000 lifetime gems projected. N²/2 ≤ 500K pair operations on tiny set objects — milliseconds. If gem_total grows pathologically, this view's `diversity_score` would degrade, but the rest of the response is O(N). Risk LOW; document in CONCEPTS.md (Wave 3) as a known scaling limitation. |
| T-04-09 | Repudiation | Metrics view side effects | mitigate (N/A) | Both views are pure reads; no DB writes, no third-party calls. No audit log required. The `logger.error` on exception paths is the only side effect. |
| T-04-10 | Information Disclosure | error responses leak stack trace | mitigate | `except Exception` branches return `{'error': '<generic>'}` strings, NOT `str(e)` to the client. Stack traces stay in server logs via `logger.error`. Pattern verified in 04-PATTERNS.md "Error Handling" subsection. |
| T-04-11 | Elevation of Privilege | get_or_create on UserProfile silently creates a row | accept | `UserProfile.objects.get_or_create(user=user)` is the existing pattern used in Phase 2 and Phase 3 views — see `views.py` line 803–811 context. Creating an empty UserProfile for an authenticated user has no security impact (the user is already authenticated and would have one created anyway on first gem fetch). |
</threat_model>

<verification>
1. `cd backend && python manage.py check` — Django system check passes; both URLs resolve, both views importable.
2. `cd backend && python -m pytest tests/test_metrics.py -x -q` — green, ≥10 passing tests, 0 failures.
3. `cd backend && python -m pytest tests/ -x -q` — entire backend suite green.
4. `grep -nE "recommendation-metrics|recommendation-trend" backend/config/urls.py` — two distinct URL entries.
5. `grep -nE "^def (get_recommendation_metrics|get_recommendation_trend|_jaccard_distance)" backend/apps/core/views.py` — three function definitions present.
6. (Smoke) `cd backend && python manage.py runserver` then `curl -s http://127.0.0.1:8000/api/recommendation-metrics/` returns 403 (no session) — confirms IsAuthenticated guard.
</verification>

<success_criteria>
- Two new views (`get_recommendation_metrics`, `get_recommendation_trend`) implemented in `backend/apps/core/views.py`
- One pure helper (`_jaccard_distance`) implemented at module scope and unit-tested
- Two new URL paths registered in `backend/config/urls.py`
- All ≥10 stub tests from 04-01 turned GREEN; new TestTrendEndpoint tests added (≥3); total ≥13 green Phase 4 tests
- Full backend test suite remains green
- IsAuthenticated decorator enforced on both views (T-04-05 mitigated)
- All metrics computed from `DailyGem.*` and `UserProfile.data` only — no new DB columns (D-01 satisfied)
- `hidden_gem_rate` and `avg_popularity` come from `DailyGem.track_popularity`, NOT from RecommendationLog (Pitfall 4 mitigated; explicit grep gate in acceptance criteria)
</success_criteria>

<output>
After completion, create `.planning/phases/04-metrics-evaluation-documentation/04-02-SUMMARY.md` capturing:
- Final response shape for both endpoints (actual JSON keys returned)
- Notable decisions: how cold-start (0 gems, 1 gem, <7 gems) was handled in each branch
- Test count: actual passing count for each test class
- Any deviations from 04-PATTERNS.md skeleton and the reason
</output>
