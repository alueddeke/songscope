---
phase: 04-metrics-evaluation-documentation
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified:
  - frontend/app/profile/page.tsx
  - frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx
  - frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx
  - frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx
  - frontend/app/profile/components/DiversityScore/DiversityScore.tsx
autonomous: false
requirements: []
tags: [frontend, react, nextjs, recharts, profile-page, wiring]

must_haves:
  truths:
    - "MetricsStrip is rendered on /profile (it was unimported before this plan — verified gap) (D-02)"
    - "Below TopArtists, a new <section> titled 'How your taste is evolving' renders four components: LikeTrendChart, TasteProfileChart, DiversityScore, ImprovementStory (D-06)"
    - "LikeTrendChart renders a Recharts LineChart with stroke=#1DB954 when /api/recommendation-trend/ returns ≥2 data points (D-05, D-08)"
    - "TasteProfileChart renders a Recharts horizontal BarChart with fill=#1DB954 when /api/recommendation-metrics/ returns a non-empty top_genres_pct array (D-05, D-07)"
    - "DiversityScore shows a percentage tile when diversity_score is non-null, '—' when null"
    - "ImprovementStory renders left/right stat blocks with a delta arrow when both first_7_rate and last_7_rate are non-null; renders nothing when first_7_rate is null"
    - "All four new components have `\"use client\"` as line 1; profile/page.tsx remains a server component (no `\"use client\"` added)"
    - "`cd frontend && npx tsc --noEmit` (or `npm run build`) exits 0 — no TypeScript errors"
  artifacts:
    - path: frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx
      provides: 7-day rolling like-rate line chart
      min_lines: 40
      contains: 'stroke="#1DB954"'
    - path: frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx
      provides: top-10 genre horizontal bar chart
      min_lines: 40
      contains: 'fill="#1DB954"'
    - path: frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx
      provides: first-7-vs-last-7 like-rate before/after with delta
      min_lines: 30
      contains: 'improvement_story'
    - path: frontend/app/profile/components/DiversityScore/DiversityScore.tsx
      provides: single Jaccard-derived diversity tile
      min_lines: 25
      contains: 'diversity_score'
    - path: frontend/app/profile/page.tsx
      provides: wiring for MetricsStrip + new section containing all four new components
      contains: 'How your taste is evolving'
  key_links:
    - from: frontend/app/profile/page.tsx
      to: frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
      via: ESM import + JSX render
      pattern: "import MetricsStrip from './components/MetricsStrip/MetricsStrip'"
    - from: frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx
      to: /api/recommendation-trend/
      via: get() helper from services/axios
      pattern: '/api/recommendation-trend/'
    - from: frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx
      to: /api/recommendation-metrics/
      via: get() helper reading top_genres_pct
      pattern: 'top_genres_pct'
    - from: frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx
      to: /api/recommendation-metrics/
      via: get() helper reading improvement_story
      pattern: 'improvement_story'
    - from: frontend/app/profile/components/DiversityScore/DiversityScore.tsx
      to: /api/recommendation-metrics/
      via: get() helper reading diversity_score
      pattern: 'diversity_score'
---

<objective>
Build the four new "use client" components mandated by 04-UI-SPEC.md (LikeTrendChart, TasteProfileChart, ImprovementStory, DiversityScore), and wire both the existing MetricsStrip (which was built but never imported — a verified gap in 04-RESEARCH.md) AND the new components into `frontend/app/profile/page.tsx`.

Purpose: deliver the visual half of Phase 4. After this plan, a logged-in user scrolling to the bottom of `/profile` sees a complete metrics dashboard driven by the Wave 1 endpoints. Closes Pitfall 2 from 04-RESEARCH.md (MetricsStrip unwired) and implements D-05, D-06, D-07, D-08.

Output: four new component files at `frontend/app/profile/components/<Name>/<Name>.tsx`, plus an updated `profile/page.tsx` with five new imports and a new `<section>` block placed below the existing TopArtists section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-metrics-evaluation-documentation/04-CONTEXT.md
@.planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md
@.planning/phases/04-metrics-evaluation-documentation/04-PATTERNS.md
@.planning/phases/04-metrics-evaluation-documentation/04-UI-SPEC.md
@frontend/app/profile/page.tsx
@frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx
@frontend/app/profile/components/DailyGem/DailyGem.tsx
@frontend/app/profile/components/TopArtists/TopArtists.tsx
@frontend/services/axios.ts

<interfaces>
<!-- Backend response shapes (LOCKED by Wave 1, plan 04-02). -->

From /api/recommendation-metrics/ (LOCKED):
```typescript
interface Metrics {
  total_recommended: number;
  avg_popularity: number;
  novel_track_rate: number;
  hidden_gem_rate: number;
  gem_total: number;
  gem_liked: number;
  gem_disliked: number;
  gem_acceptance_rate: number | null;
  top_genres: string[];
  top_genres_pct: Array<{genre: string, pct: number}>;
  improvement_story: { first_7_rate: number | null; last_7_rate: number | null; delta: number | null; };
  diversity_score: number | null;
  message?: string;
}
```

From /api/recommendation-trend/ (LOCKED):
```typescript
interface TrendPoint { date: string; like_rate: number; }   // date = "YYYY-MM-DD", like_rate = 0..100
interface TrendResponse { data: TrendPoint[]; message?: string; }
```

<!-- Existing frontend helpers (reuse, do NOT reimplement). -->

From frontend/services/axios.ts:
- `get<T>(path: string): Promise<T>` — handles cookies, baseURL, JSON parse

From frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx (analog for fetch + silent-fail pattern):
- `useState/useEffect` shell, `get<Metrics>("/api/recommendation-metrics/")`, `catch {}` silent-fail, `setLoading(false)` in finally
- Local `Stat` component pattern (lines 19–26): `<div className="flex flex-col gap-0.5 min-w-[100px]"><span className="text-green text-lg font-bold tabular-nums">{value}</span><span className="text-gray-500 text-xs uppercase tracking-widest">{label}</span></div>`

From frontend/app/profile/components/DailyGem/DailyGem.tsx (loading pattern lines 58–73):
- 3 bouncing dots: `<div className="flex gap-1 justify-center py-8">{[0,1,2].map(i => <div key={i} className="w-2 h-2 rounded-full bg-green animate-bounce" style={{animationDelay: `${i*0.15}s`}}/>)}</div>`

<!-- UI tokens (LOCKED by 04-UI-SPEC.md, do NOT introduce new colors/sizes/spacings). -->

Colors: stroke `#1DB954` for LineChart Line; fill `#1DB954` for BarChart Bar; gridline `#374151`; axis tick fill `#6b7280`; YAxis category-tick fill `#9ca3af`; tooltip bg `#111827`, border `#374151`, text `#f3f4f6`.
Typography: section heading `text-2xl font-bold text-white`; label `text-xs text-gray-500 uppercase tracking-widest`; stat value `text-lg font-bold tabular-nums text-green`; body `text-sm text-gray-300`.
Spacing: section `py-16 px-4 md:px-8 lg:px-16 border-t border-gray-800`; grid `grid-cols-1 lg:grid-cols-2 gap-12`; stat row `flex flex-wrap gap-8 mt-12 pt-8 border-t border-gray-800`.

<!-- Import path convention used throughout profile/components/ — match exactly so axios.ts resolves. -->

From frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx line 4:
```typescript
import { get } from "../../../../services/axios";
```
(Four levels up from `app/profile/components/<Name>/`. Use this exact relative path in the new files for consistency.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LikeTrendChart + TasteProfileChart (the two chart components)</name>
  <files>frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx, frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx</files>
  <read_first>
    - frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx (entire file — fetch + silent-fail + interface declaration pattern)
    - frontend/app/profile/components/DailyGem/DailyGem.tsx (lines 58–73 for the 3-dot loading pattern; lines 1–3 for the `"use client"` directive)
    - frontend/services/axios.ts (signature of `get<T>(path)` helper)
    - .planning/phases/04-metrics-evaluation-documentation/04-UI-SPEC.md (Component Inventory §1 LikeTrendChart and §2 TasteProfileChart — locked design)
    - .planning/phases/04-metrics-evaluation-documentation/04-PATTERNS.md (LikeTrendChart pattern, lines ~380–425; TasteProfileChart horizontal BarChart pattern, lines ~442–456)
    - .planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md (Pattern 4 + Pattern 5 verified Recharts in Next.js 14 patterns; Pitfall 6 hydration warning)
    - frontend/tailwind.config.ts (line 23 confirms `green: "#1DB954"`)
  </read_first>
  <action>
    Create two new component files using the directory pattern `frontend/app/profile/components/<Name>/<Name>.tsx`. Both files MUST start with `"use client";` on line 1 (Pitfall 6 / Next.js 14 App Router requirement — verified in 04-RESEARCH.md). NO `next/dynamic` wrapper, NO `ssr:false` — bare `"use client"` is sufficient for Recharts 3.x in Next.js 14.

    **LikeTrendChart.tsx** (implements D-08, locked):
    - Imports: `useState`, `useEffect` from `react`; `ResponsiveContainer`, `LineChart`, `Line`, `XAxis`, `YAxis`, `Tooltip`, `CartesianGrid` from `recharts`; `get` from `"../../../../services/axios"`.
    - Local interfaces `TrendPoint { date: string; like_rate: number }` and `TrendResponse { data: TrendPoint[]; message?: string }`.
    - State: `data: TrendPoint[]` (default `[]`), `loading: boolean` (default `true`).
    - `useEffect(() => { get<TrendResponse>("/api/recommendation-trend/").then(r => setData(r.data || [])).catch(() => {}).finally(() => setLoading(false)); }, [])` — silent-fail per UI-SPEC Copywriting Contract row "API error (charts) — Silent fail".
    - Render branches:
      1. `loading` → the 3-dot bouncing-dots pattern wrapped in `<div className="flex gap-1 justify-center py-8">...</div>` (copy from DailyGem.tsx lines 58–73, but without the "Finding your gem for today…" text per UI-SPEC row "Loading state copy").
      2. `data.length < 2` → cold-start message `<p className="text-gray-400 text-sm text-center py-8">Not enough data yet — your like-rate trend will appear after a few days of gems.</p>` (exact copy from UI-SPEC).
      3. Otherwise → `<ResponsiveContainer width="100%" height={220}>` wrapping a `<LineChart data={data}>` containing:
         - `<CartesianGrid stroke="#374151" />`
         - `<XAxis dataKey="date" tickFormatter={d => new Date(d).toLocaleDateString('en-US', {month:'short', day:'numeric'})} tick={{ fill: "#6b7280", fontSize: 12 }} />`
         - `<YAxis domain={[0, 100]} tick={{ fill: "#6b7280", fontSize: 12 }} />`
         - `<Tooltip formatter={(v: number) => \`${v}%\`} contentStyle={{ background: "#111827", border: "1px solid #374151" }} itemStyle={{ color: "#f3f4f6" }} />`
         - `<Line type="monotone" dataKey="like_rate" stroke="#1DB954" strokeWidth={2} dot={false} />`
    - Export as default function `LikeTrendChart()`.

    **TasteProfileChart.tsx** (implements D-07, locked):
    - Imports: same React hooks; `ResponsiveContainer`, `BarChart`, `Bar`, `XAxis`, `YAxis`, `Tooltip` from `recharts`; `get` helper.
    - Local interfaces `GenrePct { genre: string; pct: number }` and `MetricsResponse { top_genres_pct?: GenrePct[]; message?: string }`. The component cherry-picks `top_genres_pct` from the big metrics payload.
    - State: `data: GenrePct[]` (default `[]`), `loading: boolean` (default `true`).
    - `useEffect`: same `get<MetricsResponse>("/api/recommendation-metrics/").then(r => setData(r.top_genres_pct || [])).catch(() => {}).finally(() => setLoading(false))`.
    - Render branches:
      1. `loading` → 3-dot pattern.
      2. `data.length === 0` → cold-start message `<p className="text-gray-400 text-sm text-center py-8">Your taste profile will appear once your listening history builds up.</p>` (exact copy from UI-SPEC).
      3. Otherwise → `<ResponsiveContainer width="100%" height={280}>` wrapping `<BarChart layout="vertical" data={data}>` containing:
         - `<XAxis type="number" domain={[0, 100]} tick={{ fill: "#6b7280", fontSize: 12 }} />`
         - `<YAxis type="category" dataKey="genre" width={120} tick={{ fill: "#9ca3af", fontSize: 12 }} />`
         - `<Tooltip formatter={(v: number) => \`${v}%\`} contentStyle={{ background: "#111827", border: "1px solid #374151" }} itemStyle={{ color: "#f3f4f6" }} />`
         - `<Bar dataKey="pct" fill="#1DB954" radius={[0, 3, 3, 0]} />`
    - Export as default function `TasteProfileChart()`.

    Both files must use the relative axios import path `"../../../../services/axios"` to match the existing MetricsStrip convention exactly. Do NOT introduce a `@/services/axios` alias if it isn't already in tsconfig (verify by reading frontend/tsconfig.json `paths`; if alias is not configured, use the relative path).

    Do NOT add any external state management, context, or props. Each chart is self-contained and self-fetching. Do NOT memoize, debounce, or add retry logic — silent fail is the locked contract (UI-SPEC Copywriting Contract).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc_out.log && [ ! -s /tmp/tsc_out.log -o ! "$(grep -E 'error TS' /tmp/tsc_out.log)" ]</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx` exists AND has `"use client";` as the literal first non-empty line: `head -1 frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx | grep -c '"use client"'` ≥ 1
    - File `frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx` exists AND has `"use client";` as the literal first non-empty line
    - LikeTrendChart imports `LineChart` from `recharts`: `grep -c "from \"recharts\"" frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx` ≥ 1 AND `grep -c "LineChart" frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx` ≥ 1
    - LikeTrendChart calls `/api/recommendation-trend/`: `grep -c "/api/recommendation-trend/" frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx` ≥ 1
    - LikeTrendChart uses LOCKED color: `grep -c 'stroke="#1DB954"' frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx` ≥ 1
    - TasteProfileChart imports `BarChart` from recharts: `grep -c "BarChart" frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx` ≥ 1
    - TasteProfileChart calls `/api/recommendation-metrics/` and reads `top_genres_pct`: `grep -c "top_genres_pct" frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx` ≥ 1
    - TasteProfileChart uses LOCKED color: `grep -c 'fill="#1DB954"' frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx` ≥ 1
    - Both files include the cold-start empty-state copy (exact strings from UI-SPEC): `grep -c "Not enough data yet" frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx` ≥ 1 AND `grep -c "Your taste profile will appear" frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx` ≥ 1
    - TypeScript compiles cleanly: `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "error TS"` == 0
    - Neither file adds any `axios` import other than the local helper: `grep -c "import.*from .axios" frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx` == 0 (the helper is `"../../../../services/axios"`, which is matched by a different grep that uses the literal services path)
  </acceptance_criteria>
  <done>LikeTrendChart and TasteProfileChart files exist, `"use client"` on line 1, correct Recharts imports + axios helper import, locked color tokens applied, cold-start empty-state copy matches UI-SPEC, TypeScript build is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build ImprovementStory + DiversityScore (the two stat-tile components)</name>
  <files>frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx, frontend/app/profile/components/DiversityScore/DiversityScore.tsx</files>
  <read_first>
    - frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx (Stat sub-component at lines 19–26; null-render pattern at line 50 `if (loading || !metrics || metrics.message) return null;`)
    - .planning/phases/04-metrics-evaluation-documentation/04-UI-SPEC.md (Component Inventory §3 ImprovementStory and §4 DiversityScore; Copywriting Contract rows for both)
    - .planning/phases/04-metrics-evaluation-documentation/04-PATTERNS.md (ImprovementStory section lines ~480–504; DiversityScore section lines ~510–520)
  </read_first>
  <action>
    Both components are pure data tiles, no charts. Both fetch `/api/recommendation-metrics/` (yes, this is a duplicate of TasteProfileChart's call — accepted per 04-RESEARCH.md "Anti-Patterns to Avoid" note: "Either lift state to a parent component or accept four calls (single-user app — acceptable)"). Do NOT prematurely optimize; keep each component self-fetching for isolation.

    **ImprovementStory.tsx** (implements D-04):
    - `"use client";` line 1; same imports pattern as Task 1.
    - Local interfaces `Story { first_7_rate: number | null; last_7_rate: number | null; delta: number | null }` and `MetricsResponse { improvement_story?: Story; message?: string }`.
    - State: `story: Story | null` (default `null`), `loading: boolean` (default `true`).
    - useEffect: `get<MetricsResponse>("/api/recommendation-metrics/").then(r => setStory(r.improvement_story ?? null)).catch(() => {}).finally(() => setLoading(false))`.
    - Render branches:
      1. `loading || !story || story.first_7_rate === null || story.last_7_rate === null` → return `null` (silent skip per UI-SPEC §3 cold-start row "show skip — render nothing").
      2. Otherwise → two side-by-side `Stat`-style blocks plus a delta badge. Define a local `Stat` component matching MetricsStrip's exact JSX (lines 19–26): `<div className="flex flex-col gap-0.5 min-w-[100px]"><span className="text-green text-lg font-bold tabular-nums">{value}</span><span className="text-gray-500 text-xs uppercase tracking-widest">{label}</span></div>`. Wrap the two Stats and the delta badge in `<div className="flex items-end gap-6">...</div>`.
      3. Left Stat: label `"When I started"`, value `\`${story.first_7_rate}%\``.
      4. Right Stat: label `"Now"`, value `\`${story.last_7_rate}%\``.
      5. Delta badge: a `<span>` whose class is `text-green text-xs` when `story.delta > 0`, `text-red-400 text-xs` when `story.delta < 0`, and `text-gray-500 text-xs` when `story.delta === 0`. Text: `▲ +{delta}pp` for positive, `▼ {delta}pp` (delta is already negative, so display as `▼ -3pp` style) for negative, `— 0pp` for zero. Exact copy strings from UI-SPEC Copywriting Contract.

    **DiversityScore.tsx** (implements D-11):
    - `"use client";` line 1; same import pattern.
    - Local interface `MetricsResponse { diversity_score?: number | null; message?: string }`.
    - State: `score: number | null` (default `null`), `loading: boolean` (default `true`).
    - useEffect: `get<MetricsResponse>("/api/recommendation-metrics/").then(r => setScore(r.diversity_score ?? null)).catch(() => {}).finally(() => setLoading(false))`.
    - Render branches:
      1. `loading` → return `null` (silent loading per MetricsStrip convention; this is a tile, not a chart, so no bouncing dots).
      2. Otherwise render a Stat-style block: label `"Genre diversity"` (text-xs uppercase tracking-widest gray-500), value either `${(score * 100).toFixed(0)}%` when score is a number, or `"—"` when `score === null` (cold-start). Apply `text-green text-lg font-bold tabular-nums` to the value.
      3. Add a `title="How different your recommended tracks have been from each other. 100% = every track from a completely different genre."` attribute on the wrapping `<div>` for the native browser tooltip (per UI-SPEC Copywriting Contract row "Diversity score tooltip"). Wrap content in `<div className="flex flex-col gap-0.5 min-w-[100px]" title="...">...</div>`.

    Both files must use the same `"../../../../services/axios"` import path. No new external deps. Do NOT add Recharts imports to these two files — they are not charts.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -cE "error TS" | awk '{exit !($1 == 0)}'</automated>
  </verify>
  <acceptance_criteria>
    - File `frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx` exists AND has `"use client";` on line 1
    - File `frontend/app/profile/components/DiversityScore/DiversityScore.tsx` exists AND has `"use client";` on line 1
    - ImprovementStory reads `improvement_story`: `grep -c "improvement_story" frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx` ≥ 1
    - ImprovementStory includes copy strings: `grep -cE "When I started|Now" frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx` ≥ 2
    - ImprovementStory renders delta with arrow chars: `grep -cE "▲|▼" frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx` ≥ 2
    - DiversityScore reads `diversity_score`: `grep -c "diversity_score" frontend/app/profile/components/DiversityScore/DiversityScore.tsx` ≥ 1
    - DiversityScore uses the locked label text: `grep -c "Genre diversity" frontend/app/profile/components/DiversityScore/DiversityScore.tsx` ≥ 1
    - DiversityScore handles null with "—": `grep -cE '"—"|"—"' frontend/app/profile/components/DiversityScore/DiversityScore.tsx` ≥ 1 (UTF-8 em dash, exact char from UI-SPEC)
    - Neither file imports from `recharts`: `grep -cE "from \"recharts\"" frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx frontend/app/profile/components/DiversityScore/DiversityScore.tsx` == 0
    - TypeScript compiles: `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "error TS"` == 0
  </acceptance_criteria>
  <done>ImprovementStory and DiversityScore files exist with `"use client"`, correct API field reads, locked copy strings and color tokens, null-handling branches per UI-SPEC, TypeScript builds clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire MetricsStrip + four new components into profile/page.tsx with the new section</name>
  <files>frontend/app/profile/page.tsx</files>
  <read_first>
    - frontend/app/profile/page.tsx (entire current file — note it is a SERVER component, uses `cookies()`, has NO `"use client"` directive; existing import lines 1–5; existing JSX at lines 41–71 with `<section>` blocks)
    - .planning/phases/04-metrics-evaluation-documentation/04-UI-SPEC.md (Section wrapper subsection — exact JSX template for the new <section>; lines ~148–168 contain the locked block)
    - .planning/phases/04-metrics-evaluation-documentation/04-PATTERNS.md (profile/page.tsx pattern lines ~256–311 — import addition + section wrapper)
    - .planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md (Pitfall 7: profile/page.tsx is a server component — do NOT add `"use client"`, `useState`, or `useEffect`)
  </read_first>
  <action>
    Modify `frontend/app/profile/page.tsx`. This file is a server component; the change is purely additive: 5 new ESM imports near the top and a new JSX block inserted after the existing `<section className="py-64"><TopArtists/></section>` block (around current line 67).

    Step 1 — Add 5 new import statements after the existing `import TopArtists from './components/TopArtists/TopArtists'` (line 5). New lines, in this order:
    ```
    import MetricsStrip from './components/MetricsStrip/MetricsStrip'
    import LikeTrendChart from './components/LikeTrendChart/LikeTrendChart'
    import TasteProfileChart from './components/TasteProfileChart/TasteProfileChart'
    import DiversityScore from './components/DiversityScore/DiversityScore'
    import ImprovementStory from './components/ImprovementStory/ImprovementStory'
    ```
    (Code block shown for clarity — when implementing, add these as 5 plain import lines; do not embed inside markdown.)

    Step 2 — Insert the new JSX block immediately after the closing `</section>` of the TopArtists section, INSIDE the existing `<div className=" w-[100%] max-w-[1300px] mx-auto">` wrapper (so all profile content stays within the page's max-width container). The block per 04-UI-SPEC.md "Section wrapper" subsection:

    First, render `<MetricsStrip />` (no extra wrapper — MetricsStrip provides its own `<div className="w-full border-t border-gray-800 py-6 ...">` per its existing code at line 56). Then render the new `<section>`:

    - Outer: `<section className="w-full border-t border-gray-800 py-16 px-4 md:px-8 lg:px-16">`
    - Inside: `<h2 className="text-2xl font-bold text-white mb-8">How your taste is evolving</h2>`
    - Then a `<div className="grid grid-cols-1 lg:grid-cols-2 gap-12">` containing two child `<div>`s — left with label "Like-rate trend (7-day rolling)" + `<LikeTrendChart />`; right with label "Your taste profile" + `<TasteProfileChart />`. Labels use class `text-xs text-gray-500 uppercase tracking-widest mb-4`.
    - After the grid, a `<div className="flex flex-wrap gap-8 mt-12 pt-8 border-t border-gray-800">` containing `<DiversityScore />` and `<ImprovementStory />` (order: DiversityScore first per UI-SPEC).
    - Close the section.

    Step 3 — CRITICAL: do NOT add `"use client"` to this file. Do NOT add `useState`, `useEffect`, `onClick`, or any other client-side hook. All five new imports refer to `"use client"` files; React's RSC boundary handles the transition automatically. (Pitfall 7 in 04-RESEARCH.md.)

    Step 4 — Do NOT modify the existing `getUserName()` function, the existing `<section>` blocks (welcome hero, Recommendation, TopArtists), the Image import, the `cookies()` call, or the `redirect()` calls. Pure additive change.

    Step 5 — Save the file. Confirm by running `cd frontend && npx tsc --noEmit` (must pass with 0 errors) and `cd frontend && npm run build 2>&1 | grep -E "Compiled successfully|error"` (must show "Compiled successfully" if the script exists; otherwise tsc --noEmit is the proof).
  </action>
  <verify>
    <automated>cd frontend && grep -c "How your taste is evolving" app/profile/page.tsx | awk '{exit !($1 >= 1)}' && grep -c "import MetricsStrip" app/profile/page.tsx | awk '{exit !($1 >= 1)}' && npx tsc --noEmit 2>&1 | grep -cE "error TS" | awk '{exit !($1 == 0)}'</automated>
  </verify>
  <acceptance_criteria>
    - profile/page.tsx contains exactly 5 new imports — all named imports referencing the new and existing component files: `grep -cE "^import (MetricsStrip|LikeTrendChart|TasteProfileChart|DiversityScore|ImprovementStory) from " frontend/app/profile/page.tsx` ≥ 5
    - profile/page.tsx contains the locked section heading text: `grep -c "How your taste is evolving" frontend/app/profile/page.tsx` == 1
    - profile/page.tsx contains the locked label texts: `grep -c "Like-rate trend (7-day rolling)" frontend/app/profile/page.tsx` == 1 AND `grep -c "Your taste profile" frontend/app/profile/page.tsx` == 1
    - profile/page.tsx renders all five components in JSX (self-closing tags): `grep -cE "<(MetricsStrip|LikeTrendChart|TasteProfileChart|DiversityScore|ImprovementStory) ?/>" frontend/app/profile/page.tsx` ≥ 5
    - profile/page.tsx is STILL a server component — no `"use client"` directive added: `grep -c '"use client"' frontend/app/profile/page.tsx` == 0
    - profile/page.tsx still imports `cookies` from `next/headers`: `grep -c "from 'next/headers'" frontend/app/profile/page.tsx` ≥ 1
    - profile/page.tsx has NOT introduced `useState`, `useEffect`, or `onClick`: `grep -cE "useState|useEffect|onClick" frontend/app/profile/page.tsx` == 0
    - TypeScript compiles cleanly across the whole frontend: `cd frontend && npx tsc --noEmit 2>&1 | grep -cE "error TS"` == 0
    - The Next.js build succeeds (if `npm run build` is wired): `cd frontend && npm run build 2>&1 | tail -50 | grep -cE "Compiled successfully|✓ Compiled"` ≥ 1 (skip this check if the build script is not configured — tsc gate already proves type validity)
  </acceptance_criteria>
  <done>profile/page.tsx imports all five components, renders MetricsStrip and the new section with locked copy strings and layout, remains a server component (no `"use client"`), TypeScript and Next.js build clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of the metrics dashboard at /profile</name>
  <files>(human verification — no file edits; reads frontend/app/profile/page.tsx output in browser)</files>
  <read_first>
    - .planning/phases/04-metrics-evaluation-documentation/04-UI-SPEC.md (Component Inventory, Copywriting Contract — used as the visual checklist below)
    - .planning/phases/04-metrics-evaluation-documentation/04-VALIDATION.md (Manual-Only Verifications table — three of the five rows are this task)
  </read_first>
  <what-built>
    A logged-in user visiting `/profile` now sees, scrolling down from the top:
    1. Welcome hero with the user's display name (unchanged from before)
    2. Recommendation / DailyGem section (unchanged)
    3. TopArtists section (unchanged)
    4. **NEW**: MetricsStrip row showing gem count, acceptance rate, hidden gem rate, avg popularity, top genres (this component was built earlier but never wired)
    5. **NEW**: A "How your taste is evolving" section containing:
       - Left column: a Recharts line chart showing 7-day rolling like-rate over time (or "Not enough data yet…" if <2 gem dates)
       - Right column: a Recharts horizontal bar chart showing top-10 genre percentages (or empty-state copy if no taste vector)
       - Below those, a row containing the DiversityScore tile and the ImprovementStory delta block
  </what-built>
  <how-to-verify>
    Prerequisite: backend running (`cd backend && python manage.py runserver`), frontend dev server running (`cd frontend && npm run dev`), and you are logged in via Spotify OAuth so a `sessionid` cookie exists in the browser.

    1. **Navigate** to `http://localhost:3000/profile` (or whatever the dev port is — confirm in `npm run dev` output).
    2. **Scroll** to the bottom of the page past the TopArtists grid.
    3. **MetricsStrip wiring (Pitfall 2 fix)** — Confirm a thin horizontal row appears showing labelled stat blocks (Gems shown, Gems liked, Acceptance rate, Avg popularity, Hidden gem rate) plus a "Top genres" cluster of pill-shaped tags. If absent or the row is blank, the wiring failed — STOP and report.
    4. **Section heading** — Below MetricsStrip, confirm the heading "How your taste is evolving" in white, large, bold text.
    5. **LikeTrendChart visual** (UI-SPEC §1 + VALIDATION row 1) — Confirm the line chart in the left grid cell. Verify:
       - X-axis labels are dates formatted like "May 5" (month abbrev + day, no year).
       - Y-axis labels span 0–100.
       - The line itself is green (Spotify-green `#1DB954`), 2px stroke, no dot markers.
       - Gridlines are subtle gray. Hovering a data point shows a dark tooltip with the date and a percentage like "67%".
       - If you have fewer than 2 gem dates in your history, you should instead see "Not enough data yet — your like-rate trend will appear after a few days of gems."
    6. **TasteProfileChart visual** (UI-SPEC §2 + VALIDATION row 2) — Confirm the horizontal bar chart in the right grid cell. Verify:
       - Y-axis lists up to 10 genre names; each bar extends rightward.
       - Bars are filled with the same `#1DB954` green; right edge has small rounding.
       - X-axis is 0–100. Tooltip on hover shows the genre and percentage.
       - If your taste vector is empty, you should see "Your taste profile will appear once your listening history builds up." instead.
    7. **DiversityScore tile** — Below the two charts, on the left of the bottom row, confirm a small stat block. Label "Genre diversity" (gray, small, uppercase). Value is a green percentage like "47%" — OR an em dash "—" if your gem history doesn't have enough non-empty `Track.genres` (this is expected per 04-RESEARCH.md Pitfall 1; document the empty state if you see it).
    8. **ImprovementStory block** — To the right of DiversityScore (or wrapping below it on narrow screens), confirm two stat blocks side-by-side labelled "When I started" and "Now", each showing a percentage. To the right of "Now" should be a small delta badge: `▲ +Npp` in green for improvement, `▼ -Npp` in red for regression, or `— 0pp` in gray. If you have fewer than 2 historical gems, this block renders nothing — that is expected.
    9. **Color compliance** (VALIDATION row 3) — Open browser devtools, inspect the LineChart line element, confirm its `stroke` attribute is exactly `#1DB954`. Inspect a BarChart bar element, confirm `fill` is exactly `#1DB954`. Should match the `text-green` accent already used elsewhere on the page.
    10. **No console errors** — Open the browser console. There should be no React hydration warnings, no Recharts warnings, no 401/403/500 from the metrics API (200 expected). A single failed call to `/api/recommendation-metrics/` returning `{message: 'No gems yet'}` is acceptable for new users — MetricsStrip silently hides, chart components show empty-state copy.
    11. **Responsive check** — Resize browser to a narrow viewport (~390px). Confirm: the two-column chart grid collapses to a single column. The MetricsStrip wraps gracefully. No horizontal scroll appears.
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 11 verification steps pass. If any step fails, describe the specific failure (which component, what's wrong visually or in the console). Failures of items 1–8 are blocking; failures of items 9–11 are blocking unless documented with a clear remediation note.
  </resume-signal>
  <action>This task is a blocking human-verification checkpoint, not an autonomous code task. The executor opens the browser per the &lt;how-to-verify&gt; steps and either reports "approved" or surfaces concrete failures with the failing item number. No file edits are performed during this task; all implementation is already complete in Tasks 1–3 of this plan.</action>
  <verify>
    <automated>echo "human checkpoint — see how-to-verify steps 1–11; pass criterion is the user typing 'approved'"</automated>
  </verify>
  <done>User has typed "approved" after completing all 11 verification steps in &lt;how-to-verify&gt;. Any blocking failures (items 1–8) have been resolved by re-running prior tasks; items 9–11 either pass or have a documented remediation note from the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Django via session cookie | All 5 components hit authenticated endpoints; `IsAuthenticated` from plan 04-02 is the gate |
| Recharts SVG rendering → DOM | Recharts renders SVG without dangerouslySetInnerHTML; data flows in via React props — no XSS surface |
| User Spotify display name / genre strings → React JSX | Strings are rendered as React text children (auto-escaped) — no raw HTML injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Cross-Site Scripting | Genre name strings rendered in TasteProfileChart Y-axis, MetricsStrip top_genres tags | mitigate | React JSX auto-escapes string children. The genre names originate from Spotify's API (third-party) but are stored in `UserProfile.data['taste_vector']` as dict keys — rendered via `{genre}` JSX expression, never via `dangerouslySetInnerHTML`. ASVS L1 V5.3.3 satisfied. |
| T-04-13 | Cross-Site Request Forgery | GET requests for metrics | accept | All four new components issue GET only (no state mutation). CSRF tokens are not required for GETs. Existing MetricsStrip pattern (verified pre-existing) confirms this is the project's accepted norm for read-only client fetches. |
| T-04-14 | Information Disclosure | Recharts tooltips expose data on hover | accept | Tooltips show data already rendered on the chart axes. No additional sensitive data is leaked. The user is viewing their own data (authenticated session). |
| T-04-15 | Denial of Service | TasteProfileChart rendering with huge top_genres_pct | mitigate | Backend caps `top_genres_pct` to 10 items (Wave 1 plan acceptance criteria + D-07). Even if backend regresses, frontend renders whatever it receives — Recharts handles bounded list sizes well. Risk LOW. |
| T-04-16 | Tampering | Component imports vs file moves | mitigate | All five new component file paths are explicit relative imports. `npx tsc --noEmit` is the verify gate that catches broken imports. |
| T-04-17 | Information Disclosure | Hydration mismatch leaking server-side state | mitigate | All four new components are `"use client"` with `useState(null)` + `useEffect` fetch — no server-rendered state to mismatch. profile/page.tsx stays a server component but only renders the client component refs, not their inner state. (Pitfall 6 + Pitfall 7 mitigated.) |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` — exits 0; no TS errors anywhere.
2. `find frontend/app/profile/components -name "*.tsx" -newer frontend/app/profile/components/MetricsStrip/MetricsStrip.tsx` lists the four new files (LikeTrendChart, TasteProfileChart, ImprovementStory, DiversityScore).
3. `grep -c '"use client"' frontend/app/profile/components/LikeTrendChart/LikeTrendChart.tsx frontend/app/profile/components/TasteProfileChart/TasteProfileChart.tsx frontend/app/profile/components/ImprovementStory/ImprovementStory.tsx frontend/app/profile/components/DiversityScore/DiversityScore.tsx` — each shows ≥1.
4. profile/page.tsx contains the new section heading and all five JSX renders (grep gates in Task 3 acceptance).
5. Manual checkpoint (Task 4) is approved by the user.
</verification>

<success_criteria>
- Four new client components built and TypeScript-clean
- MetricsStrip wired into profile/page.tsx (closes Pitfall 2 — the unwired-component gap from research)
- New "How your taste is evolving" section renders below TopArtists with correct heading, grid, and stat row layout
- All locked colors (#1DB954, gridline gray, tooltip dark-card) and locked copy strings (UI-SPEC Copywriting Contract) are present verbatim
- profile/page.tsx remains a server component — no `"use client"`, no React hooks added
- Human verification (Task 4) confirms visual correctness on a real browser session
</success_criteria>

<output>
After completion, create `.planning/phases/04-metrics-evaluation-documentation/04-03-SUMMARY.md` capturing:
- Final file paths and line counts of the four new components
- The exact JSX block added to profile/page.tsx (paste or diff)
- Browser screenshots or notes from the Task 4 checkpoint
- Any deviations from 04-UI-SPEC.md and reason
</output>
