---
phase: 04-metrics-evaluation-documentation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - backend/tests/test_metrics.py
autonomous: true
requirements: []
tags: [setup, dependencies, test-scaffolding, recharts]

must_haves:
  truths:
    - "Running `npm ls recharts` inside `frontend/` reports `recharts@3.x` (no `(empty)`) (D-05)"
    - "`backend/tests/test_metrics.py` exists and is collected by pytest"
    - "`pytest backend/tests/test_metrics.py -x -q` exits non-zero with at least 6 failing/error stubs (RED state)"
    - "Test classes `TestMetricsEndpoint`, `TestTrendEndpoint`, `TestJaccard`, `TestTasteVector` all exist in test_metrics.py"
    - "Existing test suite (`pytest backend/tests/ -x -q --ignore=backend/tests/test_metrics.py`) remains green"
  artifacts:
    - path: frontend/package.json
      provides: recharts ^3.8.x dependency entry under `dependencies`
      contains: '"recharts"'
    - path: frontend/package-lock.json
      provides: locked recharts dependency tree
      contains: '"recharts"'
    - path: backend/tests/test_metrics.py
      provides: failing test stubs for Phase 4 metrics endpoints + Jaccard helper
      min_lines: 60
      contains: 'class TestMetricsEndpoint'
  key_links:
    - from: frontend/package.json
      to: node_modules/recharts
      via: npm install
      pattern: '"recharts": "\\^?3\\.'
    - from: backend/tests/test_metrics.py
      to: backend/conftest.py
      via: pytest django settings module already configured by conftest
      pattern: 'DJANGO_SETTINGS_MODULE'
---

<objective>
Set up Wave 0 prerequisites for Phase 4. Two independent prerequisites are bundled here so Wave 1 (backend endpoints) and Wave 2 (frontend charts) both unblock at once.

Purpose: (a) install `recharts@3.8.x` into `frontend/` so chart components in Wave 2 can import it, (b) create `backend/tests/test_metrics.py` with failing stubs for the four test classes mandated by 04-VALIDATION.md so Wave 1 has automated `<verify>` commands available from task one.

Output: `frontend/package.json` + lock with `recharts` entry; `backend/tests/test_metrics.py` with class skeletons that pytest collects and reports as RED (expected failures / NotImplementedError) — i.e. proper TDD seed state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-metrics-evaluation-documentation/04-CONTEXT.md
@.planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md
@.planning/phases/04-metrics-evaluation-documentation/04-VALIDATION.md

<interfaces>
<!-- Conftest-provided test environment (already wired). New test file does NOT need to repeat DJANGO_SETTINGS_MODULE setup. -->

From backend/tests/conftest.py:
- `DJANGO_SETTINGS_MODULE = 'config.settings'`
- `django.setup()` called at import time

From backend/apps/core/models.py (line refs from 04-RESEARCH.md):
- `DailyGem(user, date, track, was_liked, track_popularity)` — lines 281–298
- `RecommendationLog(user, track, source, liked, was_novel, track_popularity)` — lines 226–252
- `Track(id, name, artist, album, genres)` — `genres` is `JSONField(default=list)`, line 167
- `UserProfile(user, data)` — `data` is JSONField with `taste_vector`, `source_stats` keys

From frontend/package.json (existing dependencies the install must NOT disturb):
- `next`, `react`, `react-dom`, `axios`, `tailwindcss`, `lucide-react`, `shadcn-ui`

From 04-RESEARCH.md "Per Phase Requirements → Test Map":
Test classes required: `TestMetricsEndpoint`, `TestTrendEndpoint`, `TestJaccard`, `TestTasteVector`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install recharts 3.8.x into frontend</name>
  <files>frontend/package.json, frontend/package-lock.json</files>
  <read_first>
    - frontend/package.json (current dependency block, scripts, name field; do NOT overwrite existing deps)
    - .planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md (Environment Availability section; `recharts` version 3.8.1 verified on npm registry, line ~677)
    - .planning/phases/04-metrics-evaluation-documentation/04-UI-SPEC.md (Design System table; recharts is the locked chart library per D-05)
  </read_first>
  <action>
    Run `npm install recharts` from the `frontend/` directory (cwd MUST be `frontend/`, not repo root). This installs the latest 3.x version (~3.8.1 per 04-RESEARCH.md, verified on npm registry 2026-05-12). Implements D-05 (locked: recharts as the chart library).

    Do NOT use `--save-dev` — recharts is a runtime dependency consumed by client components (LikeTrendChart, TasteProfileChart in Wave 2). It must land under `dependencies`, not `devDependencies`.

    Do NOT pin to an exact version — caret-range is fine (npm default). Do NOT modify any other dependency. Do NOT run `npm audit fix`, `npm update`, or any other lifecycle command — only the single install.

    After install, verify:
      1. `frontend/package.json` `dependencies` block contains a `"recharts"` key.
      2. `frontend/package-lock.json` contains the recharts entry under `packages/node_modules/recharts`.
      3. `cd frontend && npm ls recharts` prints a single resolved version line, no `(empty)` and no UNMET PEER DEPENDENCY warnings that block the chart components (React 18 peer is satisfied by existing repo deps).

    If `npm install` fails with a peer-dep ERR (e.g. unsupported React version), STOP and surface the error — do NOT pass `--legacy-peer-deps` or `--force` without checkpoint approval. Recharts 3.x supports React 16.8+, so failure here indicates a deeper repo issue.
  </action>
  <verify>
    <automated>cd frontend && npm ls recharts 2>&1 | grep -E "recharts@3\." | grep -v "(empty)"</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` `dependencies` object contains a key matching the regex `^"recharts"`: literal pattern `grep -c '"recharts"' frontend/package.json` ≥ 1
    - `frontend/package-lock.json` contains the string `"node_modules/recharts"`: `grep -c '"node_modules/recharts"' frontend/package-lock.json` ≥ 1
    - `cd frontend && npm ls recharts` exit code = 0 AND stdout matches regex `recharts@3\.[0-9]+\.[0-9]+`
    - No other entry in `dependencies` was added or removed (compare against git diff: `git diff frontend/package.json | grep -c '^[-+].*":' | awk '{exit !($1 <= 2)}'` — at most one added line for recharts and possibly a trailing-comma adjustment)
  </acceptance_criteria>
  <done>recharts ^3.x is installed under `frontend/dependencies`, lockfile updated, no other deps touched, `npm ls recharts` returns a resolved version line.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold backend/tests/test_metrics.py with 4 failing test classes</name>
  <files>backend/tests/test_metrics.py</files>
  <read_first>
    - backend/tests/conftest.py (already configures `DJANGO_SETTINGS_MODULE = 'config.settings'` and calls `django.setup()`; new test file MUST NOT repeat that — verified in 04-PATTERNS.md "Test File Structure" section)
    - backend/tests/test_feedback_learning.py (Django TestCase + DB fixture pattern; class structure to mirror — first 80 lines)
    - backend/tests/test_recommendation_scoring.py (pure `unittest.TestCase` pattern for math helpers — first 80 lines)
    - .planning/phases/04-metrics-evaluation-documentation/04-RESEARCH.md (Phase Requirements → Test Map section — enumerates the 7 behaviors that must each have a stub)
    - .planning/phases/04-metrics-evaluation-documentation/04-VALIDATION.md (Per-Task Verification Map — confirms `pytest tests/test_metrics.py::TestJaccard -x -q` and `::TestTrendEndpoint` as Wave 1 verify commands)
  </read_first>
  <action>
    Create `backend/tests/test_metrics.py` containing four test classes that pytest can collect but that all FAIL (RED state — this is intentional TDD seed; Wave 1 turns them GREEN).

    Required classes and the minimum number of failing test methods per class (each method body should be `self.fail("Not implemented — Wave 1")` or `raise NotImplementedError("Wave 1")` so pytest reports a clear failure, NOT a passing test):

    - `TestMetricsEndpoint(django.test.TestCase)` — 3 stub methods covering: (a) `gem_acceptance_rate` is `None` when zero gems, (b) `hidden_gem_rate` is computed from `DailyGem.track_popularity < 40` (NOT `RecommendationLog.was_novel`, which is always True per 04-RESEARCH.md Pitfall 4), (c) response includes ALL nine MetricsStrip interface fields plus the three Phase 4 extension fields (`top_genres_pct`, `improvement_story`, `diversity_score`).
    - `TestTrendEndpoint(django.test.TestCase)` — 2 stub methods covering: (a) returns `{'data': [], 'message': 'Not enough data'}` when fewer than 2 distinct gem dates exist, (b) rolling 7-day window correctness for a known 10-day fixture.
    - `TestJaccard(unittest.TestCase)` — 3 stub methods covering: (a) `jaccard_distance([], []) == 0.0`, (b) `jaccard_distance(['rock'], ['jazz']) == 1.0` (fully disjoint per D-10), (c) `jaccard_distance(['rock', 'jazz'], ['rock', 'pop']) == 0.5` (one shared of three union — actual value: 1 - 1/3 ≈ 0.667; the stub records the expected math, not a precomputed wrong number).
    - `TestTasteVector(django.test.TestCase)` — 2 stub methods covering: (a) `top_genres_pct` length ≤ 10, (b) `sum(p['pct'] for p in top_genres_pct)` is approximately 100.0 (within float tolerance of 0.5).

    Imports at top of file:
      - `import unittest`
      - `from django.test import TestCase`
      - `from django.contrib.auth.models import User`
      - `from apps.core.models import DailyGem, RecommendationLog, Track, UserProfile`

    Do NOT import the views under test yet — they don't exist. Each stub method can declare the imports it would need in a comment, but the actual call site MUST be a `self.fail(...)` so the test fails on assertion, not on ImportError. Example body pattern:
    ```
        # Wave 1: from apps.core.views import get_recommendation_metrics
        self.fail("Wave 1: implement gem_acceptance_rate null case")
    ```
    (Code block shown here for clarity of intent only — do not place fenced blocks in plans; this is illustrative.)

    Do NOT call `django.setup()` or set `DJANGO_SETTINGS_MODULE` — `conftest.py` already handles it.

    File MUST be at least 60 lines (counting class/method skeletons) to encode the full test surface. Each test method must have a one-line docstring describing the behavior it will eventually verify (this becomes Wave 1's spec).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_metrics.py -q --collect-only 2>&1 | grep -E "TestMetricsEndpoint|TestTrendEndpoint|TestJaccard|TestTasteVector" | wc -l | awk '{exit !($1 >= 4)}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f backend/tests/test_metrics.py`
    - File contains all four required class declarations: `grep -cE '^class (TestMetricsEndpoint|TestTrendEndpoint|TestJaccard|TestTasteVector)\b' backend/tests/test_metrics.py` ≥ 4
    - File imports both `unittest` and `django.test.TestCase`: `grep -c "^import unittest" backend/tests/test_metrics.py` ≥ 1 AND `grep -c "from django.test import TestCase" backend/tests/test_metrics.py` ≥ 1
    - File has at least 10 test methods total (3+2+3+2 minimum = 10): `grep -cE '^\s+def test_' backend/tests/test_metrics.py` ≥ 10
    - File does NOT set `DJANGO_SETTINGS_MODULE` (conftest handles it): `grep -c "DJANGO_SETTINGS_MODULE" backend/tests/test_metrics.py` == 0
    - Running pytest on the file produces RED state (non-zero exit, but tests are COLLECTED): `cd backend && python -m pytest tests/test_metrics.py --tb=no -q 2>&1 | grep -cE "^FAILED |^ERROR "` ≥ 8 (each stub method should report as FAILED, not be skipped)
    - Existing test suite still green excluding the new red stubs: `cd backend && python -m pytest tests/ --ignore=tests/test_metrics.py -x -q` exits 0
    - File length: `wc -l backend/tests/test_metrics.py | awk '{print $1}'` ≥ 60
  </acceptance_criteria>
  <done>backend/tests/test_metrics.py exists with 4 collected classes and ≥10 failing test stubs, conftest still owns the Django setup, rest of the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → frontend/node_modules | npm package fetched over HTTPS from registry.npmjs.org; supply-chain trust |
| Developer machine → backend/tests/ | New test file is read-only data exercising the codebase; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | recharts npm package (supply chain) | accept | Use the official `recharts` package from npmjs.org (verified version 3.8.1 in 04-RESEARCH.md). No third-party fork. npm's default integrity check (sha512 in lockfile) is sufficient for a portfolio project. Do NOT add a `postinstall` hook from a non-vetted source. |
| T-04-02 | Tampering | package.json / lock | mitigate | Single-purpose install (`npm install recharts` only) so the diff is reviewable. Reject `--force` / `--legacy-peer-deps` without checkpoint, per task action constraint. |
| T-04-03 | Information Disclosure | backend/tests/test_metrics.py | accept | Stubs use throwaway `testuser` / `pass` credentials inside test DB; no production secrets referenced. Standard Django TestCase pattern, DB auto-rolled back per test. |
| T-04-04 | Denial of Service | npm install hang / oversized recharts dep | accept | recharts is ~50 KB minified and ~200 KB unpacked — well within Next.js budget. Risk LOW for a single-user portfolio app. No mitigation required. |
</threat_model>

<verification>
After both tasks complete:

1. `cd frontend && npm ls recharts` shows `recharts@3.x.y` (single resolved version).
2. `cd backend && python -m pytest tests/test_metrics.py --collect-only -q` lists all four test classes and ≥10 test methods.
3. `cd backend && python -m pytest tests/ --ignore=tests/test_metrics.py -x -q` is green — no existing test regressed.
4. `cd backend && python -m pytest tests/test_metrics.py -q` is RED with the expected ≥10 failures (Wave 1 turns these GREEN).
5. `git diff --stat frontend/package.json frontend/package-lock.json backend/tests/test_metrics.py` shows exactly three files modified/created.
</verification>

<success_criteria>
- recharts ^3.x landed under `frontend/dependencies` and lockfile updated
- backend/tests/test_metrics.py exists with 4 classes, ≥10 failing stubs, ≥60 lines
- Existing pytest suite remains green
- New test file produces a clean RED state ready for Wave 1 to turn GREEN
- No other frontend dep was modified, no `DJANGO_SETTINGS_MODULE` duplicated in the new test file
</success_criteria>

<output>
After completion, create `.planning/phases/04-metrics-evaluation-documentation/04-01-SUMMARY.md` capturing:
- recharts version actually installed (npm ls output)
- Test class skeleton structure created
- Confirmation that Wave 1 has unblocked automated `<verify>` commands
</output>
