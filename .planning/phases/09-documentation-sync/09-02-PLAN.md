---
phase: 09-documentation-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SYSTEM_DESIGN.md
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "SYSTEM_DESIGN.md DailyGem field table lists all 9 substantive fields including the four v1.1 additions"
    - "SYSTEM_DESIGN.md Data Flow section describes the _build_gem_explanation call between scoring and response"
    - "SYSTEM_DESIGN.md documents that add_track_to_liked sets was_saved=True on the matching DailyGem row"
    - "SYSTEM_DESIGN.md documents the score_breakdown dict structure in the GET /api/daily-gem/ response"
  artifacts:
    - path: "SYSTEM_DESIGN.md"
      provides: "Updated persistence table, data flow steps, API surface entry, component description for ScoreBreakdown"
      contains: "score_breakdown"
  key_links:
    - from: "SYSTEM_DESIGN.md Persistence Layer DailyGem row"
      to: "four v1.1 columns"
      via: "expanded Key fields cell"
      pattern: "was_saved.*taste_vector_snapshot"
    - from: "SYSTEM_DESIGN.md Data Flow step 9"
      to: "_build_gem_explanation"
      via: "new step inserted between scoring and response"
      pattern: "_build_gem_explanation"
---

<objective>
Update SYSTEM_DESIGN.md to close four documentation gaps introduced in v1.1:

G-03: Expand the DailyGem row in the Persistence Layer table to include the four new v1.1 model
fields (`score_breakdown`, `score_total`, `was_saved`, `taste_vector_snapshot`) plus flag
`was_skipped` as present-but-not-yet-wired.

G-04: Insert a new step in "Data Flow: Daily Gem Request" describing the `_build_gem_explanation`
call, and add a ScoreBreakdown entry to Component Descriptions.

G-05: Extend the `POST /api/add-track-to-liked/` API Surface row to document the `was_saved=True`
side-effect on the DailyGem row.

G-06: Document the `score_breakdown` dict in the `GET /api/daily-gem/` Data Flow response
description, add `ScoreBreakdown` to the Architecture Diagram Frontend subgraph, and add the
ScoreBreakdown component entry to Component Descriptions.

Purpose: SYSTEM_DESIGN.md currently describes the pre-v1.1 DailyGem model and data flow.
An interviewer inspecting the persistence table sees only 5 fields; the actual model has 9.
The `score_breakdown` response field and the deterministic explanation helper are invisible.

Output: SYSTEM_DESIGN.md with four targeted edits; all other content preserved unchanged.
</objective>

<execution_context>
@/Users/antonilueddeke/Desktop/Projects/songscope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonilueddeke/Desktop/Projects/songscope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/antonilueddeke/Desktop/Projects/songscope/.planning/ROADMAP.md
@/Users/antonilueddeke/Desktop/Projects/songscope/.planning/phases/09-documentation-sync/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand DailyGem persistence table (G-03) and update Architecture Diagram (G-06 partial)</name>
  <files>SYSTEM_DESIGN.md</files>
  <action>
Two edits in SYSTEM_DESIGN.md:

**Edit A — Persistence Layer table (G-03).**

Locate the table under "### Persistence Layer" (the table has a "Model / Key fields / Purpose" header). Find this exact row:

```
| `DailyGem` | `user`, `track`, `date`, `was_liked`, `track_popularity` | One gem per user per day; source of truth for metrics |
```

Replace that entire row with:

```
| `DailyGem` | `user`, `track`, `date`, `was_liked`, `track_popularity`, `score_breakdown` (JSONField, default={}), `score_total` (FloatField, nullable), `was_saved` (BooleanField, nullable), `taste_vector_snapshot` (JSONField, nullable), `was_skipped` (BooleanField, default=False — not yet wired) | One gem per user per day; source of truth for metrics. `score_breakdown` stores the three scoring components at gem creation time. `score_total` is the composite score. `was_saved` is set to True when the user saves the track to Spotify (independent of `was_liked`). `taste_vector_snapshot` captures the user's taste vector at recommendation time for offline evaluation — enables comparing what the model "knew" then against eventual feedback without reconstructing historical state. `was_skipped` exists in the model but is not yet wired to any view (reserved for a future skip-signal feedback loop). |
```

After editing, the DailyGem row spans the full expanded content. The other table rows (`User`, `UserProfile`, `RecommendationLog`, `Track`) are unchanged.

**Edit B — Architecture Diagram Frontend subgraph (G-06 partial).**

Locate the mermaid `flowchart LR` block. Find the `subgraph Frontend` section. Currently it ends with:

```
        ImprovementStory[ImprovementStory]
    end
```

Change that block so `ScoreBreakdown` is listed as the last item in the Frontend subgraph and connected to `DailyGemUI`:

```
        ImprovementStory[ImprovementStory]
        ScoreBreakdown[ScoreBreakdown]
    end
```

Then, in the arrow section after the subgraph definitions, find the line:

```
    DailyGemUI -->|GET| GemView
```

Add this line immediately after it:

```
    DailyGemUI --> ScoreBreakdown
```

Do not alter any other lines in the mermaid block.
  </action>
  <verify>
    <automated>grep -n "taste_vector_snapshot\|was_skipped\|ScoreBreakdown" /Users/antonilueddeke/Desktop/Projects/songscope/SYSTEM_DESIGN.md | head -20</automated>
  </verify>
  <done>
SYSTEM_DESIGN.md contains "taste_vector_snapshot", "was_skipped", and "ScoreBreakdown" after this task.
The DailyGem row in the Persistence Layer table lists all 9 fields.
The Architecture Diagram Frontend subgraph contains `ScoreBreakdown[ScoreBreakdown]`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Data Flow steps, Component Descriptions, and API Surface (G-04, G-05, G-06)</name>
  <files>SYSTEM_DESIGN.md</files>
  <action>
Three edits in SYSTEM_DESIGN.md:

**Edit A — Data Flow: insert _build_gem_explanation step (G-04).**

Locate "## Data Flow: Daily Gem Request" and find step 8 and step 9 in their current form:

```
8. `_score_recommendations()` applies `0.4 * genre_sim + 0.3 * novelty + 0.3 * feedback_multiplier` then multiplies by the Thompson-sampled source weight.
9. Top-ranked candidate is saved as a `DailyGem` row and a `RecommendationLog` row (for future metric computation). Response returned to the frontend.
```

Replace those two lines with the following three lines (step 8 unchanged, new step 9 inserted, old step 9 renumbered to 10, and steps 10-13 renumbered to 11-14):

```
8. `_score_recommendations()` applies `0.4 * genre_sim + 0.3 * novelty + 0.3 * feedback_multiplier` then multiplies by the Thompson-sampled source weight.
9. The top-ranked candidate's `score_breakdown` dict (`{genre_sim, novelty, feedback_multiplier, source}`) is passed to `_build_gem_explanation(breakdown, track_name, artist_name, source)`. This pure helper function inspects the three numeric components, identifies the dominant one (`argmax`), and returns a fixed sentence from one of four templates. No external call is made; the same breakdown always produces the same sentence. The result is stored as `DailyGem.explanation`.
10. Top-ranked candidate is saved as a `DailyGem` row (with `score_breakdown`, `score_total`, `explanation`, and `taste_vector_snapshot` populated) and a `RecommendationLog` row. The response to the frontend includes the `score_breakdown` dict so the `ScoreBreakdown` component can render the three bar values without a separate API call.
11. User sees the gem card and clicks thumbs-up or thumbs-down.
12. `DailyGemUI` posts `POST /api/submit-feedback/` with `{track_id, feedback_type}`.
13. `submit_feedback` calls `personalization_engine.apply_feedback_learning(feedback)`, which updates `UserProfile.data['taste_vector']` and `UserProfile.data['source_stats']` in a single DB write.
14. `MetricsStrip`, `LikeTrendChart`, `TasteProfileChart`, `DiversityScore`, and `ImprovementStory` silently re-fetch their data on the user's next page load.
```

After this edit the Data Flow has 14 steps (was 13). Steps 1-8 are identical to the original. Steps 11-14 are the original steps 10-13 with only the number changed — do not alter their prose.

**Edit B — API Surface table: add was_saved side-effect (G-05).**

Locate the API Surface table. Find this exact row:

```
| POST | `/api/add-track-to-liked/` | `add_track_to_liked` | Session | Save track to Spotify liked songs | 4 |
```

Replace the Purpose cell only (leave all other cells unchanged):

```
| POST | `/api/add-track-to-liked/` | `add_track_to_liked` | Session | Save track to Spotify liked songs via `sp.current_user_saved_tracks_add()`. Side-effect: calls `DailyGem.objects.filter(user, date=today, track__spotify_id=track_id).update(was_saved=True)` so that `was_saved` can become `True` independently of `was_liked`. This enables OR-semantics in `compound_hit_rate`. | 4 |
```

**Edit C — Component Descriptions: add ScoreBreakdown entry (G-06).**

Locate the "### DiversityScore" component description block (ending with the "Known limitation" bullet). After the entire DiversityScore block (including its "Key invariants" bullet list) and before "### Recommendation Engine (hybrid)", insert the following new component description block:

```

### ScoreBreakdown

Client component. Reads the `score_breakdown` field from the `GET /api/daily-gem/` response (passed as a prop from `DailyGemUI`) and renders three horizontal progress bars: Genre Match (`genre_sim`), Novelty (`novelty`), and Feedback (`feedback_multiplier`). Bar width is `Math.round(raw * 100 / 5) * 5` — rounded to the nearest 5% for visual cleanliness.

Source: `frontend/app/profile/components/DailyGem/ScoreBreakdown.tsx`

Key invariants:
- `score_breakdown` is returned in the `GET /api/daily-gem/` response for both cached and fresh gems. For cached gems it is read from the persisted `DailyGem.score_breakdown` column; for fresh gems it comes from `_score_recommendations()` and is simultaneously written to the DB.
- The component receives `score_breakdown` as a prop — it does not make an additional API call.
- Each raw float (0–1 range) is multiplied by 100 then rounded to the nearest 5 before rendering as a percentage width.

```
  </action>
  <verify>
    <automated>grep -n "_build_gem_explanation\|was_saved=True\|ScoreBreakdown\|score_breakdown" /Users/antonilueddeke/Desktop/Projects/songscope/SYSTEM_DESIGN.md | head -30</automated>
  </verify>
  <done>
SYSTEM_DESIGN.md contains:
- "_build_gem_explanation" in the Data Flow section (new step 9).
- "was_saved=True" in the API Surface table add-track-to-liked row.
- "ScoreBreakdown" in both the mermaid diagram (from Task 1) and the Component Descriptions section.
- "score_breakdown" in the Data Flow step 10 response description.
Data Flow has 14 steps (grep "^[0-9]\+\." returns 14 matches in the Data Flow section).
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit SYSTEM_DESIGN.md changes</name>
  <files>SYSTEM_DESIGN.md</files>
  <action>
Stage and commit only SYSTEM_DESIGN.md. The commit covers G-03, G-04, G-05, and G-06.

Run from the repo root (/Users/antonilueddeke/Desktop/Projects/songscope):

  git add SYSTEM_DESIGN.md
  git commit -m "docs(09-02): update SYSTEM_DESIGN.md for v1.1 changes

G-03: DailyGem persistence table expanded to 9 fields (score_breakdown, score_total,
was_saved, taste_vector_snapshot, was_skipped).
G-04: Data Flow now has 14 steps; new step 9 documents _build_gem_explanation call.
ScoreBreakdown added to Component Descriptions.
G-05: add_track_to_liked API row documents was_saved=True side-effect and OR-semantics link.
G-06: score_breakdown dict documented in Data Flow response. ScoreBreakdown added to
Architecture Diagram Frontend subgraph.

No code changes — documentation sync only."

Do not stage or commit any other file.
  </action>
  <verify>
    <automated>git -C /Users/antonilueddeke/Desktop/Projects/songscope log --oneline -3</automated>
  </verify>
  <done>
`git log` shows the new commit at HEAD. Commit message contains "09-02". SYSTEM_DESIGN.md is the only file in the commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → file system | Document content written directly to SYSTEM_DESIGN.md; no user input, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Tampering | SYSTEM_DESIGN.md | accept | Doc-only phase; no runtime attack surface. File is committed to version control so accidental overwrites are recoverable via git. |
</threat_model>

<verification>
After all tasks complete:

1. Persistence Layer DailyGem row contains all 9 fields — verify with:
   grep -c "taste_vector_snapshot" SYSTEM_DESIGN.md  (must be >= 1)
   grep -c "was_skipped" SYSTEM_DESIGN.md  (must be >= 1)

2. Data Flow has 14 numbered steps — count lines matching "^[0-9]+\." in the Data Flow block.

3. Step 9 contains "_build_gem_explanation" — verify with:
   grep -n "_build_gem_explanation" SYSTEM_DESIGN.md  (must appear in Data Flow section)

4. API Surface add-track-to-liked row contains "was_saved=True" — verify with:
   grep -n "was_saved=True" SYSTEM_DESIGN.md  (must appear in API Surface table)

5. Component Descriptions contains a ScoreBreakdown entry — verify with:
   grep -n "### ScoreBreakdown" SYSTEM_DESIGN.md  (must return a result)

6. Architecture Diagram contains "ScoreBreakdown[ScoreBreakdown]" — verify with:
   grep -n "ScoreBreakdown\[ScoreBreakdown\]" SYSTEM_DESIGN.md  (must return a result)

7. All existing sections remain intact:
   grep "## Architecture Diagram" SYSTEM_DESIGN.md → found
   grep "## Component Descriptions" SYSTEM_DESIGN.md → found
   grep "## API Surface" SYSTEM_DESIGN.md → found
   grep "## Data Flow: Daily Gem Request" SYSTEM_DESIGN.md → found
   grep "## Operational Constraints" SYSTEM_DESIGN.md → found

8. git log shows exactly one new commit with only SYSTEM_DESIGN.md changed.
</verification>

<success_criteria>
- DailyGem Persistence Layer row lists all 9 substantive fields including score_breakdown, score_total, was_saved, taste_vector_snapshot; was_skipped noted as not yet wired.
- taste_vector_snapshot purpose (offline evaluation) is explained in the field table.
- Data Flow step 9 describes _build_gem_explanation: pure function, argmax of components, fixed templates, no external call.
- Data Flow step 10 documents that the response includes score_breakdown and that it is persisted at gem creation (not re-derived on read).
- API Surface add-track-to-liked row documents was_saved=True DB side-effect and the link to compound_hit_rate OR-semantics.
- ScoreBreakdown component described in Component Descriptions with three-bar layout and nearest-5% rounding rule.
- ScoreBreakdown present in Architecture Diagram Frontend subgraph.
- No existing SYSTEM_DESIGN.md content altered.
- Changes committed.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-sync/09-02-SUMMARY.md` using the summary template.
</output>
