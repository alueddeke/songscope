---
phase: 09-documentation-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CONCEPTS.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "CONCEPTS.md contains a compound_hit_rate definition with the OR-semantics formula and None-identity rationale"
    - "CONCEPTS.md contains a section explaining that gem explanations are produced by a deterministic pure function with no external API calls"
  artifacts:
    - path: "CONCEPTS.md"
      provides: "Updated evaluation metrics section and new deterministic-explanation section"
      contains: "compound_hit_rate"
  key_links:
    - from: "CONCEPTS.md Recommendation Evaluation Metrics"
      to: "compound_hit_rate definition"
      via: "appended paragraph after hidden_gem_rate code block"
      pattern: "compound_hit_rate"
    - from: "CONCEPTS.md Table of Contents"
      to: "new Gem Explanation section"
      via: "new TOC entry linking to anchor"
      pattern: "Gem Explanation"
---

<objective>
Update CONCEPTS.md to close two documentation gaps introduced in v1.1:

G-01: Define `compound_hit_rate` (with OR-semantics and None-identity rationale) inside
"Recommendation Evaluation Metrics."

G-02: Add a "Gem Explanation (Template-Based, Deterministic)" section explaining that
`_build_gem_explanation` is a pure function — no LLM, no network call, fixed sentence templates.

Purpose: Keep CONCEPTS.md interview-ready. An interviewer reading the current file can reasonably
assume the explanation field is AI-generated (the system does use OpenAI for AIFeedback) and has no
source for the compound_hit_rate metric shown in the frontend.

Output: CONCEPTS.md with two additions; all existing content is preserved unchanged.
</objective>

<execution_context>
@/Users/antonilueddeke/Desktop/Projects/songscope/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonilueddeke/Desktop/Projects/songscope/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/antonilueddeke/Desktop/Projects/songscope/.planning/ROADMAP.md
@/Users/antonilueddeke/Desktop/Projects/songscope/.planning/phases/09-documentation-sync/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert compound_hit_rate paragraph (G-01)</name>
  <files>CONCEPTS.md</files>
  <action>
Open CONCEPTS.md. Locate the "### Code (in this codebase)" subsection of "## Recommendation Evaluation Metrics" (currently lines 316-325). Find the closing code block that ends with:

```
improvement_story    → first_7_rate vs last_7_rate (learning signal, not an eval metric per se)
```

That block is followed by a blank line and then "### Interview Talking Point". Insert the following new paragraph BETWEEN that closing ``` and the "### Interview Talking Point" heading (i.e., after the closing fence but before the heading):

---
### Compound Hit Rate

`compound_hit_rate` is a v1.1 addition to the metrics endpoint. It is a broader version of `gem_acceptance_rate` that counts a gem as a "hit" if the user liked it OR saved it to their Spotify library — whichever came first.

**Formula:**

```
compound_hit_rate = compound_hits / gem_total

where compound_hits = count of gems where (was_liked IS True OR was_saved IS True)
```

**Why OR semantics:** A user who saves a gem to Spotify but never taps the in-app thumbs-up has signalled strong value. Restricting the numerator to `was_liked IS True` would under-count positive engagement for users who prefer the Spotify save gesture. Using OR captures both pathways.

**Why `IS True` and not a truthiness check:** Python's `is True` identity check means `None is True` evaluates to `False`. A gem with no feedback recorded yet (`was_liked = None`, `was_saved = None`) is treated as a miss, not a hit. This prevents NULL values from inflating the metric.

Source: `backend/apps/core/views.py`, lines 425-428.

---

After inserting, verify the section order is: existing code block → new "### Compound Hit Rate" subsection → existing "### Interview Talking Point".

Do NOT modify the existing "### Interview Talking Point" content or the "### Formula" / "### Code" blocks above it.
  </action>
  <verify>
    <automated>grep -n "compound_hit_rate" /Users/antonilueddeke/Desktop/Projects/songscope/CONCEPTS.md | head -20</automated>
  </verify>
  <done>
CONCEPTS.md contains "compound_hit_rate" in the "Recommendation Evaluation Metrics" section.
The OR-semantics rationale ("saves a gem") and None-identity rationale ("IS True") are both present.
Existing section content is intact above and below the insertion.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Gem Explanation section (G-02) and update TOC</name>
  <files>CONCEPTS.md</files>
  <action>
Two edits in CONCEPTS.md:

**Edit A — New section.**

Locate "## Spotify API Deprecation Pivot" (currently appears after the "## Compound Success Metric" section). Insert the following complete new section immediately BEFORE "## Spotify API Deprecation Pivot" (i.e., between the end of the Compound Success Metric interview talking point and the Spotify API section heading):

---

## Gem Explanation (Template-Based, Deterministic)

### Intuition

Each `DailyGem` row has an `explanation` field — a one-sentence human-readable description of why a particular track was chosen. An interviewer reading the codebase might assume this is generated by an LLM (the system does call OpenAI for the `AIFeedback` feature). It is not.

The explanation is produced by `_build_gem_explanation()`, a pure Python function in `backend/apps/core/views.py` (lines 1037–1092). It takes the three score breakdown components (`genre_sim`, `novelty`, `feedback_multiplier`) and the track metadata, finds the dominant scoring component, and fills one of four fixed sentence templates. There is no external call, no randomness, and no AI: the same breakdown always produces the same sentence.

### How It Works

```
dominant = argmax({genre_sim, novelty, feedback_multiplier})

if dominant == genre_sim:
    → "We think you'll love {track} by {artist} — it matches your taste in {source} tracks."
if dominant == novelty:
    → "{track} by {artist} is a hidden gem — popular enough to be good, underground enough to feel like a find."
if dominant == feedback_multiplier:
    → "You've liked {artist} before, so {track} seemed like a natural next pick."
(fallback):
    → "{track} by {artist} scored well across all three dimensions — genre fit, novelty, and your feedback history."
```

The three sentence shapes are constants; there is no model inference step.

### Why Deterministic by Design

Determinism has two benefits for a portfolio project:

1. **Debuggability.** Given a `score_breakdown` dict and track metadata, you can reproduce the explanation from the DB row without re-running any model.
2. **No external dependency at render time.** If OpenAI is unreachable, gems still get explanations. The `AIFeedback` feature (which does call OpenAI) is a separate code path triggered only when the user explicitly requests AI feedback.

### Code (in this codebase)

```python
# Source: backend/apps/core/views.py, lines 1037-1092
def _build_gem_explanation(breakdown, track_name, artist_name, source) -> str:
    """
    Pure function: no external calls, no logging, no exceptions on any reasonable input.
    Returns one of four sentence shapes based on the dominant scoring component.
    """
    components = {
        'genre_sim': breakdown.get('genre_sim', 0),
        'novelty': breakdown.get('novelty', 0),
        'feedback_multiplier': breakdown.get('feedback_multiplier', 0),
    }
    dominant = max(components, key=components.get)
    # Three fixed sentence templates — no LLM
    ...
```

### Interview Talking Point

"The explanation field looks like it could be AI-generated — it reads as natural language. But it is a pure function: three `if` branches, four sentence templates, deterministic output. I made this choice deliberately. An LLM call at gem-retrieval time would add latency, a billing dependency, and a failure mode. The template approach is instant, free, and reproducible. The honest disclosure is that the sentences are formulaic — a future version could use a fine-tuned model if richer explanations mattered. For a portfolio system the deterministic version is strictly better."

---

**Edit B — Table of Contents.**

Locate the Table of Contents block (lines 5-16 in the original file). The current list ends with:

```
8. [Spotify API Deprecation Pivot](#spotify-api-deprecation-pivot)
9. [Further Reading](#further-reading)
```

Change those two lines to:

```
8. [Gem Explanation (Template-Based, Deterministic)](#gem-explanation-template-based-deterministic)
9. [Spotify API Deprecation Pivot](#spotify-api-deprecation-pivot)
10. [Further Reading](#further-reading)
```

(The anchor `#gem-explanation-template-based-deterministic` is the standard GitHub Markdown anchor for the heading "Gem Explanation (Template-Based, Deterministic)".)

Perform Edit A first, then Edit B. After both edits the TOC entry count is 10, the new section appears between Compound Success Metric and Spotify API Deprecation Pivot, and the existing section numbering/content is unchanged.
  </action>
  <verify>
    <automated>grep -n "Gem Explanation\|_build_gem_explanation\|pure function\|no external" /Users/antonilueddeke/Desktop/Projects/songscope/CONCEPTS.md | head -20</automated>
  </verify>
  <done>
CONCEPTS.md contains the "## Gem Explanation (Template-Based, Deterministic)" section between
"## Compound Success Metric" and "## Spotify API Deprecation Pivot".
The TOC lists 10 entries with the new section at position 8.
The phrases "pure function", "no external calls", and "fixed sentence templates" (or equivalent) appear in the new section.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit CONCEPTS.md changes</name>
  <files>CONCEPTS.md</files>
  <action>
Stage and commit only CONCEPTS.md. The commit covers both G-01 and G-02 additions.

Run from the repo root (/Users/antonilueddeke/Desktop/Projects/songscope):

  git add CONCEPTS.md
  git commit -m "docs(09-01): add compound_hit_rate definition and deterministic explanation section

G-01: compound_hit_rate paragraph with OR-semantics and None-identity rationale added to
Recommendation Evaluation Metrics section.
G-02: Gem Explanation (Template-Based, Deterministic) section added; TOC updated to 10 entries.

No code changes — documentation sync only."

Do not stage or commit any other file.
  </action>
  <verify>
    <automated>git -C /Users/antonilueddeke/Desktop/Projects/songscope log --oneline -3</automated>
  </verify>
  <done>
`git log` shows the new commit at HEAD. Commit message contains "09-01". CONCEPTS.md is the only file in the commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Editor → file system | Document content written directly to CONCEPTS.md; no user input, no external data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | CONCEPTS.md | accept | Doc-only phase; no runtime attack surface. File is committed to version control so accidental overwrites are recoverable via git. |
</threat_model>

<verification>
After both tasks complete:

1. grep confirms "compound_hit_rate" appears in CONCEPTS.md inside the Recommendation Evaluation Metrics section.
2. grep confirms "_build_gem_explanation" and "pure function" appear in CONCEPTS.md in the new Gem Explanation section.
3. TOC has 10 numbered entries (count lines matching `^\d+\.` in the TOC block).
4. All existing sections remain present and unmodified:
   - grep "## Cosine Similarity" CONCEPTS.md → found
   - grep "## Novelty Scoring" CONCEPTS.md → found
   - grep "## Thompson Sampling" CONCEPTS.md → found
   - grep "## Compound Success Metric" CONCEPTS.md → found
   - grep "## Spotify API Deprecation Pivot" CONCEPTS.md → found
5. git log shows exactly one new commit with only CONCEPTS.md changed.
</verification>

<success_criteria>
- CONCEPTS.md contains `compound_hit_rate = (was_liked IS True OR was_saved IS True) / gem_total` with OR-semantics rationale and None-identity explanation.
- CONCEPTS.md contains a standalone section explaining `_build_gem_explanation` is a pure function with no external calls and fixed sentence templates.
- TOC updated to include the new section at position 8.
- No existing CONCEPTS.md content altered.
- Changes committed.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-sync/09-01-SUMMARY.md` using the summary template.
</output>
