---
phase: 06-schema-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/apps/core/models.py
  - backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py
autonomous: true
requirements:
  - SCHEMA-01
  - METRIC-01
tags:
  - django
  - migration
  - schema

must_haves:
  truths:
    - "DailyGem model declares four new fields: score_breakdown, score_total, was_saved, taste_vector_snapshot"
    - "A new migration file 0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py exists in backend/apps/core/migrations/"
    - "Running python manage.py migrate from backend/ applies the new migration without error"
    - "Running python manage.py makemigrations --check from backend/ exits 0 (no pending model changes)"
    - "score_breakdown declaration uses default=dict (callable), NOT default={} (mutable literal)"
  artifacts:
    - path: "backend/apps/core/models.py"
      provides: "DailyGem class with 4 new fields declared after was_liked (line 289)"
      contains: "score_breakdown = models.JSONField(default=dict"
    - path: "backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py"
      provides: "Auto-generated Django migration adding 4 fields via migrations.AddField"
      contains: "0007_spotifytoken_refresh_token_nullable"
  key_links:
    - from: "backend/apps/core/migrations/0008_*.py"
      to: "backend/apps/core/migrations/0007_spotifytoken_refresh_token_nullable.py"
      via: "dependencies list in Migration class"
      pattern: "\\(\"core\", \"0007_spotifytoken_refresh_token_nullable\"\\)"
    - from: "backend/apps/core/migrations/0008_*.py"
      to: "backend/apps/core/models.py DailyGem"
      via: "migrations.AddField(model_name='dailygem', ...) operations"
      pattern: "model_name=\"dailygem\""
---

<objective>
Add four new fields to the DailyGem model and generate a single Django migration to apply them to the database. This is the schema foundation for v1.1 — Phase 7 wires score persistence and the was_saved compound metric on top of this schema.

Purpose: Establish the columns required for score breakdown persistence (SCHEMA-01) and compound success tracking (METRIC-01). Zero business logic changes — pure schema phase per CONTEXT.md domain boundary.
Output: Modified DailyGem model + auto-generated migration file 0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-schema-migration/06-CONTEXT.md
@.planning/phases/06-schema-migration/06-PATTERNS.md

<interfaces>
<!-- Existing DailyGem field block (backend/apps/core/models.py line 280-291) -->
<!-- New fields insert AFTER was_liked (line 289), BEFORE was_skipped (line 290) -->

Current DailyGem field block:
- user (ForeignKey User)
- track (ForeignKey Track)
- date (DateField, default=timezone.localdate)
- explanation (TextField, blank=True)
- image_url (URLField, max_length=500, blank=True)
- preview_url (URLField, max_length=500, blank=True)
- track_popularity (IntegerField, default=0)
- was_liked (BooleanField, null=True, blank=True)         <- existing nullable Boolean template
- was_skipped (BooleanField, default=False)
- created_at (DateTimeField, auto_now_add=True)

JSONField default=dict pattern reference (UserProfile.data, line 38):
  data = models.JSONField(default=dict, blank=True)

Latest migration head (dependency target):
  ("core", "0007_spotifytoken_refresh_token_nullable")
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add four fields to DailyGem model</name>
  <files>backend/apps/core/models.py</files>
  <read_first>
    - backend/apps/core/models.py (full file — confirm DailyGem class boundaries at line 280-291 and UserProfile.data JSONField pattern at line 38)
    - .planning/phases/06-schema-migration/06-PATTERNS.md (field declarations and placement rule)
    - .planning/phases/06-schema-migration/06-CONTEXT.md §decisions (D-02, D-04 locked rules)
  </read_first>
  <action>
    Edit the DailyGem class in backend/apps/core/models.py. Insert four new field declarations between the existing was_liked field (currently line 289) and the existing was_skipped field (currently line 290), in this exact order:

    1. score_breakdown: models.JSONField, default=dict (callable — D-04 forbids default={}), blank=True
    2. score_total: models.FloatField, null=True, blank=True
    3. was_saved: models.BooleanField, null=True, blank=True  (mirrors was_liked exactly per CONTEXT canonical_refs)
    4. taste_vector_snapshot: models.JSONField, null=True, blank=True

    Do NOT modify any other DailyGem fields. Do NOT modify Meta, __str__, or other model classes. Do NOT add methods, properties, signals, or save() overrides — those are Phase 7 concerns per D-05.

    Placement rule from 06-PATTERNS.md: keep the nullable Boolean fields grouped together. The final DailyGem field order after this edit must be: user, track, date, explanation, image_url, preview_url, track_popularity, was_liked, score_breakdown, score_total, was_saved, taste_vector_snapshot, was_skipped, created_at.

    Implements SCHEMA-01 (score_breakdown, score_total, taste_vector_snapshot) and METRIC-01 (was_saved) per locked decisions D-02, D-03, D-04, D-05.
  </action>
  <verify>
    <automated>cd backend && python -c "from apps.core.models import DailyGem; fields = {f.name for f in DailyGem._meta.get_fields()}; assert {'score_breakdown', 'score_total', 'was_saved', 'taste_vector_snapshot'}.issubset(fields), f'missing fields: {fields}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: grep -E "score_breakdown\s*=\s*models\.JSONField\(default=dict" backend/apps/core/models.py returns exactly one match
    - Source assertion: grep -E "score_total\s*=\s*models\.FloatField\(.*null=True.*blank=True\)" backend/apps/core/models.py returns exactly one match
    - Source assertion: grep -E "was_saved\s*=\s*models\.BooleanField\(.*null=True.*blank=True\)" backend/apps/core/models.py returns exactly one match
    - Source assertion: grep -E "taste_vector_snapshot\s*=\s*models\.JSONField\(.*null=True.*blank=True\)" backend/apps/core/models.py returns exactly one match
    - Source assertion: grep -E "default=\{\s*\}" backend/apps/core/models.py | grep -v "^#" returns zero matches (D-04 — no mutable dict literal defaults)
    - Behavior assertion: `cd backend && python -c "from apps.core.models import DailyGem; [print(f.name) for f in DailyGem._meta.get_fields()]"` lists score_breakdown, score_total, was_saved, taste_vector_snapshot
    - CLI: `cd backend && python manage.py check apps.core` exits 0 (no model validation errors)
  </acceptance_criteria>
  <done>DailyGem._meta.get_fields() includes score_breakdown, score_total, was_saved, taste_vector_snapshot; `python manage.py check` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Auto-generate migration 0008 via makemigrations</name>
  <files>backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py</files>
  <read_first>
    - backend/apps/core/migrations/0007_spotifytoken_refresh_token_nullable.py (confirm latest head and dependency chain shape)
    - backend/apps/core/migrations/0005_dailygem_image_url_dailygem_preview_url.py (analog AddField on DailyGem)
    - .planning/phases/06-schema-migration/06-PATTERNS.md (expected output shape of the new migration)
    - .planning/phases/06-schema-migration/06-CONTEXT.md §decisions (D-02 — one migration only)
  </read_first>
  <action>
    From the backend/ directory, run `python manage.py makemigrations core` to auto-generate the migration. Django will detect the four AddField operations against the existing DailyGem model and produce the migration file. Do NOT hand-write the migration body (D-02 preference for auto-generation).

    Expected output filename: 0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py (Django concatenates field names alphabetically; if the generated filename differs slightly in field order, RENAME the file to match the canonical name in this plan's files_modified field so downstream phases reference a deterministic name).

    Verify the generated file:
    - dependencies = [("core", "0007_spotifytoken_refresh_token_nullable")]
    - Contains exactly four migrations.AddField operations, one each for: score_breakdown (JSONField, blank=True, default=dict), score_total (FloatField, blank=True, null=True), was_saved (BooleanField, blank=True, null=True), taste_vector_snapshot (JSONField, blank=True, null=True)
    - All operations use model_name="dailygem"
    - No AlterField, RemoveField, RunPython, or RunSQL operations (zero data migration per D-03)

    If makemigrations produces additional operations on unrelated models (it should not, since Task 1 only modified DailyGem), STOP and report — do NOT commit unintended schema changes.

    Implements D-02 (one migration only) and D-03 (no data migration).
  </action>
  <verify>
    <automated>cd backend && python manage.py migrate --check 2>&1 | grep -E "(0008_dailygem_score_breakdown|No planned migration operations)" || (python manage.py migrate apps.core && python manage.py migrate --check)</automated>
  </verify>
  <acceptance_criteria>
    - CLI: `ls backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py` exits 0 (file exists at canonical path)
    - Source assertion: grep -c "migrations.AddField" backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py returns 4
    - Source assertion: grep -c "model_name=\"dailygem\"" backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py returns 4
    - Source assertion: grep -E '\("core", "0007_spotifytoken_refresh_token_nullable"\)' backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py returns exactly one match
    - Source assertion: grep -E "(RunPython|RunSQL|AlterField|RemoveField)" backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py | grep -v "^#" returns zero matches
    - Source assertion: grep -E "JSONField\(blank=True, default=dict\)" backend/apps/core/migrations/0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot.py returns exactly one match (score_breakdown)
    - Behavior assertion: `cd backend && python manage.py makemigrations --check --dry-run` exits 0 (no further pending model changes after this migration)
    - Behavior assertion: `cd backend && python manage.py migrate` applies the migration without error and includes "Applying core.0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot... OK" in stdout
  </acceptance_criteria>
  <done>Migration file exists at canonical path, applies cleanly via `python manage.py migrate`, and `makemigrations --check` exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→git→production | Migration file is committed and will run against production SQLite DB on next deploy. A malformed migration corrupts the schema or destroys data. |
| Django ORM→SQLite | Schema additions translate to ALTER TABLE statements. Nullable columns with defaults are safe; non-nullable additions without defaults would fail on tables with existing rows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | 0008 migration file | mitigate | Use `python manage.py makemigrations` (auto-generated, no hand-edits per D-02); verify exactly 4 AddField ops, zero RunPython/RunSQL/RemoveField via acceptance_criteria grep gates |
| T-06-02 | Denial of Service | DailyGem table | mitigate | All four fields are nullable or have safe scalar defaults (default=dict, null=True) — ALTER TABLE will succeed on existing rows without rewriting data (D-03 no data migration) |
| T-06-03 | Tampering | score_breakdown default | mitigate | Acceptance grep enforces default=dict (callable), forbids default={} (mutable literal — Django/Python anti-pattern per D-04) |
| T-06-04 | Information Disclosure | taste_vector_snapshot JSONField | accept | Field stores a user's own taste vector (genre weights) — not PII, only accessible via authenticated user-scoped queries downstream in Phase 7 |
| T-06-05 | Repudiation | Migration history | accept | Django's django_migrations table records every applied migration with timestamp; auto-generated migration is reproducible from model state |
| T-06-06 | Elevation of Privilege | Schema migration | accept | makemigrations and migrate both require local shell access to the Django project; no remote attack surface in this phase (no view/API changes) |
</threat_model>

<verification>
After both tasks complete:

1. `cd backend && python manage.py makemigrations --check --dry-run` exits 0 (no pending model changes — model and migrations are in sync)
2. `cd backend && python manage.py migrate` applies 0008 cleanly to the dev DB
3. `cd backend && python manage.py showmigrations core` lists `[X] 0008_dailygem_score_breakdown_score_total_was_saved_taste_vector_snapshot`
4. Source-level grep gates from acceptance_criteria all pass
</verification>

<success_criteria>
- DailyGem model in models.py declares score_breakdown (JSONField default=dict blank=True), score_total (FloatField null=True blank=True), was_saved (BooleanField null=True blank=True), taste_vector_snapshot (JSONField null=True blank=True)
- Migration 0008 exists at canonical filename, depends on 0007, contains exactly 4 AddField operations on dailygem, contains no data-migration operations
- `python manage.py migrate` and `python manage.py makemigrations --check` both succeed
- Zero business logic changes — no view, engine, serializer, or test code touched in this plan
</success_criteria>

<output>
After completion, create `.planning/phases/06-schema-migration/06-01-SUMMARY.md` documenting:
- The exact filename of the generated migration
- The four field declarations as added to DailyGem
- Confirmation that `migrate` applied cleanly and `makemigrations --check` is clean
</output>
