---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - backend/apps/recommendations/personalization_engine.py
autonomous: true
requirements:
  - PHASE3-LEARNING
must_haves:
  truths:
    - "A LIKE on a track adds its genres to UserProfile.data['taste_vector'] with weight lr=0.1"
    - "A DISLIKE on a track decrements its genres in UserProfile.data['taste_vector'] by lr=0.1, clamping to 0"
    - "remove_feedback_learning() decrements the genres of the unliked track by lr=0.1, clamping to 0"
    - "All changes persist to DB via UserProfile.objects.save(update_fields=['data'])"
    - "TestTasteVectorUpdate tests in test_feedback_learning.py go GREEN"
  artifacts:
    - path: "backend/apps/recommendations/personalization_engine.py"
      provides: "Working apply_feedback_learning() and remove_feedback_learning() that update UserProfile.data['taste_vector']"
      contains: "UserProfile.objects.get"
  key_links:
    - from: "backend/apps/core/views.py"
      to: "backend/apps/recommendations/personalization_engine.py"
      via: "personalization_engine.apply_feedback_learning(feedback)"
      pattern: "apply_feedback_learning"
    - from: "backend/apps/recommendations/personalization_engine.py"
      to: "backend/apps/core/models.py"
      via: "UserProfile.objects.get(user=self.user)"
      pattern: "UserProfile.objects.get"
---

<objective>
Implement the online taste-vector update in PersonalizationEngine. Each LIKE increments genre weights; each DISLIKE decrements. Changes persist immediately to UserProfile.data['taste_vector'].

Purpose: After this plan, the engine actually learns from explicit feedback. The taste_vector built by _build_taste_vector() in HybridRecommendationEngine will be augmented (not replaced) by real-time feedback signals.
Output: personalization_engine.py with working apply_feedback_learning() and remove_feedback_learning().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-feedback-learning-loop/03-CONTEXT.md
@.planning/phases/03-feedback-learning-loop/03-RESEARCH.md
@.planning/phases/02-user-taste-vector-real-scoring/02-03-SUMMARY.md
@backend/apps/recommendations/personalization_engine.py
@backend/apps/core/models.py
@backend/tests/test_feedback_learning.py

<interfaces>
From backend/apps/core/models.py:
- class UserProfile(models.Model):
    user = OneToOneField(User)
    data = JSONField(default=dict)  -- stores: {'taste_vector': {genre: count}, 'preferences': {}, 'base_data': {}, 'source_stats': {}}
    def save(self, update_fields=None): standard Django save

- class UserFeedback(models.Model):
    user = ForeignKey(User)
    track = ForeignKey(Track)
    feedback_type = CharField(choices: LIKE/DISLIKE/SAVE/SKIP/PLAY)
    track_features = JSONField(default=dict)

- class Track(models.Model):
    spotify_id = CharField
    name = CharField
    artist = CharField
    genres = JSONField(default=list)  -- list of genre strings e.g. ['indie rock', 'folk']

From backend/apps/recommendations/personalization_engine.py (current):
- class PersonalizationEngine:
    def __init__(self, user): self.user = user; self.preferences = UserPreferences.objects.get_or_create(user=user)[0]
    def apply_feedback_learning(self, feedback): near-no-op, only logs
    def remove_feedback_learning(self, track_id): only deletes UserFeedback row

From backend/apps/core/views.py (call site — DO NOT MODIFY THIS FILE):
- Line ~616: personalization_engine.apply_feedback_learning(feedback)  -- feedback is a UserFeedback ORM object
- Line ~620: personalization_engine.remove_feedback_learning(track.spotify_id)  -- track_id is a string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement apply_feedback_learning() to update taste_vector online</name>
  <files>backend/apps/recommendations/personalization_engine.py</files>
  <read_first>
    backend/apps/recommendations/personalization_engine.py
    backend/apps/core/models.py
    backend/tests/test_feedback_learning.py
  </read_first>
  <behavior>
    - LIKE feedback (feedback_type == 'LIKE' or 'SAVE'): for each genre in feedback.track.genres, taste_vector[genre] += 0.1; clamp to max of 0 (can only grow on LIKE)
    - DISLIKE feedback (feedback_type == 'DISLIKE' or 'SKIP'): for each genre in feedback.track.genres, taste_vector[genre] = max(0.0, taste_vector.get(genre, 0) - 0.1)
    - Other feedback types: no taste_vector change (PLAY etc.)
    - Changes persist: profile.save(update_fields=['data']) called after mutation
    - Cold-start: if feedback.track.genres is empty list, log a warning and return without crash
    - No circular import: UserProfile imported inside method body using "from apps.core.models import UserProfile"
  </behavior>
  <action>
    Rewrite apply_feedback_learning() in personalization_engine.py. The method signature stays the same: def apply_feedback_learning(self, feedback: UserFeedback).

    Implementation steps (IN METHOD BODY — no module-level import of UserProfile):
    1. Import UserProfile inside the method: from apps.core.models import UserProfile
    2. Fetch profile: profile = UserProfile.objects.get(user=self.user)
    3. Get taste_vector: taste_vector = profile.data.get('taste_vector', {})
    4. Get genres from feedback.track.genres (already populated — no Spotify API call needed)
    5. If feedback.feedback_type in ('LIKE', 'SAVE'): for genre in genres: taste_vector[genre] = taste_vector.get(genre, 0.0) + 0.1
    6. If feedback.feedback_type in ('DISLIKE', 'SKIP'): for genre in genres: taste_vector[genre] = max(0.0, taste_vector.get(genre, 0.0) - 0.1)
    7. profile.data['taste_vector'] = taste_vector
    8. profile.save(update_fields=['data'])
    9. Log: logger.info(f"Updated taste_vector for user {self.user.id}: {len(genres)} genres from {feedback.feedback_type}")

    The learning rate 0.1 is a module-level constant: TASTE_VECTOR_LR = 0.1 (defined at top of file).

    Do NOT remove any other existing methods in personalization_engine.py. Only rewrite apply_feedback_learning().
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_feedback_learning.py::TestTasteVectorUpdate::test_like_increments_genre_weights tests/test_feedback_learning.py::TestTasteVectorUpdate::test_dislike_decrements_genre_weights -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TestTasteVectorUpdate.test_like_increments_genre_weights passes (GREEN)
    - TestTasteVectorUpdate.test_dislike_decrements_genre_weights passes (GREEN)
    - grep -n "UserProfile.objects.get" backend/apps/recommendations/personalization_engine.py returns at least 1 match inside apply_feedback_learning
    - grep -n "TASTE_VECTOR_LR" backend/apps/recommendations/personalization_engine.py returns at least 1 match
    - grep -n "update_fields=\[.data.\]" backend/apps/recommendations/personalization_engine.py returns at least 1 match inside apply_feedback_learning
    - No module-level "from apps.core.models import UserProfile" in personalization_engine.py (import must be inside method to avoid circular import)
  </acceptance_criteria>
  <done>apply_feedback_learning() reads UserProfile from DB, mutates taste_vector, and persists. Two TasteVectorUpdate tests go GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement remove_feedback_learning() to undo taste_vector update</name>
  <files>backend/apps/recommendations/personalization_engine.py</files>
  <read_first>
    backend/apps/recommendations/personalization_engine.py
    backend/tests/test_feedback_learning.py
  </read_first>
  <behavior>
    - remove_feedback_learning(track_id: str): finds the LIKE UserFeedback entry for this track+user, gets its track.genres, decrements each genre by TASTE_VECTOR_LR, clamps to 0, saves profile
    - If no UserFeedback LIKE row found for this track_id: log warning and return without crash
    - If track does not exist in DB: log warning and return without crash
    - Changes persist: profile.save(update_fields=['data'])
    - TestTasteVectorUpdate.test_remove_feedback_reverses_like goes GREEN
  </behavior>
  <action>
    Rewrite remove_feedback_learning() in personalization_engine.py. Signature stays: def remove_feedback_learning(self, track_id: str).

    Implementation steps (IN METHOD BODY):
    1. Import inside method: from apps.core.models import UserProfile, Track
    2. Try to fetch: track = Track.objects.get(spotify_id=track_id); handle DoesNotExist with logger.warning + return
    3. Fetch profile: profile = UserProfile.objects.get(user=self.user)
    4. Get taste_vector: taste_vector = profile.data.get('taste_vector', {})
    5. Get genres from track.genres
    6. For each genre: taste_vector[genre] = max(0.0, taste_vector.get(genre, 0.0) - TASTE_VECTOR_LR)
    7. profile.data['taste_vector'] = taste_vector; profile.save(update_fields=['data'])
    8. Log: logger.info(f"Reversed taste_vector for user {self.user.id}: removed {len(genres)} genre weights for track {track_id}")

    Do NOT delete the existing UserFeedback row in this method — that deletion happens in the caller (views.py) separately. This method ONLY handles the taste_vector update.

    Keep TASTE_VECTOR_LR = 0.1 constant from Task 1 (already defined).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_feedback_learning.py::TestTasteVectorUpdate -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All TestTasteVectorUpdate tests pass (GREEN) — at minimum test_like_increments_genre_weights, test_dislike_decrements_genre_weights, test_remove_feedback_reverses_like
    - grep -n "def remove_feedback_learning" backend/apps/recommendations/personalization_engine.py returns 1 match
    - grep -n "track.genres" backend/apps/recommendations/personalization_engine.py returns at least 1 match inside remove_feedback_learning
    - grep -n "update_fields=\[.data.\]" backend/apps/recommendations/personalization_engine.py returns at least 2 matches (one per method)
    - python -m pytest tests/test_feedback_learning.py::TestTasteVectorUpdate exits 0
  </acceptance_criteria>
  <done>remove_feedback_learning() correctly reverses taste_vector increments. All TestTasteVectorUpdate tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| views.py → PersonalizationEngine | UserFeedback ORM object passed to apply_feedback_learning — caller is authenticated (IsAuthenticated enforced in submit_feedback view) |
| PersonalizationEngine → UserProfile.data | JSONField write — concurrent submissions could corrupt data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | apply_feedback_learning — taste_vector manipulation | mitigate | Method only called from submit_feedback which requires IsAuthenticated; taste_vector keys are genre strings (no exec/eval risk) |
| T-03-04 | Denial of Service | Concurrent feedback submissions → JSONField race condition | accept | Single-user portfolio app; SQLite row-level locking prevents true concurrent writes; Django ORM get+save is not atomic but collision probability is negligible at demo scale |
| T-03-05 | Spoofing | Forged feedback_type bypassing view validation | mitigate | FeedbackSubmissionSerializer validates feedback_type against choices (LIKE/DISLIKE/SAVE/SKIP/PLAY) before ORM object is created; PersonalizationEngine receives already-validated UserFeedback object |
| T-03-06 | Information Disclosure | Taste vector exposed via personalization-summary endpoint | accept | Endpoint requires IsAuthenticated; data is user's own preference data |
</threat_model>

<verification>
cd backend && python -m pytest tests/test_feedback_learning.py::TestTasteVectorUpdate -v
Expected: all TestTasteVectorUpdate tests PASS (GREEN).
cd backend && python -m pytest tests/test_feedback_learning.py::TestThompsonBandit tests/test_feedback_learning.py::TestBellCurveNovelty -v
Expected: still FAIL (Wave 2 and Wave 3 not yet implemented).
</verification>

<success_criteria>
- apply_feedback_learning() writes to UserProfile.data['taste_vector'] and saves
- remove_feedback_learning() reverses the increment and saves
- All TestTasteVectorUpdate tests GREEN
- TestThompsonBandit and TestBellCurveNovelty still RED (correct — not yet implemented)
</success_criteria>

<output>
After completion, create .planning/phases/03-feedback-learning-loop/03-02-SUMMARY.md
</output>
