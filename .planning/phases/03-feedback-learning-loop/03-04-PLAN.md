---
phase: "03"
plan: "04"
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - backend/apps/recommendations/hybrid_recommendation_engine.py
  - backend/apps/core/views.py
  - backend/config/urls.py
autonomous: true
requirements:
  - PHASE3-LEARNING
must_haves:
  truths:
    - "novelty uses Gaussian bell-curve: exp(-((popularity - midpoint)^2) / (2 * width^2)) with defaults midpoint=30, width=20"
    - "preferred_popularity_range cold-start: if key absent from UserProfile.data['preferences'], midpoint=30 width=20 used"
    - "/api/daily-gem/ GET endpoint is registered in config/urls.py and returns HTTP 200 for authenticated users"
    - "get_daily_gem response includes 'score_breakdown': {'genre_sim': float, 'novelty': float, 'source': str} for the selected gem"
    - "TestBellCurveNovelty tests in test_feedback_learning.py go GREEN"
  artifacts:
    - path: "backend/apps/recommendations/hybrid_recommendation_engine.py"
      provides: "Bell-curve novelty in _score_recommendations()"
      contains: "math.exp"
    - path: "backend/apps/core/views.py"
      provides: "score_breakdown field in get_daily_gem() response"
      contains: "score_breakdown"
    - path: "backend/config/urls.py"
      provides: "api/daily-gem/ URL path"
      contains: "api/daily-gem/"
  key_links:
    - from: "frontend/app/profile/components/DailyGem/"
      to: "backend/config/urls.py"
      via: "GET /api/daily-gem/"
      pattern: "api/daily-gem/"
    - from: "backend/apps/core/views.py"
      to: "backend/apps/recommendations/hybrid_recommendation_engine.py"
      via: "_score_recommendations sets rec['score_breakdown']"
      pattern: "score_breakdown"
---

<objective>
Three final Phase 3 wires: (1) Replace linear novelty with a bell-curve Gaussian that peaks at the user's preferred popularity midpoint. (2) Add score_breakdown to the daily gem API response. (3) Register /api/daily-gem/ in URLs.

Purpose: The engine now discovers tracks matching the user's actual popularity sweet-spot (not just "obscure = good"). The gem response exposes the score components so the UI can show "why this gem". The /api/daily-gem/ URL makes the frontend DailyGem component functional.
Output: Bell-curve novelty in hybrid_recommendation_engine.py, score_breakdown in views.py get_daily_gem(), /api/daily-gem/ path in config/urls.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-feedback-learning-loop/03-CONTEXT.md
@.planning/phases/03-feedback-learning-loop/03-RESEARCH.md
@.planning/phases/02-user-taste-vector-real-scoring/02-03-SUMMARY.md
@.planning/phases/03-feedback-learning-loop/03-02-SUMMARY.md
@.planning/phases/03-feedback-learning-loop/03-03-SUMMARY.md
@backend/apps/recommendations/hybrid_recommendation_engine.py
@backend/apps/core/views.py
@backend/config/urls.py
@backend/tests/test_feedback_learning.py

<interfaces>
From backend/apps/recommendations/hybrid_recommendation_engine.py (current _score_recommendations, line ~742):
- novelty = 1.0 - (rec.get('popularity', 50) / 100.0)   ← REPLACE THIS LINE with bell curve
- rec['score'] = 0.4 * genre_sim + 0.3 * novelty + 0.3 * feedback_multiplier   ← LOCKED, unchanged
- self.profile.data.get('preferences', {}) already accessed in method for liked/disliked artists
- source_weights applied as multiplier after score (added in Plan 03)

Bell-curve formula:
  novelty = math.exp(-((popularity - midpoint)**2) / (2 * width**2))
  midpoint, width from self.profile.data.get('preferences', {}).get('preferred_popularity_range', {'midpoint': 30, 'width': 20})
  import math is needed (standard library)

From backend/apps/core/views.py (current get_daily_gem, line ~980):
- gem_data = candidates[0]  — the selected recommendation dict
- gem_data may contain 'score' and 'score_breakdown' keys if _score_recommendations() sets them

From backend/apps/core/views.py (JsonResponse return at line ~1107):
  return JsonResponse({
      'track': {...},
      'explanation': explanation,
      'date': str(gem.date),
      'cached': True,   ← (from cached branch)
  })
  Need to add: 'score_breakdown': gem_data.get('score_breakdown', {})

From backend/config/urls.py (current paths):
- path('api/recommendations/', views.get_recommendations, name='get_recommendations')
- path('api/submit-feedback/', views.submit_feedback, name='submit_feedback')
- No 'api/daily-gem/' path currently registered

From backend/apps/core/views.py:
- get_daily_gem is already defined (view function, lines ~978-1127)
- Need to verify: is it imported in views.py's own file? Yes — it's a function in views.py itself
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace linear novelty with Gaussian bell-curve in _score_recommendations()</name>
  <files>backend/apps/recommendations/hybrid_recommendation_engine.py</files>
  <read_first>
    backend/apps/recommendations/hybrid_recommendation_engine.py
    backend/tests/test_feedback_learning.py
  </read_first>
  <behavior>
    - Bell curve: novelty = math.exp(-((popularity - midpoint)**2) / (2 * width**2))
    - midpoint and width come from self.profile.data.get('preferences', {}).get('preferred_popularity_range', {'midpoint': 30, 'width': 20})
    - Cold-start defaults: midpoint=30, width=20
    - A track at popularity=30 with midpoint=30 scores novelty=1.0 (peak)
    - A track at popularity=0 with midpoint=30, width=20 scores novelty=exp(-((0-30)^2)/(2*20^2)) = exp(-900/800) ≈ 0.325
    - A track at popularity=100 with midpoint=30, width=20 scores novelty=exp(-((100-30)^2)/(2*20^2)) = exp(-4900/800) ≈ 0.002
    - TestBellCurveNovelty.test_novelty_peaks_at_preferred_midpoint passes GREEN
    - TestBellCurveNovelty.test_cold_start_midpoint_default_is_30 passes GREEN
    - Phase 2 scoring tests in test_recommendation_scoring.py must still pass (the locked formula 0.4*genre_sim + 0.3*novelty + 0.3*feedback_multiplier is unchanged — only the novelty calculation changes)
  </behavior>
  <action>
    In _score_recommendations() in hybrid_recommendation_engine.py:

    1. Add "import math" at the top of the file if not already present.
    2. Before the "for rec in recommendations:" loop, add:
       prefs = self.profile.data.get('preferences', {})
       pop_range = prefs.get('preferred_popularity_range', {'midpoint': 30, 'width': 20})
       midpoint = pop_range.get('midpoint', 30)
       width = pop_range.get('width', 20)
    3. Replace the existing novelty line:
       OLD: novelty = 1.0 - (rec.get('popularity', 50) / 100.0)
       NEW: popularity = rec.get('popularity', 50); novelty = math.exp(-((popularity - midpoint) ** 2) / (2 * width ** 2))

    IMPORTANT: The variable name "popularity" already exists as rec.get('popularity', 50) inside the loop in some form. Rename the inner variable to "popularity" (an integer/float) before computing novelty. Ensure there is no name collision with any other local variable.

    Also store score components on the rec dict for use by the daily gem view:
    After computing genre_sim, novelty, and feedback_multiplier but BEFORE setting rec['score']:
    rec['score_breakdown'] = {'genre_sim': round(genre_sim, 4), 'novelty': round(novelty, 4), 'feedback_multiplier': round(feedback_multiplier, 4), 'source': rec.get('source', '')}

    The existing "rec['score'] = 0.4 * genre_sim + 0.3 * novelty + 0.3 * feedback_multiplier" line MUST remain unchanged (LOCKED formula).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_feedback_learning.py::TestBellCurveNovelty tests/test_recommendation_scoring.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - TestBellCurveNovelty.test_novelty_peaks_at_preferred_midpoint passes GREEN
    - TestBellCurveNovelty.test_cold_start_midpoint_default_is_30 passes GREEN
    - python -m pytest tests/test_recommendation_scoring.py exits 0 (no Phase 2 regression)
    - grep -n "math.exp" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 1 match inside _score_recommendations
    - grep -n "midpoint" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 2 matches (definition + use)
    - grep -n "preferred_popularity_range" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 1 match
    - grep -n "score_breakdown" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 1 match (rec['score_breakdown'] = ...)
    - grep -n "0.4 \* genre_sim" backend/apps/recommendations/hybrid_recommendation_engine.py returns 1 match (locked formula still present)
    - Novelty for popularity=30 with midpoint=30 is approximately 1.0: python3 -c "import math; print(math.exp(-((30-30)**2)/(2*20**2)))" outputs 1.0
  </acceptance_criteria>
  <done>Bell-curve novelty active. score_breakdown stored on rec dict. TestBellCurveNovelty GREEN. Phase 2 tests still GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add score_breakdown to get_daily_gem response and register /api/daily-gem/ URL</name>
  <files>backend/apps/core/views.py, backend/config/urls.py</files>
  <read_first>
    backend/apps/core/views.py
    backend/config/urls.py
  </read_first>
  <action>
    Two changes in this task:

    CHANGE A — views.py (get_daily_gem response):
    Read views.py and find the get_daily_gem() function. There are TWO return JsonResponse blocks:
    1. The "cached" branch (returns existing gem): add 'score_breakdown': {} to the response (empty breakdown for cached gems — we don't re-score)
    2. The "fresh" branch (newly created gem): add 'score_breakdown': gem_data.get('score_breakdown', {}) to the response

    In the fresh branch, gem_data is the recommendation dict selected as candidates[0]. After Task 1 of this plan, gem_data will contain 'score_breakdown' set by _score_recommendations(). Access it as gem_data.get('score_breakdown', {}).

    The existing response fields ('track', 'explanation', 'date') are unchanged. Only add 'score_breakdown' as a new top-level key.

    CHANGE B — config/urls.py:
    Read config/urls.py. Find the urlpatterns list. Add this path:
      path('api/daily-gem/', views.get_daily_gem, name='get_daily_gem'),
    Place it near the other api/ paths (e.g. after 'api/recommendations/').
    Verify that views.get_daily_gem is importable — the function is already defined in apps/core/views.py and the file already imports from apps.core import views (or uses the views variable). No new import needed in urls.py.

    Do NOT modify any other view function or URL. Do NOT change the get_daily_gem view logic — only add 'score_breakdown' to the two JsonResponse return statements.
  </action>
  <verify>
    <automated>cd backend && python -c "import django; django.setup(); from apps.core import views; print('get_daily_gem' in dir(views))" && python -c "from django.urls import reverse; import django; django.setup(); print(reverse('get_daily_gem'))"</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "api/daily-gem/" backend/config/urls.py returns 1 match
    - grep -n "get_daily_gem" backend/config/urls.py returns 1 match
    - grep -c "score_breakdown" backend/apps/core/views.py returns 2 (one in cached branch, one in fresh branch)
    - python -c "from django.urls import reverse; import django; django.setup(); print(reverse('get_daily_gem'))" prints "/api/daily-gem/"
    - curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/api/daily-gem/ returns 403 (unauthenticated = forbidden, not 404 = route exists)
    - The fresh branch response includes key 'score_breakdown' as a dict: grep -A3 "score_breakdown" backend/apps/core/views.py shows gem_data.get('score_breakdown', {})
  </acceptance_criteria>
  <done>/api/daily-gem/ URL registered. Both response branches include score_breakdown. Route returns 403 for unauthenticated (not 404).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/daily-gem/ | Unauthenticated requests must be rejected (403); score_breakdown must not leak other users' data |
| recommendation dict → score_breakdown | score_breakdown values computed server-side; not user-controlled input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Spoofing | GET /api/daily-gem/ without authentication | mitigate | @permission_classes([IsAuthenticated]) already on get_daily_gem — unauthenticated requests return 403, not 401; enforced by DRF |
| T-03-12 | Information Disclosure | score_breakdown exposes internal scoring weights | accept | score_breakdown shows genre_sim, novelty, feedback_multiplier per-track — reveals only user's own taste vector signal; no cross-user data. Low-sensitivity. |
| T-03-13 | Denial of Service | math.exp underflow on extreme (popularity, midpoint, width) combinations | accept | math.exp(-x) approaches 0.0 for large x — Python does not raise on underflow, returns 0.0; score is valid float |
| T-03-14 | Tampering | preferred_popularity_range injected via unvalidated API | mitigate | preferred_popularity_range is written only by internal engine methods (not exposed as a user-writable API field in Phase 3). Verify no external endpoint writes it. |
| T-03-15 | Elevation of Privilege | /api/daily-gem/ accessible to wrong user's data via path traversal | accept | View uses request.user (session-bound) for all DB queries — no user_id in URL path; no traversal risk |
</threat_model>

<verification>
cd backend && python -m pytest tests/test_feedback_learning.py -v
Expected: ALL tests GREEN (TestTasteVectorUpdate + TestThompsonBandit + TestBellCurveNovelty).

cd backend && python -m pytest tests/test_recommendation_scoring.py -v
Expected: all Phase 2 scoring tests still GREEN (no regression).

Check URL registered:
python -c "import django; django.setup(); from django.urls import reverse; print(reverse('get_daily_gem'))"
Expected: prints "/api/daily-gem/"

Check score_breakdown in view:
grep -c "score_breakdown" backend/apps/core/views.py
Expected: 2 (one per response branch)
</verification>

<success_criteria>
- Bell-curve novelty peaks at preferred popularity midpoint with Gaussian exp formula
- /api/daily-gem/ is registered and returns 403 for unauthenticated (not 404)
- get_daily_gem response includes score_breakdown dict with genre_sim, novelty, feedback_multiplier, source
- All 7+ tests in test_feedback_learning.py GREEN
- All Phase 2 scoring tests in test_recommendation_scoring.py still GREEN
</success_criteria>

<output>
After completion, create .planning/phases/03-feedback-learning-loop/03-04-SUMMARY.md
</output>
