---
phase: "03"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_feedback_learning.py
  - backend/requirements.txt
autonomous: true
requirements:
  - PHASE3-LEARNING
must_haves:
  truths:
    - "Test file exists with stubs for all three Phase 3 behaviors (taste-vector update, bandit sampling, bell-curve novelty)"
    - "openai is present in requirements.txt"
    - "All test stubs fail (RED) when run against current code"
  artifacts:
    - path: "backend/tests/test_feedback_learning.py"
      provides: "Failing test stubs for taste vector online update, Thompson bandit, bell-curve novelty"
      exports: ["TestTasteVectorUpdate", "TestThompsonBandit", "TestBellCurveNovelty"]
    - path: "backend/requirements.txt"
      provides: "openai package declaration"
      contains: "openai"
  key_links:
    - from: "backend/tests/test_feedback_learning.py"
      to: "backend/apps/recommendations/personalization_engine.py"
      via: "import PersonalizationEngine"
      pattern: "from apps.recommendations.personalization_engine import PersonalizationEngine"
    - from: "backend/tests/test_feedback_learning.py"
      to: "backend/apps/recommendations/hybrid_recommendation_engine.py"
      via: "import HybridRecommendationEngine"
      pattern: "from apps.recommendations.hybrid_recommendation_engine import HybridRecommendationEngine"
---

<objective>
Create failing test stubs for the three Phase 3 behaviors before any implementation begins. Confirm openai is declared in requirements.txt.

Purpose: Gate-enforce TDD flow. Wave 1–3 plans make these tests go green; tests cannot pass until the engine actually learns.
Output: backend/tests/test_feedback_learning.py (failing stubs), requirements.txt with openai.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-feedback-learning-loop/03-CONTEXT.md
@.planning/phases/03-feedback-learning-loop/03-RESEARCH.md
@backend/tests/test_recommendation_scoring.py
@backend/apps/recommendations/personalization_engine.py
@backend/apps/recommendations/hybrid_recommendation_engine.py
@backend/requirements.txt

<interfaces>
From backend/apps/recommendations/personalization_engine.py (current state):
- class PersonalizationEngine: def __init__(self, user) — stores self.user, creates self.preferences via UserPreferences.objects.get_or_create
- def apply_feedback_learning(self, feedback: UserFeedback) — currently near-no-op
- def remove_feedback_learning(self, track_id: str) — currently only deletes UserFeedback ORM row

From backend/apps/recommendations/hybrid_recommendation_engine.py (current state after Phase 2):
- def get_recommendation_weights(self) — returns static dict from UserProfile.data['recommendation_weights']
- def _score_recommendations(self, recommendations) — novelty = 1.0 - (popularity / 100.0), LOCKED formula 0.4*genre_sim + 0.3*novelty + 0.3*feedback_multiplier

From backend/apps/core/models.py:
- class UserProfile: data = JSONField(default=dict) — stores taste_vector, preferences, source_stats, base_data
- class UserFeedback: feedback_type (LIKE/DISLIKE/SAVE/SKIP/PLAY), track FK, track_features JSONField

From backend/tests/test_recommendation_scoring.py (analog for test style):
- Uses unittest.TestCase with django.test.TestCase pattern
- Uses Mock objects to avoid DB in unit tests
- make_engine() helper creates HybridRecommendationEngine with mocked profile.data
- _make_rec() helper builds recommendation dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing test stubs for all three Phase 3 behaviors</name>
  <files>backend/tests/test_feedback_learning.py</files>
  <read_first>
    backend/tests/test_recommendation_scoring.py
    backend/apps/recommendations/personalization_engine.py
    backend/apps/recommendations/hybrid_recommendation_engine.py
    backend/apps/core/models.py
  </read_first>
  <behavior>
    - TestTasteVectorUpdate.test_like_increments_genre_weights: after apply_feedback_learning(feedback) where feedback.feedback_type='LIKE' and feedback.track.genres=['indie rock'], UserProfile.data['taste_vector']['indie rock'] is greater than its initial value
    - TestTasteVectorUpdate.test_dislike_decrements_genre_weights: after apply_feedback_learning(feedback) where feedback_type='DISLIKE' and genres=['pop'], taste_vector['pop'] is less than initial value (or zero-clamped if negative)
    - TestTasteVectorUpdate.test_remove_feedback_reverses_like: after apply_feedback_learning(like) then remove_feedback_learning(track_id), taste_vector change is reversed (decremented or removed)
    - TestThompsonBandit.test_beta_sample_increases_with_successes: after recording 5 successes for 'artist_network' source, get_recommendation_weights()['artist_network'] exceeds the cold-start static default weight (0.25)
    - TestThompsonBandit.test_cold_start_returns_static_defaults: with source_stats empty or all sources having s+f < 3, get_recommendation_weights() returns the same dict as UserProfile.get_recommendation_weights() (static defaults)
    - TestBellCurveNovelty.test_novelty_peaks_at_preferred_midpoint: with preferred_popularity_range={'midpoint': 30, 'width': 20}, a track with popularity=30 scores higher novelty than a track with popularity=0 or popularity=100
    - TestBellCurveNovelty.test_cold_start_midpoint_default_is_30: with no preferred_popularity_range in UserProfile.data, midpoint defaults to 30 and width defaults to 20
  </behavior>
  <action>
    Create backend/tests/test_feedback_learning.py using the same unittest.TestCase subclass style as test_recommendation_scoring.py.

    Imports: import unittest, import math, from unittest.mock import Mock, patch, from django.test import TestCase (for DB-involved tests), from apps.recommendations.personalization_engine import PersonalizationEngine, from apps.recommendations.hybrid_recommendation_engine import HybridRecommendationEngine, from apps.core.models import UserProfile, UserFeedback, Track.

    Use two layers:
    1. Pure-logic unit tests (no DB): mock UserProfile with profile.data dict, mock feedback with feedback.feedback_type and feedback.track.genres. Call the target methods directly against mock objects.
    2. DB-integrated tests for apply_feedback_learning/remove_feedback_learning: use Django TestCase, create a User + UserProfile in setUp, create a Track with genres=['indie rock'], create a UserFeedback ORM row, call PersonalizationEngine(user).apply_feedback_learning(feedback), then reload UserProfile from DB and assert taste_vector changed.

    Each test class must have at least one @unittest.expectedFailure or self.fail() stub so the file starts fully RED. Do NOT write passing implementations — write assertions against the DESIRED behavior that the current no-op code will not satisfy.

    For TestThompsonBandit: create a helper make_engine_with_stats(source_name, successes, failures) that returns a HybridRecommendationEngine instance with profile.data['source_stats'] = {source_name: {'s': successes, 'f': failures}}.

    For TestBellCurveNovelty: create a helper make_engine_with_prefs(midpoint, width) that returns a HybridRecommendationEngine instance with profile.data['preferences']['preferred_popularity_range'] = {'midpoint': midpoint, 'width': width}. Create _make_rec(popularity) helper building a minimal rec dict.

    File structure:
    - Header: django/unittest imports, sys.path setup if needed
    - class TestTasteVectorUpdate (django.test.TestCase) — DB-integrated
    - class TestThompsonBandit (unittest.TestCase) — pure unit, mocked profile
    - class TestBellCurveNovelty (unittest.TestCase) — pure unit, mocked profile
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_feedback_learning.py -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists at backend/tests/test_feedback_learning.py
    - File imports PersonalizationEngine and HybridRecommendationEngine without ImportError (python -c "import django; django.setup(); from apps.recommendations.personalization_engine import PersonalizationEngine" exits 0)
    - pytest run shows at least 7 test items collected
    - All 7 tests FAIL (RED) — no test should pass against current no-op code
    - No SyntaxError or ImportError in the file itself
    - grep -c "def test_" backend/tests/test_feedback_learning.py returns 7 or more
  </acceptance_criteria>
  <done>7+ failing test stubs in backend/tests/test_feedback_learning.py. All RED against current implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Ensure openai is in requirements.txt</name>
  <files>backend/requirements.txt</files>
  <read_first>
    backend/requirements.txt
    backend/apps/ai/ai_feedback_service.py
  </read_first>
  <action>
    Read backend/requirements.txt. If the line "openai" (or "openai>=X.Y.Z") is NOT already present, append "openai>=1.0.0" to requirements.txt. If it is already present, no change needed.

    Also verify openai is actually importable in the virtualenv. Run: cd backend && python -c "import openai; print(openai.__version__)" and if it fails with ModuleNotFoundError, run pip install openai>=1.0.0.

    Do not change any other dependency. Do not change the openai version if one is already pinned (leave it as-is if present).
  </action>
  <verify>
    <automated>cd backend && python -c "import openai; print(openai.__version__)" && grep -v "^#" requirements.txt | grep -c openai</automated>
  </verify>
  <acceptance_criteria>
    - "cd backend && python -c 'import openai'" exits with code 0 (no ModuleNotFoundError)
    - grep -v "^#" backend/requirements.txt | grep -c openai returns 1
    - openai.__version__ prints a semantic version string (e.g. "1.x.x")
  </acceptance_criteria>
  <done>openai importable and declared in requirements.txt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → app code | Tests import and call real engine methods — test isolation required to prevent state bleed between test runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | test_feedback_learning.py test isolation | mitigate | Each Django TestCase test runs in transaction that rolls back — no cross-test DB pollution; mock profile.data for unit tests to avoid real DB |
| T-03-02 | Information Disclosure | openai API key in test environment | accept | Tests mock or skip OpenAI calls; RecommendationExplainer fallback path works without OPENAI_API_KEY |
</threat_model>

<verification>
Run full check: cd backend && python -m pytest tests/test_feedback_learning.py -v
Expected: 7+ tests collected, 7+ FAILED (RED state confirmed).
Run: grep -v "^#" backend/requirements.txt | grep openai — expect 1 match.
</verification>

<success_criteria>
- backend/tests/test_feedback_learning.py exists with 7+ test methods across 3 test classes
- All tests FAIL against current code (RED)
- openai is importable and in requirements.txt
</success_criteria>

<output>
After completion, create .planning/phases/03-feedback-learning-loop/03-01-SUMMARY.md
</output>
