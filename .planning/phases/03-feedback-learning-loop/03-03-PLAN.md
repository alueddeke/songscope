---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/apps/recommendations/hybrid_recommendation_engine.py
autonomous: true
requirements:
  - PHASE3-LEARNING
must_haves:
  truths:
    - "get_recommendation_weights() uses Thompson Sampling (Beta distribution) to sample weights from UserProfile.data['source_stats']"
    - "Cold-start: if a source has s+f < 3, its static default weight is used instead of sampling"
    - "_score_recommendations() applies source weights as a post-score multiplier: score *= source_weights.get(rec.get('source', ''), 1.0)"
    - "The LOCKED formula 0.4*genre_sim + 0.3*novelty + 0.3*feedback_multiplier is unchanged"
    - "TestThompsonBandit tests in test_feedback_learning.py go GREEN"
  artifacts:
    - path: "backend/apps/recommendations/hybrid_recommendation_engine.py"
      provides: "Thompson Sampling in get_recommendation_weights(), source weight multiplier in _score_recommendations()"
      contains: "Beta(s+1, f+1)"
  key_links:
    - from: "backend/apps/recommendations/hybrid_recommendation_engine.py"
      to: "backend/apps/core/models.py"
      via: "self.profile.data.get('source_stats', {})"
      pattern: "source_stats"
    - from: "_score_recommendations"
      to: "get_recommendation_weights"
      via: "source_weights.get(rec.get('source', ''), 1.0)"
      pattern: "source_weights"
---

<objective>
Replace the static source weight lookup in HybridRecommendationEngine with Thompson Sampling. Each of the 5 candidate sources (playlist_mining, artist_network, genre_search, related_artists, contextual) tracks successes and failures in UserProfile.data['source_stats']. Weights are sampled from Beta(s+1, f+1) distributions and normalized. The sampled weights are applied as a post-score multiplier in _score_recommendations().

Purpose: The bandit learns which source produces liked tracks and allocates more exploration budget there. Cold-start sources (< 3 observations) use static defaults.
Output: Two methods in hybrid_recommendation_engine.py — get_recommendation_weights() with Beta sampling, _score_recommendations() with source weight multiplier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-feedback-learning-loop/03-CONTEXT.md
@.planning/phases/03-feedback-learning-loop/03-RESEARCH.md
@.planning/phases/02-user-taste-vector-real-scoring/02-03-SUMMARY.md
@backend/apps/recommendations/hybrid_recommendation_engine.py
@backend/apps/core/models.py
@backend/tests/test_feedback_learning.py

<interfaces>
From backend/apps/core/models.py:
- class UserProfile.data (JSONField): expected schema after this plan:
  {
    'taste_vector': {'genre': count},
    'preferences': {'liked_artists': [], 'disliked_artists': [], 'preferred_popularity_range': {'midpoint': 30, 'width': 20}},
    'source_stats': {
      'playlist_mining': {'s': 0, 'f': 0},
      'artist_network': {'s': 0, 'f': 0},
      'genre_search': {'s': 0, 'f': 0},
      'related_artists': {'s': 0, 'f': 0},
      'contextual': {'s': 0, 'f': 0}
    },
    'base_data': {'top_artists': [], 'top_tracks': []}
  }

- def get_recommendation_weights(self): returns static dict — this method is on UserProfile model
    Static defaults: {'playlist_mining': 0.3, 'artist_network': 0.25, 'genre_search': 0.2, 'related_artists': 0.15, 'contextual': 0.1}

From backend/apps/recommendations/hybrid_recommendation_engine.py (current state after Phase 2):
- def get_recommendation_weights(self): calls self.profile.get_recommendation_weights() — returns static dict
- def _score_recommendations(self, recommendations): computes 0.4*genre_sim + 0.3*novelty + 0.3*feedback_multiplier, does NOT apply source weights
- self.profile is a UserProfile ORM instance (assigned in __init__ via UserProfile.objects.get_or_create(user=self.user))

Standard library available: import random (random.betavariate(a, b) implements Beta sampling)
numpy is imported in the file already.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace get_recommendation_weights() with Thompson Sampling bandit</name>
  <files>backend/apps/recommendations/hybrid_recommendation_engine.py</files>
  <read_first>
    backend/apps/recommendations/hybrid_recommendation_engine.py
    backend/apps/core/models.py
    backend/tests/test_feedback_learning.py
  </read_first>
  <behavior>
    - COLD_START_THRESHOLD = 3 (constant at class or module level): if source has s+f < threshold, use static default weight for that source
    - For sources meeting threshold: sample theta = random.betavariate(s+1, f+1)
    - Collect all thetas (using default for cold-start sources), normalize by sum so weights sum to 1.0
    - Return dict: {'playlist_mining': w1, 'artist_network': w2, 'genre_search': w3, 'related_artists': w4, 'contextual': w5}
    - If source_stats is completely empty (no data at all): return static defaults unchanged (pure cold-start)
    - TestThompsonBandit.test_beta_sample_increases_with_successes: passes GREEN
    - TestThompsonBandit.test_cold_start_returns_static_defaults: passes GREEN
  </behavior>
  <action>
    Rewrite the get_recommendation_weights() METHOD ON HybridRecommendationEngine (NOT on UserProfile — that method stays unchanged). The HybridRecommendationEngine method currently calls self.profile.get_recommendation_weights(). Replace this with Thompson Sampling logic.

    Static defaults dict (define as a module-level constant or class variable SOURCE_DEFAULTS):
    SOURCE_DEFAULTS = {'playlist_mining': 0.3, 'artist_network': 0.25, 'genre_search': 0.2, 'related_artists': 0.15, 'contextual': 0.1}

    COLD_START_THRESHOLD = 3 (module-level constant)

    Implementation:
    1. source_stats = self.profile.data.get('source_stats', {})
    2. If source_stats is empty: return dict(SOURCE_DEFAULTS)
    3. For each source in SOURCE_DEFAULTS:
       stats = source_stats.get(source, {'s': 0, 'f': 0})
       n = stats['s'] + stats['f']
       If n < COLD_START_THRESHOLD: theta[source] = SOURCE_DEFAULTS[source]
       Else: theta[source] = random.betavariate(stats['s'] + 1, stats['f'] + 1)
    4. total = sum(theta.values()); if total == 0: return dict(SOURCE_DEFAULTS)
    5. Normalize: return {k: v / total for k, v in theta.items()}

    Use import random (standard library, already in Python stdlib — add import random at top of file if not already present).

    DO NOT remove or rename the method. The signature remains: def get_recommendation_weights(self) -> dict.
    DO NOT modify UserProfile.get_recommendation_weights() — that method provides the static defaults and is untouched.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_feedback_learning.py::TestThompsonBandit -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - TestThompsonBandit.test_beta_sample_increases_with_successes passes GREEN
    - TestThompsonBandit.test_cold_start_returns_static_defaults passes GREEN
    - grep -n "betavariate" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 1 match
    - grep -n "COLD_START_THRESHOLD" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 1 match
    - grep -n "source_stats" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 1 match
    - python -m pytest tests/test_feedback_learning.py::TestThompsonBandit exits 0
  </acceptance_criteria>
  <done>get_recommendation_weights() samples from Beta distributions. TestThompsonBandit tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Apply source weights as post-score multiplier in _score_recommendations()</name>
  <files>backend/apps/recommendations/hybrid_recommendation_engine.py</files>
  <read_first>
    backend/apps/recommendations/hybrid_recommendation_engine.py
    backend/tests/test_feedback_learning.py
  </read_first>
  <action>
    Modify _score_recommendations() to apply source weights as a post-score multiplier. The LOCKED formula 0.4*genre_sim + 0.3*novelty + 0.3*feedback_multiplier MUST NOT change.

    Add these two lines in _score_recommendations(), AFTER computing rec['score'] via the locked formula and BEFORE the final sort:

    Line A (before the loop): source_weights = self.get_recommendation_weights()
    Line B (inside the loop, after rec['score'] = ...): rec['score'] *= source_weights.get(rec.get('source', ''), 1.0)

    The exact insertion points:
    - source_weights = self.get_recommendation_weights() is added ONCE, BEFORE the "for rec in recommendations:" loop
    - rec['score'] *= source_weights.get(rec.get('source', ''), 1.0) is added INSIDE the loop, immediately AFTER the line "rec['score'] = 0.4 * genre_sim + 0.3 * novelty + 0.3 * feedback_multiplier"

    The fallback for unknown source key is 1.0 (neutral — no boost or penalty). This is correct: if rec has no 'source' key, the multiplier is 1.0 and the score is unchanged.

    DO NOT change any other logic in _score_recommendations(). The final sort by score descending is unchanged.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_feedback_learning.py tests/test_recommendation_scoring.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "source_weights = self.get_recommendation_weights()" backend/apps/recommendations/hybrid_recommendation_engine.py returns 1 match inside _score_recommendations
    - grep -n "source_weights.get" backend/apps/recommendations/hybrid_recommendation_engine.py returns at least 1 match
    - grep -n "0.4 \* genre_sim" backend/apps/recommendations/hybrid_recommendation_engine.py returns 1 match (locked formula unchanged)
    - python -m pytest tests/test_recommendation_scoring.py exits 0 (Phase 2 tests still GREEN — no regression)
    - python -m pytest tests/test_feedback_learning.py::TestThompsonBandit exits 0
    - The line "rec['score'] *= source_weights.get" appears after "rec['score'] = 0.4 * genre_sim" in the file (verify with grep -n)
  </acceptance_criteria>
  <done>source weight multiplier applied in _score_recommendations(). LOCKED formula unchanged. Phase 2 scoring tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UserProfile.data → get_recommendation_weights() | source_stats values read from JSONField — must handle missing keys gracefully |
| recommendation dict → _score_recommendations() | rec['source'] comes from strategy method return values — should be one of the 5 known strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | source_stats manipulation via crafted UserProfile.data | accept | UserProfile.data is server-side only; never written from user-controlled API input directly. source_stats written only by engine internals |
| T-03-08 | Denial of Service | betavariate(0+1, 0+1) = Beta(1,1) = uniform random — no crash | accept | Beta(1,1) is a valid distribution (uniform); cold-start threshold prevents pathological sampling before enough observations |
| T-03-09 | Spoofing | rec['source'] set to an unexpected string → multiplier 1.0 | accept | Fallback of 1.0 is neutral — unknown source gets no boost or penalty, not a security risk |
| T-03-10 | Information Disclosure | Thompson weights visible to frontend via recommendations response | accept | Weights not exposed in API response; only final sorted scores are returned |
</threat_model>

<verification>
cd backend && python -m pytest tests/test_feedback_learning.py::TestThompsonBandit tests/test_recommendation_scoring.py -v
Expected: TestThompsonBandit GREEN, Phase 2 scoring tests still GREEN.
cd backend && python -m pytest tests/test_feedback_learning.py::TestBellCurveNovelty -v
Expected: still FAIL (Wave 3 not yet implemented).
</verification>

<success_criteria>
- get_recommendation_weights() uses Thompson Sampling with Beta(s+1, f+1)
- Cold-start sources (s+f < 3) use static defaults
- _score_recommendations() multiplies base score by source weight
- LOCKED formula 0.4*genre_sim + 0.3*novelty + 0.3*feedback_multiplier unchanged
- TestThompsonBandit tests GREEN, Phase 2 scoring tests still GREEN
</success_criteria>

<output>
After completion, create .planning/phases/03-feedback-learning-loop/03-03-SUMMARY.md
</output>
